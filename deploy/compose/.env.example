# SPDX-FileCopyrightText: 2026 Olivares.AI
# SPDX-License-Identifier: AGPL-3.0-only
#
# Copy to .env and fill in. `docker compose` reads this automatically.

# --- Image (PIN BY DIGEST in production) -----------------------------------
# Default is the floating tag for convenience; for a verifiable deploy set the
# digest you verified with cosign (scripts/verify-release.sh / cosign verify):
#   OLIVARES_IMAGE=docker.io/olivaresai/olivares@sha256:<digest>
# docker.io is the official registry. ghcr.io/olivaresai/olivares is the fallback:
# same digest, same signatures, and no anonymous-pull rate limit.
OLIVARES_IMAGE=docker.io/olivaresai/olivares:latest

# --- Postgres override only (docker-compose.postgres.yml) ------------------
# The Postgres superuser password (used once, at cluster init).
POSTGRES_SUPERUSER_PASSWORD=change-me-superuser
# The least-privilege olivares_app role password (engine connects as this).
OLIVARES_DB_PASSWORD=change-me-app
# Optional: pin the Postgres image by digest too.
# OLIVARES_POSTGRES_IMAGE=postgres@sha256:<digest>

# Distinct URI-safe password for the PostgreSQL administrative read pool.
OLIVARES_ADMIN_PASSWORD=

# --- Where the host publishes the console ----------------------------------
# The engine binds every interface INSIDE the container; this variable decides
# which host address the port is published on. The default, 0.0.0.0, is every
# interface of the host: this is a server, the console serves TLS with a
# self-signed first-boot certificate, and there are no default credentials.
#
# Set it to 127.0.0.1 to restrict the console to the host itself, or to one
# interface address to publish on just that one. A published Docker port is
# reached through Docker's own DNAT rules, ahead of a host firewall's INPUT
# chain, so this variable — not `ufw deny 8443` — is what closes it.
# OLIVARES_BIND=127.0.0.1

# --- The address a browser reaches the console at --------------------------
# The engine binds :8443 inside the container, which is a BIND and not an
# address: the first-boot banner can only offer a same-machine URL, and a
# passkey relying party cannot be derived from it. Set this to the https:// URL
# your operators actually type — through your reverse proxy, ingress or host
# name — and the banner prints it. Passkeys then work at that address provided
# it is a host name the verifier accepts (an IP address is not one) and the
# console is reached over a secure context.
#
# Leave it unset for a purely local trial: empty means "no declared address" and
# the engine behaves exactly as before. It is read at start-up only.
# OLIVARES_PUBLIC_URL=https://olivares.example.com
