# Commit outcome hosted oracle — expected exits (design of `commit-outcome-oracle.yml`)

**Placement.** Repository `olivaresai/olivares`. The workflow is `.github/workflows/commit-outcome-oracle.yml`. The mutants and this file are `ci/oracles/commit-outcome/*.patch` and `ci/oracles/commit-outcome/EXPECTED.md`. Every job runs on `ubuntu-latest`. `oracle`, `routes` and `generated` each assert `runner.environment == github-hosted` in their first step, and `verdict` asserts it in its only step. PostgreSQL 16 (`pgvector/pgvector:pg16`, pinned digest) is a service of `routes` only. No cache and no artifact from another run is used.
**Triggers.** Each push to `feat/commit-outcome-proxy-measurement`, or to a branch named `feat/commit-outcome-proxy-measurement-<suffix>`, runs the whole workflow once on the pushed head. `workflow_dispatch` runs it on a named head; its `candidate_sha` must be 40 hex digits and the head of the dispatched ref, and an empty or malformed value is refused. Every job runs on both events; a branch deletion is not measured. Concurrency is per commit, so a newer push never replaces a pending run. Every run asserts `candidate == github.sha == HEAD` and a clean checkout. It records the event, the runner name and placement, and the variables `CI_RUNNER`/`CI_RUNNER_LIGHT`. The exits below are those of a head that carries the commit outcome change, its tests and these files. On any other head, the PASS line prints only if every expectation below holds there too.
**Commands.** Run from the top of the checkout with `-json` appended (implies `-v`). Each label keeps its command, raw `-json` stream, stderr and raw exit.
**Mutants.** `git apply` runs only after `git apply --numstat` proves the patch deletes exactly N lines of one file. Patches apply by context and offset: without `--3way` the `index` blob ids are ignored. The safety rests on the numstat, the context and the restore: `git checkout -- <file>`, then the blob is re-proved equal to `HEAD:<file>`.
**Marker.** After the numstat and `--check` pass and before `git apply`, the step writes `in-flight` = (step label, file, `pending`); the verdict turns it into `measured`. The next step that checks the tree (`pristine`/`restore`) reads it. For `pending`, the step ended after the marker and before its verdict: killed at its step ceiling, or exited on an error, a failed apply included. It then writes `<step label>  INABILITY the step ended while its mutant was pending …` to `verdicts.tsv` and restores the `HEAD:` blob. For `measured`, it restores the blob without a line. A stop before the marker left the tree untouched. On a workflow cancellation no later step that reads the marker runs, because each is gated on `!cancelled()`. So no INABILITY line is written and the PASS line cannot print; the `always()` upload keeps the marker as it was.
**Classes.** GREEN: raw exit 0, exactly the listed results, nothing failed or skipped (SKIP is not PASS). KILLED: raw exit 1; exactly the leaf and its parents fail; every other selected test passes; the leaf printed the listed text. INABILITY: no test result (build failure or empty selection), `panic: test timed out`, `COULD_NOT_LOOK` in the leaf (a cleanup that could not stop its worker included), or a mutant step that ended while its marker was `pending`. SURVIVED: the leaf passed. OTHER: anything else. Only GREEN and KILLED pass. An inability is never a kill.

## Job `oracle` — no database (proxy test file `cmd/olivares/communicationcommitoutcome_proxy_test.go`)
| # | `go test ./cmd/olivares -count=1 …` | Raw exit | Results (count) | Verdict and assertion |
|---|---|---|---|---|
| 1 | `-run '^(TestCommitOutcomeProxyReleaseDrainsInWireOrder\|TestCommitOutcomeProxyNonReadingPeerIsBounded)$'` | 0 | 8 pass: both tests; `ready_for_query_{after,before,during}_release`, `subsequent_query_is_usable`, `stalled_receiver_is_bounded`, `reading_receiver_is_the_positive` | GREEN 8 |
| 2 | M1 (`M1-release-lock.patch`: proxy_test.go:484-485, `s.writeMu.Lock()` + `defer s.writeMu.Unlock()` in `releaseAndDrain`); `-run '^TestCommitOutcomeProxyReleaseDrainsInWireOrder$/^ready_for_query_during_release$'` | 1 | 2 fail: the test, `…/ready_for_query_during_release` | KILLED only on `delivered "ZC", want CZ: a frame that arrived during the release overtook the buffered completion, so the release and its drain are not one operation` (http_test.go:1752). `COULD_NOT_LOOK: release schedule expired…` / `…ReadyForQuery … was not observed…` or a timeout = INABILITY |
| 3 | restore, then 1 | 0 | 8 pass | GREEN 8 |
| 4 | M2 (`M2-write-deadline.patch`: proxy_test.go:526-528, the `SetWriteDeadline(now+limit)` guard in `writeLocked`; write and deadline clear kept); `-run '^TestCommitOutcomeProxyNonReadingPeerIsBounded$/^stalled_receiver_is_bounded$'` | 1 | 2 fail: the test, `…/stalled_receiver_is_bounded`; leaf Elapsed ≥ 10 s | KILLED only on `the write did not return within 10s against a connected peer that stopped reading: the held write is not bounded` (http_test.go:1890). Cleanup is observed when the leaf finishes without `COULD_NOT_LOOK: the pipe worker did not stop after both endpoints closed` (:1856); if that line appears the run is INABILITY |
| 5 | restore, then 1 | 0 | 8 pass | GREEN 8 |
| 6 | on the candidate tree whatever 1–5 answered: `-run '^(TestCommitOutcomeProxyReleaseDrainsInWireOrder\|TestCommitOutcomeProxyArmIsSingleUse\|TestCommitOutcomeProxyNonReadingPeerIsBounded\|TestCommitOutcomeFenceWitnessRefusesASharedKey)$'` | 0 | 10 pass: the 8 of step 1 plus `TestCommitOutcomeProxyArmIsSingleUse` (one permit, one interception) and `TestCommitOutcomeFenceWitnessRefusesASharedKey` (a shared key is refused as a route's fence) | GREEN 10 |

## Job `routes` — the six key-lock routes (PostgreSQL), then both engines
Posture: `OLIVARES_TEST_POSTGRES_REQUIRED=1` (a missing server fails, never skips). The run records the server version and modes (`ssl`, `default_transaction_isolation`, `max_connections`) and both roles. `olivares_app` super/bypassrls/createrole/createdb = `f f f f`; `olivares_admin` = `f t f f` (BYPASSRLS only).
**Positive, before and after the mutants:** `go test ./cmd/olivares -count=1 -timeout 20m -run '^TestCommunicationCommitOutcomeRetryHTTPPostgres$'`. Raw exit 0, GREEN 25: the test, 6 routes, and 3 schedules each (`replay_after_commit`, `original_holds_route_fence_and_resend_recovers`, `resend_after_severed_original`). Each route's schedule (b) must also log `_FENCE_OWNED|route=<r>|` and `_RESEND_BLOCKED|route=<r>|`.
**Per route, M11-`<route>`:** `go test ./cmd/olivares -count=1 -timeout 8m -run '^TestCommunicationCommitOutcomeRetryHTTPPostgres$/^<route>$'`. Raw exit 1, 5 results. Fail: the test, `<route>`, `<route>/original_holds_route_fence_and_resend_recovers`. Pass: `<route>/replay_after_commit`, `<route>/resend_after_severed_original`. KILLED only when the leaf prints `does not hold the exact advisory tuple for "<fence prefix>` and `the route's own transaction lock is absent` (proxy_test.go:1153, schedule (b)'s ownership witness; it waits 10 s). A mutant original that never reaches the held COMMIT (`COULD_NOT_LOOK: the proxy never reported "commit_held"…`, :310) is INABILITY. The resend-wait half of (b) is not the kill: every route pins request and claim facts before its fence, so what blocks the resend is not identifiable under M11. Each M11 step runs its own `go test … -timeout 8m …` line and restores its file before it ends. Its entry first restores whatever a predecessor left `pending`, writing that step's INABILITY line.

| Route (subtest) | Mutant: exact deletion at the candidate | Fence name prefix |
|---|---|---|
| `send` | `modules/sessions/communication_service.go:475-477` (`directNoticeIdempotencyLockKey`; the message lock at :493 is kept) | `sessions_communication_command\|` |
| `ack` | `modules/sessions/communication_ack_service.go:510-514` (`directNoticeAckIdempotencyLockKey`) | `sessions:communication:ack:idem:` |
| `cursor` | `modules/sessions/communication_cursor_service.go:1179-1181` (identity lock at :1296 kept) | `sessions:communication:cursor:idem:` |
| `offer` | `modules/sessions/communication_handoff_offer_atomic.go:260-264` (`handoffIdempotencyLockKey`) | `sessions.communication.handoff.idempotency/` |
| `response` | `modules/sessions/communication_handoff_apply.go:155-157` (`applyHandoffResponse`) | `sessions.communication.handoff.idempotency/` |
| `create` | `modules/sessions/communication_channel_service.go:288-292` (slug lock) | `sessions_channel_create\|` |

Each patch is one hunk, deletions only (3/5/3/5/3/5 lines, M1 2, M2 3), reversible with `git apply -R`.
**Not measured by this workflow:** "fresh-key and fresh-slug requests remain positive" (a fresh key: 201/200; a fresh slug: 201). No test in the tree selects it: the retry and key-reuse tests hold no fresh-key or fresh-slug subtest, and this workflow adds none. The nearest measured fact is that `replay_after_commit` and `resend_after_severed_original` must pass under every M11.
**SQLite.** `LockTransaction` is a no-op on SQLite (`core/internal/store/sqlstore/scope.go:116-139`: one writer), and the route controls observe `pg_locks`. So M11 has no SQLite leg; SQLite is measured by the two families below.
**Both engines.** Store: `go test ./core/internal/store/sqlstore -count=1 -timeout 15m -run '^TestCommitOutcome'`, raw exit 0, GREEN n>0, 0 fail/skip. The passes must include `TestCommitOutcomeClassification`, `…DefiniteRollbackSQLite`, `…DefiniteRollbackPostgres`, `…ServerRejectionPostgres`, `…ExternalCancelPostgres`, `…PositiveExternalCancelPostgres` and `…StalledObservationIsBoundedPostgres`. HTTP: `go test ./cmd/olivares -count=1 -timeout 25m -run '^(TestCommunicationCommitOutcomeHTTPPostgres|TestCommunicationCommitOutcomeKeyReuseHTTPPostgres|TestCommunicationChannelHistoryWritesHTTP|TestCommunicationChannelHistoryWritesHTTPPostgres)$'`, raw exit 0, GREEN n>0, all four present (the third is the SQLite leg).

## Job `generated` — the repository's own tasks, then `git status --porcelain --untracked-files=all`
`pnpm --dir web install --frozen-lockfile`; `task web:codegen` (runs `openapi:dump`: both OpenAPI snapshots, then the typed web client); `task sdk:generate` (Go/Python/TS/Java operation layers, `gofmt`); `task build:web` (embedded console). Each raw exit is 0; an empty porcelain gives `NONE`, anything else is red (`DRIFT n`), and the artifact carries `regenerated.patch`.
**Prediction for a head that carries only the source delta: DRIFT (red).** `core/api/openapi_sessions_communication_contracts.go:180` changes the 503 description. The old text appears 19× in `web/openapi/openapi.beta.json` and 19× in `web/src/lib/api/openapi.gen.ts`, 0× in `web/openapi/openapi.json` and in the SDK operation layers. `web/src/features/communications/errors.ts` changed, so `core/internal/webui/dist` is predicted to change. The job turns green once the regenerated files are committed on the branch.

## Job `verdict` and the SDK gate
It prints one line, `commit-outcome oracle PASS at <sha> (<event>): …; repository variables CI_RUNNER=… CI_RUNNER_LIGHT=…`, only when `oracle`, `routes` and `generated` succeeded and every output equals its expectation: steps 1/3/5 `GREEN 8`, step 6 `GREEN 10`, M1 and M2 `KILLED`, routes `GREEN 25` before and after, six M11 `KILLED`, both engine families `GREEN n>0`, and generated `NONE`. An empty output is a measurement that never happened, and it is red (`::error::no verdict`, exit 1).
**SDK suites, outside this workflow:** `gh workflow run mainline-ci.yml --repo olivaresai/olivares --ref <branch>`.
- That run must show: `head_sha` equal to the candidate; `classify` giving `code=true` (event ≠ push); and `sdk-tests` succeeding with its python, typescript and java steps run.
- Placement is decided by the jobs API of that run: `gh api repos/olivaresai/olivares/actions/runs/<run>/jobs` must show, for `classify` and `sdk-tests`, a `runner_name`, `labels` = `ubuntu-latest` and a GitHub-hosted `runner_group_name`.
- Supporting evidence only: `identity.txt` and this workflow's verdict line show `CI_RUNNER`/`CI_RUNNER_LIGHT` as a job without an `environment:` sees them (repository and organization variables). A repository-level 404 from `…/actions/variables/CI_RUNNER` alone proves nothing, because an organization variable of the same name would still apply.
- The sdk-tests selectors are Java `ClientTest#neverRetriesCommitOutcomeUnknown`/`#stillRetriesOther503OnGet`, Python `test_commit_outcome_unknown_is_never_retried`/`test_other_503_get_still_retried`, and TS "never retries commit_outcome_unknown"/"still retries another 503 GET".
- None of these is ever a pass: a skipped, cancelled or undispatchable `sdk-tests` run means the SDK suites were not measured, and a self-hosted runner means they were not measured on a GitHub-hosted one.
- The Go client pair and the web vitest files run in the pull request's own `pr-ci` (`pr-test-shard`, `pr-web`), not here.

## Appendix 2026-09-24 — corrected expectations (additive; everything above is unchanged)

- **SDK spec hash (corrects line 41).** "0× … in the SDK operation layers" holds for the 503 description text only. Regenerating also moves the specification hash bound in six SDK files, `b18a87a7…` → `74069393…`, one line each: `clients/go/operations.gen.go`, `clients/go/version.gen.go`, `clients/java/src/main/java/ai/olivares/client/ApiMetadata.java`, `clients/java/src/main/java/ai/olivares/client/Client.java`, `clients/python/src/olivares_client/_operations.py`, `clients/typescript/src/version.gen.ts`. A head with the source change alone therefore drifts on those six files, `web/openapi/openapi.beta.json`, `web/src/lib/api/openapi.gen.ts` and `core/internal/webui/dist`. The commit `chore(generated): regenerate the OpenAPI snapshots and SDK spec hash` carries the eight non-dist files; until `core/internal/webui/dist` is rebuilt from the final tree, `generated` stays `DRIFT` on it.
- **Relay regression, a separate selection (in no count above).** Step 7 of `oracle` runs `go test ./cmd/olivares -count=1 -timeout 20m -run '^TestCommunicationCommitOutcomeProxyRelay$' -json`, and the verdict requires `GREEN 11`: raw exit 0 and exactly the test and its 10 subtests passing; a skip, an empty selection, an extra or missing result, or any failure is red. Steps 1–6 keep `GREEN 8`/`GREEN 10` and the routes `GREEN 25`. Predicted contrast, not measured by this workflow: with the commit `test(cmd): cover oversized, malformed and torn frames in the test proxy` and without `test(cmd): stream oversized protocol frames through the test proxy`, the same selection gives raw exit 1 and 11 results, 10 failing (the test and 9 subtests) and 1 passing (`held_total_stays_within_the_bound`). Five passing leaves, `frame_length_below_4_from_server_is_named`, `frame_length_below_4_from_client_is_named`, `truncated_frame_is_named`, `truncated_oversized_frame_is_named` and `short_write_is_named`, assert a named relay inability and log it as `COULD_NOT_LOOK: relay_error=…`; that line is expected in those five leaves only. Step 7, like every positive step above, judges results and does not read log lines, so it does not detect a `relay_error` line in any other passing test.
- **One citation moved (line 23).** `proxy_test.go:1153` is now `proxy_test.go:1388`, with the same assertion text; the kill check matches the text, not the line, so no verdict changes. `proxy_test.go:310`, `:484-485`, `:526-528` and every `http_test.go` citation are unchanged, and both mutant patches still apply as exactly 2 and 3 deleted lines.

## Appendix 2026-09-24 — the relay contrast, step 7a (additive; everything above is unchanged)

- **What is swapped.** `ci/oracles/commit-outcome/proxy-relay-before-fix.patch` (sha256 `f56fbef1f6863b7991d5945e74233a2e960d3b599a42f156b11f495bc5bded5b`, pinned on its own in `ci/oracles/commit-outcome/contrast.sha256`) is the reverse of the relay repair in `cmd/olivares/communicationcommitoutcome_proxy_test.go` and touches nothing else (numstat `12 247`, one file). Step 7a proves with `git hash-object` the final proxy blob `21b52309…` and the regression blob `e8ba9a52…`, checks the digest, applies the patch, and proves the pre-repair proxy blob `cd30aaf0…` with the regression blob unchanged. `mutants.sha256` still lists the eight `M*.patch` mutants only.
- **What it runs.** The selection of step 7 with the same Go timeout: `go test ./cmd/olivares -count=1 -timeout 20m -run '^TestCommunicationCommitOutcomeProxyRelay$' -json`, kept as `contrast_regression.{command,json,stderr,exit,results}`.
- **Expected at the pre-repair blob (not measured before the hosted run).** Raw exit 1 and exactly 11 results: `TestCommunicationCommitOutcomeProxyRelay` fails; `held_total_stays_within_the_bound` passes; these nine fail, each at its intended assertion:
  - `oversized_server_frame_passes_through`: "the 120005-byte server frame did not pass through the test proxy";
  - `oversized_client_frame_passes_through`: "the 120005-byte client frame did not pass through the test proxy";
  - `oversized_frame_while_holding_is_refused`: "…was not refused by name";
  - `frame_length_below_4_from_server_is_named`, `frame_length_below_4_from_client_is_named`, `truncated_frame_is_named`, `truncated_oversized_frame_is_named` and `closed_peer_is_named`: "the test proxy did not name how the relay ended: no "<the reason each names>" in its custody log";
  - `short_write_is_named`: "the client received less than 32768 bytes of the 120005-byte frame".

  That result is `CONTRAST relay-before-fix 10 FAIL 1 PASS`. An expected-failing subtest that passes is `NO CONTRAST`. No result (build failure or empty selection), the Go timeout, a skip or any `COULD_NOT_LOOK` is INABILITY and never counts as red.
- **Restore.** Whatever was measured, 7a checks out the final proxy bytes and proves `21b52309…` and `e8ba9a52…` before step 7; the in-flight marker is `pending` until then. A step stopped earlier leaves the marker, and step 7's entry restores the file, proves it against the committed blob and writes 7a's INABILITY line. A restore 7a cannot prove makes its verdict INABILITY and writes an INABILITY line for step 7.
- **Verdict and budget.** The verdict requires both `CONTRAST relay-before-fix 10 FAIL 1 PASS` and `GREEN 11`; the counts and mutant requirements above are unchanged. 7a's ceiling is 25 min; the oracle job's steps sum to 174 min under a 185 min ceiling.

## Appendix 2026-09-24 — the relay contrast re-pinned to a control (additive; supersedes the 7a patch digest and blob ids above)

- **Why.** The relay regression test now declares the inabilities it causes, and a relay custody check fails the owning test on any undeclared, extra or unconsumed relay inability. The file before the relay repair has no such check, so the contrast cannot use it: the regression test would not build against it.
- **Control.** It is the final proxy test file with only `clientToServer`, `serverToClient` and one doc comment of `commitOutcomeReadTypedFrame` put back as they were before the relay repair. Those regions match that source line for line, including the blanket frame bound `if length < 4 || length > commitOutcomeProxyBufferLimit {`, the silent `return` on a read error and the unchanged deferred closes. The custody check and every declaration stay as they are; nothing is stubbed.
- **Patch.** `ci/oracles/commit-outcome/proxy-relay-before-fix.patch` is now the diff from the final file to the control: sha256 `8c0eaedc4b41687be3e60e525b5dc7744fffe251714fdfdd156928e9b8714cc4`, one file, numstat `12 48`.
  - Blob ids: final `98c0d3a85e979de0bf82a8c2f38511f6ff8bf228` → control `4feb9f77306bbf29a624c3d2c9b803bb9771bfb4`, which is not the blob of the file before the repair. The relay regression test stays at `7440116a946ccc92290f3686fe11cbac5eb7e270`.
  - `contrast.sha256` pins both the patch digest and the control blob id, and step 7a proves final → control → final.
- **Expected with the control (not measured before the hosted run).** Unchanged `CONTRAST relay-before-fix 10 FAIL 1 PASS`: raw exit 1, the same nine subtests failing at the same assertions, and `held_total_stays_within_the_bound` passing.
  - The five subtests that declare an inability also print the custody check's `COULD_NOT_LOOK: relay_error=<declared> was declared and never recorded`: `frame_length_below_4_from_server_is_named` and `frame_length_below_4_from_client_is_named`, `truncated_frame_is_named` and `truncated_oversized_frame_is_named` (`from_server:short_read`), and `short_write_is_named` (`from_server:short_write`).
  - Step 7a requires each of those lines in its own subtest. Any other `COULD_NOT_LOOK` stays INABILITY.
- **Moved citations (bytes unchanged).** The M1 and M2 preimages are now at `proxy_test.go:488-489` and `:530-532` (were `:484-485` and `:526-528`). Both patches apply at offset 4 with numstat `0 2`/`0 3`; `apply_mutant` accepts that offset and `restore` returns the final blob. `mutants.sha256` and the eight mutant patches are unchanged. The step sum stays 174 min under a 185 min ceiling.
- **2026-09-24 — refreshed citations, pins as literals, superseded sentences (additive).**
  - **Citations.** At the final proxy test file, `grep -n "does not hold the exact advisory tuple" cmd/olivares/communicationcommitoutcome_proxy_test.go` gives `:1548` (was `:1388`, and `:1153` before that). `grep -n "the proxy never reported %q" cmd/olivares/communicationcommitoutcome_proxy_test.go` gives `:312` (was `:310`). The texts are unchanged, and the kill check matches the text, not the line, so no verdict changes.
  - This supersedes the "One citation moved (line 23)" bullet and the `:310` citation. The M1/M2 preimages `:488-489` and `:530-532` in the "Moved citations (bytes unchanged)" bullet stay true.
  - **Relay regression bullet.** "Predicted contrast, not measured by this workflow" is superseded by step 7a, which measures that contrast. "Step 7, like every positive step above, … does not detect a `relay_error` line in any other passing test" is superseded by the relay custody check (`proxy_test.go:1188-1215`): an undeclared relay inability fails the owning test, so a positive step cannot stay green with one.
  - **Pins.** The patch sha256 and the control blob id are pinned only as step 7a's literals `CONTRAST_PATCH_SHA256` and `CONTROL_PROXY_BLOB`. 7a checks the sha256 before applying and proves the control blob after applying. There is no `contrast.sha256`, so the sentences naming it ("pinned on its own in `ci/oracles/commit-outcome/contrast.sha256`" and "`contrast.sha256` pins both the patch digest and the control blob id") are superseded.
  - **Restore.** A restore 7a cannot prove makes 7a's own verdict INABILITY and no longer writes a line for step 7, so `verdicts.tsv` carries one line per label.
- **2026-09-24 — route correction r3 (additive; nothing above is relaxed).** The route fixtures now send the bodies the communication validators accept (text blocks with `"format": "plain"`; the reject response with a protected `reason`), and the test proxy withholds a valid client Terminate on the session whose COMMIT is held (custody `terminate_withheld=<pid>`, then `terminate_forwarded=<pid>` when `forward()` delivers it right after the COMMIT, or `terminate_closed=<pid>` when `sever()` or the proxy's close drops the connection). The relay regression gains six subtests, `terminate_is_withheld_while_commit_held`, `sever_settles_a_withheld_terminate`, `terminate_without_a_held_commit_is_relayed`, `terminate_on_another_session_is_relayed`, `malformed_terminate_is_relayed` and `terminate_during_held_answer_is_relayed`: step 7 expects `GREEN 17`; step 7a, with its control re-derived at the new head and keeping the Terminate rule as shared support, expects `CONTRAST relay-before-fix 10 FAIL 7 PASS` (the same nine failing subtests; `held_total_stays_within_the_bound` and the six new subtests pass); a Terminate contrast with only that rule removed would give `3 FAIL 14 PASS` (the test, `terminate_is_withheld_while_commit_held` and `sever_settles_a_withheld_terminate`, each at "the client's Terminate was not withheld while its COMMIT was held"). The M1/M2 preimages move to `proxy_test.go:491-492` and `:533-535` (offset 7, bytes unchanged); the fence assertion (`proxy_test.go:1642`) keeps both kill texts and appends `; holder <state>`, and an unreadable holder state is COULD_NOT_LOOK. None of this is measured yet.
- **2026-09-24 — true relay counts and the relay contrast re-pinned for the Terminate rule (additive).**
  - **Counts.** `grep -n 't.Run(' cmd/olivares/communicationcommitoutcome_proxyrelay_test.go` lists 16 subtests of `TestCommunicationCommitOutcomeProxyRelay`: the ten above and the six Terminate subtests. Step 7 therefore requires `GREEN 17`: raw exit 0 and exactly the test and its 16 subtests passing. No test entered or left the selections of steps 1–6 or of the routes, so `GREEN 8`, `GREEN 10` and `GREEN 25` stay true.
  - **Control.** It is the final proxy test file with only `clientToServer`, `serverToClient` and the doc comment of `commitOutcomeReadTypedFrame` put back as they were before the relay repair. The old client loop keeps the Terminate rule, `if kind == 'X' && len(payload) == 0 && s.p.withholdTerminate(s) { continue }`, which is the same test as the final `kind == 'X' && body == 0`, so the six Terminate subtests pass on both sides.
  - **Patch.** `proxy-relay-before-fix.patch` has sha256 `1c324a466f7642ede4bef06d8dfbfc29887ab48daefab94bd73973e6788013b0`: one file, 5 hunks, numstat `14 50`. Blob ids: final `2a47d1b876b4fe5a7a63e2db920a3a1c923e176e` → control `706f187b47dcfe27f91ddd85f8d02c384ddbf2b6` → final. The relay regression test stays at `d19eec2e4da339b49d3708eb6fb6b03470ff961e` on every side.
  - **Expected with the control (not measured before the hosted run).** `CONTRAST relay-before-fix 10 FAIL 7 PASS`, with raw exit 1. The test and the same nine subtests fail at the same assertions, with the same five declared-never-recorded lines. `held_total_stays_within_the_bound` and the six Terminate subtests pass. A contrast removing only the Terminate rule (`3 FAIL 14 PASS`) is not part of this workflow.
  - **Citations.** Each is a `grep -n` at this head:
    - `grep -n "does not hold the exact advisory tuple" cmd/olivares/communicationcommitoutcome_proxy_test.go` gives `:1642` (was `:1548`);
    - `grep -n "the proxy never reported %q" cmd/olivares/communicationcommitoutcome_proxy_test.go` gives `:313` (was `:312`);
    - the relay custody check is at `proxy_test.go:1195-1223` (was `:1188-1215`).
  - **Kill texts.** The kill check matches text, and both M11 texts are unchanged: the fence message only appends `; holder <state>`.
  - **M1/M2.** Their preimages are at `:491-492` and `:533-535`, and `git apply --check` succeeds for both at offset 7. `mutants.sha256` and the eight mutant patches are unchanged.
  - **Budget unchanged.** The oracle steps still sum to 174 min under 185. A red Terminate subtest stops at its first bounded wait (5 s for a custody record or a read, 10 s for a hold event), inside the unchanged 20-minute Go clock.
  - **Superseded.**
    - In "Relay regression, a separate selection": `GREEN 11` and "the test and its 10 subtests".
    - In the step 7a appendices: `CONTRAST relay-before-fix 10 FAIL 1 PASS`, numstat `12 48`, and every earlier patch digest and blob id.
    - In "refreshed citations, pins as literals, superseded sentences": the `:1548`, `:312` and `:1188-1215` citations.
    - In "Moved citations (bytes unchanged)": the `:488-489`/`:530-532` preimages and offset 4.
- **2026-09-24 — relay regression file re-pinned (additive; no count changes).** The two withheld-Terminate subtests now wait, inside the existing 5 s custody bound, for the proxy's own `client_gone_upstream_retained` record before `forward()` or `sever()` runs. That is the proxy's decision to retain the server socket. The same record is written by `closeUpstreamUnlessHeld` in the final proxy file and in the control, so both cases pass on both sides. Step 7a's `RELAY_TEST_BLOB` is now `83687980ff45c99d3215c9d8d8bee69aa627b1cf` (was `d19eec2e4da339b49d3708eb6fb6b03470ff961e`). Everything else is unchanged: the final proxy blob `2a47d1b876b4fe5a7a63e2db920a3a1c923e176e`, the patch (sha256 `1c324a466f7642ede4bef06d8dfbfc29887ab48daefab94bd73973e6788013b0`, numstat `14 50`), the control `706f187b47dcfe27f91ddd85f8d02c384ddbf2b6`, 16 subtests, `GREEN 17`, `CONTRAST relay-before-fix 10 FAIL 7 PASS`, M1/M2 at offset 7, and the 174 min step sum under 185.
- **2026-09-24 — the Terminate contrast, step 7b (additive).** Step 7a keeps the Terminate rule on both sides, so it says nothing about that rule; step 7b measures it.
  - **Control.** `ci/oracles/commit-outcome/proxy-terminate-before-fix.patch` turns the final proxy test file into the same file without the three lines of `clientToServer` that withhold a client Terminate while that session's COMMIT is held. It has sha256 `5d290f7df561947395773cbaa91508c49bed61c42efe6bafc2c5cbf18b125d28`, touches one file with one hunk (numstat `0 3`), and is pinned only as the step's literal.
  - **Blob ids.** final `2a47d1b876b4fe5a7a63e2db920a3a1c923e176e` → control `cd998317469d362802a336f45d76534fa5aa35e3` → final. The relay regression test stays at `83687980ff45c99d3215c9d8d8bee69aa627b1cf` on every side.
  - **Selection.** The same `-run` selection as steps 7 and 7a, with its own `-timeout 8m` (steps 7 and 7a keep `-timeout 20m`): `go test ./cmd/olivares -count=1 -timeout 8m -run '^TestCommunicationCommitOutcomeProxyRelay$' -json`, run after step 7 with a 10-minute ceiling.
  - **Expected with the control (not measured before the hosted run).** `CONTRAST terminate-before-fix 3 FAIL 14 PASS`, raw exit 1:
    - the test fails, and so do `terminate_is_withheld_while_commit_held` and `sever_settles_a_withheld_terminate`, each at "the client's Terminate was not withheld while its COMMIT was held: the server received ("X\x00\x00\x00\x04", <nil>)";
    - the other 14 subtests pass;
    - the only admitted COULD_NOT_LOOK lines are the five relay inabilities the negative subtests cause by design, each in its own subtest.
  - **INABILITY.** A build failure or empty selection, the Go timeout, a skip, any other COULD_NOT_LOOK, or a pin or blob proof that does not hold. A result set other than the expected one is OTHER, and a Terminate subtest that passes is NO CONTRAST.
  - **Restore and verdict.** The final bytes are restored and proven before the step ends; an unproven restore makes the step's own verdict INABILITY. No later step measures, so a stop before the verdict leaves the step without one, and the verdict job refuses it. The verdict requires `CONTRAST terminate-before-fix 3 FAIL 14 PASS` beside every existing expectation.
  - **Budget.** The oracle steps sum to 184 min under the unchanged 185 min ceiling.
- **2026-09-24 — source citations re-checked after the offer recognition correction (additive; no count, clock, selection or pin changes).** Each location below is where the cited text, or the mutant's whole preimage, sits at this head; the older values are historical and no longer name those lines.
  - **Moved.** The M1 kill text is at `http_test.go:2085` (was `:1752`), the M2 kill text at `:2223` (was `:1890`) and the M2 cleanup text at `:2189` (was `:1856`); the kill checks match text, not lines, so no verdict changes. The M11-offer deletion (`handoffIdempotencyLockKey`) is at `modules/sessions/communication_handoff_offer_atomic.go:324-328` (was `:260-264`): `M11-offer.patch` is byte-unchanged, applies at offset 64 and keeps numstat `0 5`.
  - **Still true.** The other five M11 deletions and the kept locks at `communication_service.go:493` and `communication_cursor_service.go:1296`; `scope.go:116-139`; the `:180` 503 description; and, from the later bullets, `proxy_test.go:1642`, `:313`, `:1195-1223` and the M1/M2 preimages at `:491-492`/`:533-535` (offset 7).
  - **Unchanged.** The proxy test `2a47d1b876b4fe5a7a63e2db920a3a1c923e176e`, the relay regression test `83687980ff45c99d3215c9d8d8bee69aa627b1cf`, both contrast patches and their controls (`706f187b…`, `cd998317…`), `GREEN 17`, `CONTRAST relay-before-fix 10 FAIL 7 PASS`, `CONTRAST terminate-before-fix 3 FAIL 14 PASS`, all ten patches, and the 184-minute step sum under 185.
  - **Not selected here.** This workflow selects none of the correction's new tests: `TestCommitOutcomeLineageWitnessRefusesForeignWaits` (`cmd/olivares`) and `TestHandoffOfferSessionRecognitionRealClaimContention`, `TestHandoffOfferRecognitionAtRealStoreSeams` and `TestHandoffOfferRecognitionRefusesChangedCurrentAuthority` (`modules/sessions`) run in the repository's ordinary test shards, a separate measurement. Nothing here is measured yet: a route green at this head stays a prediction until the hosted run.
