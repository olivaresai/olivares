---
title: "Fine-tuning собственных моделей и локальный инференс — исполнение (планируется)"
description: >-
  Что остаётся запланированным на стороне собственных моделей: чтобы платформа сама
  выполняла задания fine-tuning и сама обслуживала локальный инференс. Реестр собственных
  моделей, допуск подписанных моделей, записи происхождения и доказательства AIBOM уже
  поставляются как операции с моделями; эта страница честно говорит об исполняющей
  половине, которая не поставляется.
---

История собственных моделей — управление **моделями, которые компания обучает или
хостит сама** — делится на две половины, и запланированной остаётся только одна.

**Управляющая половина поставляется уже сегодня** как
[Модуль XXIII — операции с моделями](/ru/reference/modules/xxiii-model-operations/):
версионированный **реестр собственных моделей** (`hosted`, `fine_tuned`, `imported`),
шлюз **допуска** подписанных моделей, **записи происхождения датасетов и заданий
fine-tuning**, управляемые **записи развёртываний локального инференса** (vLLM, Ollama,
llama.cpp, другие) с повторной проверкой enforce-signed в момент развёртывания, и
генерация **AIBOM / карточки модели** с запечатыванием, заякоренным в леджере. Его
сущности и эндпоинты объявлены и обслуживаются по бета-маршрутам модулей
(`/v1/m/models/owned-models`, `/v1/m/models/model-versions`,
`/v1/m/models/finetune-jobs`, `/v1/m/models/inference-deployments`,
`/v1/m/models/aiboms`, …) — см. [справочник маршрутов модулей](/reference/api-beta/).

Эта страница отслеживает **исполняющую половину, которая запланирована и сознательно не
построена**: чтобы платформа сама *выполняла* эту работу.

## Что поставляется сегодня (на другой странице)

Управление собственными моделями реально и задокументировано на странице
[операций с моделями](/ru/reference/modules/xxiii-model-operations/):

- **реестр собственных моделей** с неизменяемыми версиями, чтобы дообученная или
  самостоятельно хостящаяся модель была первоклассной управляемой сущностью, а не
  неуправляемым эндпоинтом;
- **задания fine-tuning как записи происхождения** — инвентаризация обучающей работы,
  выполненной вовне, и версии модели, которую произвело каждое задание;
- **развёртывания локального инференса как управляемые записи** — среды обслуживания,
  которыми управляете вы, под контролем допуска (`require_signed`) и аудита.

## Что остаётся запланированным

- **Выполнение заданий fine-tuning.** Поставляемый модуль записывает статус и
  происхождение работы fine-tuning, выполненной в другом месте; платформа никогда не
  запускает, не отменяет и не исполняет обучающее задание и не хранит ни весов, ни
  содержимого датасетов. Конвейер, *исполняющий* fine-tuning с платформы, — это
  запланированная работа.
- **Обслуживание локального инференса.** Развёртывания — это управляемые записи сред,
  которые запускает оператор; платформа сама не хостит и не обслуживает инференс.
  Обслуживание локального инференса силами самой платформы — запланированная работа.

Для этой исполняющей половины не объявлено ни схемы задания, ни контракта планировщика,
ни контракта среды обслуживания, и эта страница сознательно ничего не выдумывает.

## Почему запланировано, а не поставлено

Платформа построена так, чтобы любая возможность подключалась без переархитектуры
остального, поэтому исполнение можно добавить позже поверх уже поставленных управляющих
поверхностей. Оно было помещено **после** v1 явным продуктовым решением: приоритет
первого релиза — управлять моделями и агентами, которые организация уже запускает, а
исполнение обучения/обслуживания не меняет эту базовую ценность настолько, чтобы
конкурировать за усилия v1.

Когда оно будет построено, его естественный шов уже поставлен: выполненный fine-tuning
произвёл бы **версию** модели в реестре
[операций с моделями](/ru/reference/modules/xxiii-model-operations/) и прошёл бы тот же шлюз
**допуска** подписанных моделей, что и любой произведённый вовне артефакт, а политика
вендорского стека остаётся в
[управлении моделями и провайдерами](/ru/reference/modules/x-models/).

:::caution[Честные ограничения]
- **Управляющие поверхности поставлены, исполняющие — нет.** Не читайте эту страницу как
  отрицание реестра, допуска, записей происхождения, управления развёртываниями или
  доказательств AIBOM — они существуют и задокументированы в
  [операциях с моделями](/ru/reference/modules/xxiii-model-operations/).
- **Сегодня не существует ни одной поверхности исполнения.** В поставляемом бинарнике
  нет ни обучающего конвейера, ни планировщика заданий fine-tuning, ни обслуживания
  инференса силами платформы, и для них не объявлено ни сущности, ни эндпоинта, ни
  события — даже отказывающего интерфейса.
- **Ничто здесь не является обещанием даты или глубины.** Приведённый объём — это
  запланированное направление; схема заданий и контракты сред будут спроектированы при
  постройке. Они сознательно оставлены неспецифицированными, а не сфабрикованы.
:::

## Связанные страницы

- [Модуль XXIII — операции с моделями](/ru/reference/modules/xxiii-model-operations/) — поставляемая поверхность управления собственными моделями: реестр, допуск, происхождение, развёртывания, AIBOM.
- [Каталог модулей](/ru/reference/modules/overview/) — 30 поставляемых модулей и место работы с собственными моделями.
- [Модуль X — управление моделями и провайдерами](/ru/reference/modules/x-models/) — поставляемый сосед, управляющий вендорским стеком моделей.
- [Честность и ограничения](/ru/start/honesty-and-limits/) — контракт «наблюдать широко / действовать на подмножестве» и что значит «планируется».
