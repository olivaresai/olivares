// Connector scaffold (Apache-2.0): generates a complete, compiling,
// boundary-clean out-of-tree connector repository — what "build your first
// connector" starts from (S142). Stdlib-only by design, like the zero-dep SDK
// whose contract the generated code targets.
module github.com/olivaresai/olivares/sdk/scaffold

go 1.26.5

