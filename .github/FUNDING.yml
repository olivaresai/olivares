# SPDX-FileCopyrightText: 2026 Olivares.AI
# SPDX-License-Identifier: AGPL-3.0-only

# Sponsorship / funding — displayed in the "Sponsor" button on GitHub.
# Commercial licensing and enterprise enquiries: enterprise@olivares.ai
#
# THE ORGANISATION HANDLE IS BACK AT THE HEAD OF THE ARRAY — RESTORED 2026-09-01, on the
# exact condition this file wrote on 2026-08-27 when it removed it. That day the organisation was
# not enrolled: https://github.com/sponsors/olivaresai REDIRECTED to https://github.com/olivaresai
# (what GitHub does for an account that cannot receive sponsorship), so listing it was a promise
# the button could not keep. Measured again on 2026-09-01 ~21:30Z, with curl and WITHOUT following
# redirects: /sponsors/olivaresai answers 200 and no redirect, the page is titled "Sponsor
# @olivaresai on GitHub Sponsors" and offers three monthly tiers (the amounts live on that page, not here);
# /sponsors/fran-olivares still answers 200. Enrolling was the maintainer act (bank/tax details)
# the 08-27 note asked for; this line is the code half of it, and ratifies or reverts it
# (the planner relayed the decision the same day). If /sponsors/olivaresai ever answers a 3xx again,
# take the handle out again — the rule has not changed, only the measurement.
#
# 2026-09-02 (words, relayed by the planner): the organisation and the founder's profile are
# public, active and THE MAIN channel; ko-fi (franolivares, checkout id Z1R625SAD2) is secondary.
# The array below is that order. The 09-01 lot described this array in the comment but left the
# line itself at [fran-olivares] — fixed here, with the words that decide it.
#
# WHY THESE DESTINATIONS ARE NOT IN scripts/check-emitted-urls.sh, decided here rather than left
# unstated: that gate's verdict is an HTTP status, and a sponsors handle with NO profile answers
# 200 after the redirect above. A record row would therefore certify a dead channel as healthy —
# the same shape as the github.com `blob` 200 that gate already has to work around. A row that
# cannot fail for the reason it exists is worse than no row. Re-check these by hand, without
# following redirects: an enrolled handle answers 200 at /sponsors/<handle>; an unenrolled one
# answers a 3xx to /<handle>.
github: [olivaresai, fran-olivares]
ko_fi: franolivares
custom: ["https://olivares.ai/pricing"]
