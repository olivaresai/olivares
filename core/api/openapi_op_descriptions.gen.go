// SPDX-FileCopyrightText: 2026 Olivares.AI
// SPDX-License-Identifier: AGPL-3.0-only
// Additional terms under AGPL-3.0-only section 7(a) disclaim warranty and limit liability: see DISCLAIMER.md at the repository root.

// Code generated by scripts/openapi-op-descriptions. DO NOT EDIT.
//
// One description per operation published by the two OpenAPI documents, keyed by
// "<METHOD> <spec path>". handlerDocOperationDescriptions separately records which
// descriptions were derived directly from the registered handler's doc comment; every
// other description is a row of scripts/openapi-op-catalog.tsv. Regenerate with:
//
//	bash scripts/check-openapi-op-descriptions.sh --write
//	task openapi:dump && pnpm --dir web run codegen

package api

// operationDescriptions is consulted by applyOperationDescriptions (see
// openapi_descriptions.go), which stamps the description onto every operation of both
// published documents.
var operationDescriptions = map[string]string{
	"DELETE /v1/agents/{id}":                                         "Deletes one agent of the resolved tenant by id.",
	"DELETE /v1/console/connectors":                                  "Removes one configured connector instance.",
	"DELETE /v1/console/license":                                     "Removes the installed license and reverts the deployment to the community build.",
	"DELETE /v1/console/secrets":                                     "Deletes one sealed secret by the name carried in the body.",
	"DELETE /v1/console/sources":                                     "Removes one configured source connector.",
	"DELETE /v1/console/sso":                                         "Removes the managed SSO or IdP configuration of the addressed scope.",
	"DELETE /v1/m/capabilities/configs/{id}":                         "Removes a managed config, recording a final revision snapshot (the immutable history survives the deletion) and a self-audit.",
	"DELETE /v1/m/catalog/entries/{id}":                              "Deletes a DRAFT entry. Approved/deprecated entries are kept (deprecate to retire them) so the registry's approved history is not erasable through this path.",
	"DELETE /v1/m/compliance/aims/pack/{id}":                         "Removes a maintained pack; admin-tier and self-audited.",
	"DELETE /v1/m/compliance/depth/fedramp/{id}":                     "Deletes one stored FedRAMP key security indicator by id.",
	"DELETE /v1/m/compliance/depth/sector/{id}":                      "Deletes one stored sector compliance pack by id.",
	"DELETE /v1/m/compliance/depth/us-law/{id}":                      "Deletes one stored US state law pack by id.",
	"DELETE /v1/m/compliance/dora/incidents/{id}":                    "Removes a stored classification; admin-tier and self-audited.",
	"DELETE /v1/m/compliance/dora/register/{id}":                     "Removes a maintained register; admin-tier and self-audited (a register is a governance artifact).",
	"DELETE /v1/m/compliance/nis2/incidents/{id}":                    "Deletes one classified NIS2 incident by id.",
	"DELETE /v1/m/compliance/oscal/profiles/{id}":                    "Unregisters a profile — the export reverts to include-all for that framework.",
	"DELETE /v1/m/compliance/retention/policies/{class}":             "Removes a schedule. Stopping a purge is the SAFE direction, so there is no gate — only the self-audit.",
	"DELETE /v1/m/consoleviews/views/{id}":                           "Removes a view: the owner always may; a tenant admin/owner (or superadmin) may delete ANY view — the cleanup power for views left behind by departed users.",
	"DELETE /v1/m/deploy/definitions/{id}":                           "Deletes one deployment definition by id, and refuses while a live deployment still uses it.",
	"DELETE /v1/m/eventing/subscriptions/{id}":                       "Removes a subscription. Its queued deliveries are dead-lettered lazily by the dispatcher (subscription_deleted) — the rows stay as evidence until retention prunes them.",
	"DELETE /v1/m/finops/budgets/{id}":                               "Deletes one budget by id; a policy of any other kind is refused.",
	"DELETE /v1/m/finops/cost-centers/{id}":                          "Deletes one cost center by id together with the mappings attached to it.",
	"DELETE /v1/m/finops/cost-centers/{id}/mappings/{mid}":           "Deletes one cost-center mapping by its own id.",
	"DELETE /v1/m/finops/model-rates/{id}":                           "Removes a rate catalog entry by ID.",
	"DELETE /v1/m/governance/agents/{agentID}/identity":              "Clears an agent's identity binding. Admin-tier, self-audited.",
	"DELETE /v1/m/governance/guardian/rules/{id}":                    "Deletes a rule only after terminalizing every queued action that could otherwise execute from its denormalized action row.",
	"DELETE /v1/m/governance/policies/{id}":                          "Deletes a governance policy. Admin-tier, self-audited; invalidates the ABAC cache after commit.",
	"DELETE /v1/m/governance/rbac/grants/{id}":                       "Revokes one scoped RBAC grant by id.",
	"DELETE /v1/m/governance/rbac/permission-groups/{name}":          "Deletes one permission group by name; the caller must be a tenant admin.",
	"DELETE /v1/m/governance/rbac/roles/{name}":                      "Deletes one custom role by name; the caller must be a tenant admin.",
	"DELETE /v1/m/governance/routine-policies/{id}":                  "Deletes a routine policy.",
	"DELETE /v1/m/health/checks/{id}":                                "Removes a check (hard delete). Admin-tier, self-audited.",
	"DELETE /v1/m/inferenceproxy/dlp/rules/{id}":                     "Removes one DLP rule by id (admin-tier; self-audited).",
	"DELETE /v1/m/knowledge/data-products/{id}":                      "Deletes one data product by id.",
	"DELETE /v1/m/knowledge/dlp/rules/{id}":                          "Removes one DLP rule by id (admin-tier; self-audited). Removing the last rule disables the gate entirely — the audit row is the record of WHO opened the perimeter.",
	"DELETE /v1/m/knowledge/kbs/{id}":                                "Deletes a KB and CASCADES its documents, chunks and document sensitivity labels in one transaction (no orphans).",
	"DELETE /v1/m/knowledge/memory/{id}":                             "Removes one memory entry. The declared read context gates a scoped entry exactly like a read (a session can only delete what it can see; mismatch is a 404, no existence leak), AND an entry classified ABOVE the caller's clearance is equally invisible (E5 — \"you can only act on what you can see\" holds on the classification axis too, so a low-clearance writer cannot destroy an entry it may not read).",
	"DELETE /v1/m/models/agent-artifacts/{id}":                       "Deletes one registered agent artifact by id.",
	"DELETE /v1/m/models/datasets/{id}":                              "Deletes one registered dataset by id.",
	"DELETE /v1/m/models/inference-deployments/{id}":                 "Deletes one inference deployment by id.",
	"DELETE /v1/m/models/keys/{id}":                                  "Removes a key/workspace governance reference.",
	"DELETE /v1/m/models/model-access/{id}":                          "Deletes one model-access rule by id.",
	"DELETE /v1/m/models/model-groups/{id}":                          "Deletes a model-group, but REFUSES (409) while any model-access grant still targets it — symmetric with create-time validateTargetGroup, which blocks a grant to a non-existent group.",
	"DELETE /v1/m/models/model-versions/{id}":                        "Deletes one model version by id, and refuses while a deployment still references it.",
	"DELETE /v1/m/models/owned-models/{id}":                          "Deletes one owned model by id, and refuses while a deployment still references it.",
	"DELETE /v1/m/models/routing-policies/{id}":                      "Deletes a routing policy.",
	"DELETE /v1/m/notify/routes/{id}":                                "Removes a route (hard delete). Admin-tier, self-audited.",
	"DELETE /v1/m/reporting/schedules/{id}":                          "Deletes one report schedule by id.",
	"DELETE /v1/m/reporting/templates/{type}":                        "Deletes the tenant custom template for one report type.",
	"DELETE /v1/m/sessions/runs/{ref}":                               "Deletes one session run by reference.",
	"DELETE /v1/m/sessions/templates/{id}":                           "Deletes one session template by id; a built-in template is refused.",
	"DELETE /v1/m/sessions/work-items/{id}/dependencies/{dep_id}":    "Removes one dependency edge from a work item as a validate, plan or apply command.",
	"DELETE /v1/m/sessions/workspaces/{ref}":                         "Deletes one session workspace by reference.",
	"DELETE /v1/m/sessions/workspaces/{ref}/files":                   "Deletes one file of a workspace, or a whole subtree when recursive=true.",
	"DELETE /v1/m/sourcescope/assignments/{id}":                      "Removes a connector→workspace assignment. Deleting the LAST assignment does not shrink the connector's audience, it flips the connector back to visible from EVERY workspace (ConnectorAssigned, :291-293).",
	"DELETE /v1/m/sourcescope/bindings/{id}":                         "Removes a binding (the source reverts to global/unbound, or to any remaining bindings).",
	"DELETE /v1/m/sourcescope/workspace-connectors/{id}":             "Removes a workspace connector and cascade-deletes its workspace-owned sealed secrets.",
	"DELETE /v1/system/orgs/{tenant_id}":                             "Hard-deletes one tenant org and its data, the unrecoverable step after the grace period has expired. The org is verified to exist first, so a missing tenant is not reported as a successful purge.",
	"DELETE /v1/tokens/{id}":                                         "Revokes one API token by id.",
	"GET /healthz":                                                   "Answers 200 while the process is alive. It is unauthenticated, exempt from the first-run setup gate, and exposes nothing beyond liveness.",
	"GET /livez":                                                     "Answers 200 while the process can serve. It runs no dependency check on purpose: a failing dependency must not trigger a liveness restart loop, which is what readiness is for.",
	"GET /metrics":                                                   "Serves the Prometheus text exposition of structural engine counters and gauges: request rates, in-flight requests, ingest throughput, Go runtime and store reachability. It is unauthenticated and setup-exempt, and carries no tenant data, no credentials and no per-record detail.",
	"GET /openapi.json":                                              "Returns this document, the stable REST contract of the engine. It is served without authentication and before first-run setup so client code generation can fetch it from a fresh install.",
	"GET /pod-readyz":                                                "Answers 200 when the process serves and the store is reachable, WITHOUT consulting leadership, so a hot standby is Ready to its orchestrator and a rolling update can progress. It answers 503 only when the store is down.",
	"GET /readyz":                                                    "Answers 200 when the store is reachable AND this node is the active writer, so a load balancer drains a standby or a wedged instance. The store ping carries a short timeout, and a pending first-run setup is reported without failing readiness.",
	"GET /status":                                                    "Returns the unauthenticated status summary: the same store and leader signals readiness probes use, enriched with the connector fleet aggregate when one is available. No tenant data is exposed.",
	"GET /v1/access-edges":                                           "Lists the recorded access edges of the resolved tenant, the read and read-write map between principals and resources. The read is recorded in the audit ledger.",
	"GET /v1/agents":                                                 "Lists the agents of the resolved tenant, with cursor paging.",
	"GET /v1/agents/{id}":                                            "Returns one agent of the resolved tenant by id.",
	"GET /v1/audit":                                                  "Reads a page of the tenant evidence ledger. Paging is keyset by SEQUENCE, not a cursor, and the page runs FORWARDS from ?from in ascending sequence order — oldest first. The response carries head_seq, the chain tip, which is what a caller pairs with ?from to address the newest events.",
	"GET /v1/audit/export":                                           "Streams the tenant ledger in the requested format, carrying the chain-integrity fields so an external archive or SIEM holds an independently verifiable copy. The export is itself recorded in the ledger.",
	"GET /v1/audit/pubkey":                                           "Returns the Ed25519 checkpoint verification key in PEM form, so an external party can verify exported checkpoints offline.",
	"GET /v1/audit/system":                                           "Reads the system-tenant evidence ledger, which records the cross-tenant operations. Superadmin only.",
	"GET /v1/audit/verify":                                           "Verifies the ledger chain structurally and verifies every signed checkpoint against the engine key. The verification is not itself recorded, so it does not grow the chain it inspects.",
	"GET /v1/auth/whoami":                                            "Returns the calling principal, its tenant grants, and the effective permission set it holds in each of them.",
	"GET /v1/connectors/health":                                      "Returns per-connector health metrics and the fleet summary, compositing the live source roster status with the connector catalog metadata.",
	"GET /v1/console/bus":                                            "Returns the privileged event-bus snapshot: subscribers, saturation, loss counters and the bridge state when one is configured.",
	"GET /v1/console/config/effective":                               "Returns the live effective configuration of the deployment with every secret-bearing value redacted.",
	"GET /v1/console/connectors":                                     "Lists the connector types this build offers, each with the configuration fields it accepts.",
	"GET /v1/console/health-summary":                                 "Returns the deployment health snapshot the console dashboard renders: readiness, the connector catalog size, the configured, running and failed connector counts, the user count and the SSO state.",
	"GET /v1/console/keys":                                           "Returns the non-secret custody inventory of the signing keys and sealers resolved at boot. No key material is returned.",
	"GET /v1/console/license":                                        "Returns the current license status and the entitlements it grants.",
	"GET /v1/console/secrets":                                        "Lists the sealed secrets by name with a non-secret hint for each. A stored value is never returned.",
	"GET /v1/console/setup-status":                                   "Returns which first-run setup steps the deployment has completed, so the console wizard can resume onboarding at the right step.",
	"GET /v1/console/sources":                                        "Lists the configured source connectors with the live status of each.",
	"GET /v1/console/sso":                                            "Returns the managed SSO or IdP configuration of the addressed scope. Sealed secrets are never returned.",
	"GET /v1/m/accessmap/attack-paths/escalation":                    "Serves GET /attack-paths/escalation?agent_id=...",
	"GET /v1/m/accessmap/attack-paths/exfil":                         "Serves GET /attack-paths/exfil?resource_id=...",
	"GET /v1/m/accessmap/attack-paths/reachability":                  "Serves GET /attack-paths/reachability?agent_id=...",
	"GET /v1/m/accessmap/attack-paths/summary":                       "Serves GET /attack-paths/summary.",
	"GET /v1/m/accessmap/drift":                                      "Returns the permitted-vs-observed least-privilege drift: unexpected accesses (observed, not permitted — the headline) and unused grants.",
	"GET /v1/m/accessmap/graph":                                      "Returns the R/RW graph as a React Flow node+edge contract, filtered by any of the supported edge columns.",
	"GET /v1/m/accessmap/neighbors":                                  "Returns the edges touching one node (id required) in a direction (outgoing|incoming|both, default both), as the same node+edge contract.",
	"GET /v1/m/adoption/developers":                                  "Returns the per-developer ROI breakdown from the admin Analytics lens.",
	"GET /v1/m/adoption/discrepancy":                                 "Returns the official-vs-observed daily comparison over the requested window.",
	"GET /v1/m/adoption/summary":                                     "Returns BOTH non-additive lenses over the window — the per-developer admin Analytics lens and the per-session OTLP telemetry lens — plus distinct developer/ team counts and the Claude-API-only boundary.",
	"GET /v1/m/adoption/teams":                                       "Returns the per-team breakdown from the OTLP telemetry lens (the only lens that carries team labels).",
	"GET /v1/m/adoption/trend":                                       "Returns a per-day series for ONE lens (default analytics).",
	"GET /v1/m/capabilities/configs":                                 "Lists managed MCP-server configs, optionally filtered.",
	"GET /v1/m/capabilities/configs/{id}":                            "Returns one managed config.",
	"GET /v1/m/capabilities/configs/{id}/revisions":                  "Returns the immutable version history of a config, newest revision first.",
	"GET /v1/m/capabilities/servers":                                 "Lists the live MCP-server catalog: every discovered MCPServer core entity enriched with its derived connection health, managed-config marker and tool count.",
	"GET /v1/m/capabilities/servers/{id}":                            "Returns the full management view of one MCP server.",
	"GET /v1/m/capabilities/skills":                                  "Lists the discovered skills (and the surface through which plugins/subagents appear), optionally filtered by ?server_id.",
	"GET /v1/m/capabilities/toolpins":                                "Lists the tenant approved tool pins, and answers 501 when no pin verifier is wired.",
	"GET /v1/m/capabilities/tools":                                   "Lists the discovered tools with their UNTRUSTED annotation hints, optionally filtered by ?server_id.",
	"GET /v1/m/capabilities/wiring":                                  "Returns the capability-connection graph, optionally filtered by origin or capability.",
	"GET /v1/m/catalog/connector-admission/policy":                   "Returns the connector admission policy in force, or the unset defaults when none has been authored.",
	"GET /v1/m/catalog/connector-admissions":                         "Lists the recorded connector-entry admission verdicts.",
	"GET /v1/m/catalog/entries":                                      "Lists catalog entries, optionally filtered by kind/status/slug.",
	"GET /v1/m/catalog/entries/{id}":                                 "Returns one catalog entry.",
	"GET /v1/m/catalog/entries/{id}/verify":                          "Recomputes and verifies an approved entry's integrity (hash + signature).",
	"GET /v1/m/catalog/instances":                                    "Lists instantiations, optionally filtered by status/entry_id.",
	"GET /v1/m/catalog/instances/{id}":                               "Returns one instance.",
	"GET /v1/m/catalog/mcp-admission/policy":                         "Returns the MCP server admission policy in force, or the unset defaults when none has been authored.",
	"GET /v1/m/catalog/mcp-admissions":                               "Lists the recorded MCP-entry admission verdicts.",
	"GET /v1/m/catalog/pubkey":                                       "Returns the catalog signing public key (for external verification), or reports that signing is not configured.",
	"GET /v1/m/claude-agents/sessions/{id}/events":                   "Serves a session's thread events from the wired events reader.",
	"GET /v1/m/claude-policy/{surface}/artifact":                     "Serves the latest (or ?revision=N) SIGNED artifact for a surface — the agent-pull half of the decided distribution mechanism.",
	"GET /v1/m/claude-policy/{surface}/distribution":                 "Serves the per-surface truth view (read tier): published vs signed vs observed, scope by scope — real state only, every absence named.",
	"GET /v1/m/claude-policy/{surface}/versions":                     "Returns a surface's revision history (metadata only). Read tier.",
	"GET /v1/m/claude-policy/{surface}/versions/{revision}":          "Returns one revision with its content. Read tier.",
	"GET /v1/m/compliance/aims/pack":                                 "Lists the stored AI management system packs.",
	"GET /v1/m/compliance/aims/pack/{id}":                            "Returns one AI management system pack with its full detail.",
	"GET /v1/m/compliance/aims/pack/{id}/export":                     "Exports one AI management system pack together with a ledger integrity proof taken at export time.",
	"GET /v1/m/compliance/calendar":                                  "Returns the regulatory calendar + watchlist (read-tier; static verified data, not tenant evidence). ?framework= filters both lists to the milestones/items linked to one catalog framework.",
	"GET /v1/m/compliance/capabilities":                              "Returns the capability catalog with the tenant's live evidence state — the \"what the platform can evidence right now\" map.",
	"GET /v1/m/compliance/claude-files":                              "Serves the observe inventory (read-tier).",
	"GET /v1/m/compliance/data-subjects/{id}/erasure-status":         "Returns the erasure status of one data subject, optionally narrowed by subject_kind.",
	"GET /v1/m/compliance/depth/ccm/drift":                           "Lists the recorded Cloud Controls Matrix drift findings.",
	"GET /v1/m/compliance/depth/ccm/snapshots":                       "Lists the stored Cloud Controls Matrix snapshots.",
	"GET /v1/m/compliance/depth/ccm/snapshots/{id}":                  "Returns one Cloud Controls Matrix snapshot with its full detail.",
	"GET /v1/m/compliance/depth/fedramp":                             "Lists the stored FedRAMP key security indicators.",
	"GET /v1/m/compliance/depth/fedramp/{id}":                        "Returns one FedRAMP key security indicator with its full detail.",
	"GET /v1/m/compliance/depth/fedramp/{id}/export":                 "Exports the maintained FedRAMP KSI document with a LIVE ledger integrity proof.",
	"GET /v1/m/compliance/depth/sector":                              "Lists the stored sector compliance packs.",
	"GET /v1/m/compliance/depth/sector/{id}":                         "Returns one sector compliance pack with its full detail.",
	"GET /v1/m/compliance/depth/sector/{id}/export":                  "Exports the maintained sector overlay pack with a LIVE ledger integrity proof.",
	"GET /v1/m/compliance/depth/us-law":                              "Lists the stored US state law packs.",
	"GET /v1/m/compliance/depth/us-law/{id}":                         "Returns one US state law pack with its full detail.",
	"GET /v1/m/compliance/depth/us-law/{id}/export":                  "Exports the maintained US state law pack with a LIVE ledger integrity proof so the export proves the pack was anchored to a tamper-evident ledger.",
	"GET /v1/m/compliance/dora":                                      "Assembles and returns the DORA export. Exporting the tenant's risk register + incident timeline is a SENSITIVE evidence read, so it self-audits in a committed transaction, exactly like the evidence export.",
	"GET /v1/m/compliance/dora/incidents":                            "Lists the classified DORA incidents, optionally narrowed to the major ones.",
	"GET /v1/m/compliance/dora/incidents/{id}":                       "Returns one classified DORA incident with its full detail.",
	"GET /v1/m/compliance/dora/incidents/{id}/report":                "Exports the structured major-incident report draft with a LIVE ledger integrity proof.",
	"GET /v1/m/compliance/dora/register":                             "Lists the maintained DORA registers of information.",
	"GET /v1/m/compliance/dora/register/{id}":                        "Returns one DORA register of information with its full detail.",
	"GET /v1/m/compliance/dora/register/{id}/export":                 "Exports one DORA register of information together with a ledger integrity proof taken at export time.",
	"GET /v1/m/compliance/erasure":                                   "Lists the erasure requests, optionally filtered by status.",
	"GET /v1/m/compliance/erasure/{id}":                              "Returns one erasure request with its full detail.",
	"GET /v1/m/compliance/erasure/{id}/events":                       "Lists the recorded lifecycle events of one erasure request.",
	"GET /v1/m/compliance/erasure/{id}/receipt":                      "Returns the completion receipt of one erasure request, or 404 while none has been issued.",
	"GET /v1/m/compliance/evidence":                                  "Lists the stored evidence packages, optionally filtered by framework.",
	"GET /v1/m/compliance/evidence/{id}":                             "Returns a sealed package + its per-control results. It is a SENSITIVE evidence read, so it self-audits in a committed transaction.",
	"GET /v1/m/compliance/evidence/{id}/export":                      "Exports a sealed package as an auditor-consumable dataset + manifest. ?format=csv flattens the control results; json (default) is the full package + manifest + integrity proof.",
	"GET /v1/m/compliance/frameworks":                                "Lists the compliance frameworks the engine ships, each with its authority, version pin and control count.",
	"GET /v1/m/compliance/frameworks/{id}":                           "Returns one compliance framework with its controls.",
	"GET /v1/m/compliance/frameworks/{id}/gaps":                      "Returns the controls of one framework that the tenant evidence does not satisfy, as JSON or CSV.",
	"GET /v1/m/compliance/frameworks/{id}/status":                    "Assesses one framework against the tenant live evidence and returns the per-control result, as JSON or CSV.",
	"GET /v1/m/compliance/hipaa/gap-report":                          "Assesses the HIPAA technical safeguards against the tenant live evidence and returns the gaps.",
	"GET /v1/m/compliance/holds":                                     "Lists the legal holds, optionally filtered by status.",
	"GET /v1/m/compliance/holds/check":                               "Answers whether a subject is under a legal hold, for a caller outside this process; subject_kind and subject_ref travel together.",
	"GET /v1/m/compliance/holds/{id}":                                "Returns one legal hold with its full detail.",
	"GET /v1/m/compliance/holds/{id}/events":                         "Lists the recorded lifecycle events of one legal hold.",
	"GET /v1/m/compliance/nis2/incidents":                            "Lists the recorded NIS2 incidents.",
	"GET /v1/m/compliance/nis2/incidents/{id}":                       "Returns one NIS2 incident with its full detail.",
	"GET /v1/m/compliance/nis2/incidents/{id}/export":                "Exports one NIS2 incident together with a ledger integrity proof taken at export time.",
	"GET /v1/m/compliance/oscal/profiles":                            "Lists the registered OSCAL profiles, optionally filtered by framework.",
	"GET /v1/m/compliance/oscal/profiles/{id}":                       "Returns one registered OSCAL profile with its full detail.",
	"GET /v1/m/compliance/residency":                                 "Lists the recorded data residency attestations.",
	"GET /v1/m/compliance/retention/classes":                         "Returns the fixed §2 registry with the advisory recommendations and the §7 provider-floor annotation.",
	"GET /v1/m/compliance/retention/policies":                        "Lists the retention policies with the state of each.",
	"GET /v1/m/compliance/retention/runs":                            "Lists the recorded retention runs, optionally filtered by data class.",
	"GET /v1/m/compliance/risk":                                      "Lists the recorded risk entries, optionally filtered by tier.",
	"GET /v1/m/compliance/summary":                                   "Returns a cross-framework posture summary for the tenant (the org/ tenant roll-up).",
	"GET /v1/m/consoleviews/views":                                   "Returns the caller's own views plus the tenant's shared views, optionally scoped with ?feature_id=.",
	"GET /v1/m/consoleviews/views/{id}":                              "Returns one view. A view the caller may not see is a 404, not a 403 — visibility must not leak existence (mirrors the store's not-found == other-tenant rule).",
	"GET /v1/m/deploy/definitions":                                   "Lists definitions, optionally filtered by environment or desired_status.",
	"GET /v1/m/deploy/definitions/{id}":                              "Returns one definition with its current desired spec and, when applied, the real-state snapshot from the core Deployment entity.",
	"GET /v1/m/deploy/definitions/{id}/revisions":                    "Returns the append-only revision history of a definition — the rollback targets.",
	"GET /v1/m/deploy/operations":                                    "Lists the append-only operation ledger, optionally filtered by definition_id, op or status.",
	"GET /v1/m/deploy/wirings":                                       "Lists the declared PERMITTED wirings, optionally filtered by definition_id or status.",
	"GET /v1/m/evals/calibration/items":                              "Lists a set's reference items (?set=, default all sets).",
	"GET /v1/m/evals/calibration/reports":                            "Lists calibration reports (?set=, ?judge_model=), newest first per the occurred_at index order of the store's default listing.",
	"GET /v1/m/evals/gate":                                           "Lists gate evaluations (?suite_ref=, ?verdict=).",
	"GET /v1/m/evals/gate/{id}":                                      "Returns one gate evaluation (CI re-checks it after an override).",
	"GET /v1/m/evals/runs":                                           "Lists the tenant's runs (filterable by suite_ref/subject_ref).",
	"GET /v1/m/evals/runs/{id}":                                      "Returns one run with its per-case results attached.",
	"GET /v1/m/evals/runs/{id}/results":                              "Lists one run's per-case results.",
	"GET /v1/m/evals/runs/{id}/stream":                               "Streams a completed run's per-case results, summary and a done sentinel, then closes.",
	"GET /v1/m/evals/scorecards":                                     "Aggregates the tenant's runs into scorecards. It supports optional suite_ref / subject_ref filters and a format=csv|json (json is the default).",
	"GET /v1/m/evals/suites":                                         "Lists the tenant's suites (filterable by name/status).",
	"GET /v1/m/evals/suites/{id}":                                    "Returns one suite.",
	"GET /v1/m/evals/suites/{id}/cases":                              "Lists a suite's cases (ordered by case_key for a stable view).",
	"GET /v1/m/eventing/dead-letters":                                "Lists the deliveries that ended in the dead-letter queue, narrowed to the event types the caller is authorized for.",
	"GET /v1/m/eventing/deliveries":                                  "Lists delivery state, optionally filtered by ?subscription, ?status (status=dead is the DLQ view), ?origin (live|replay) and ?event_type.",
	"GET /v1/m/eventing/egress-policy":                               "Reports whether a destination policy is in force here, and what this deployment's disposition for the control is.",
	"GET /v1/m/eventing/egress-policy/compat":                        "Reports the destinations that only a legacy exception still permits under the tenant egress policy.",
	"GET /v1/m/eventing/event-types":                                 "Returns the public event-type catalog: the subscribable types, their stability tier and the permission gating receipt.",
	"GET /v1/m/eventing/events":                                      "Lists the tenant's captured event log from a seq cursor — the pull-side companion of replay (find your cursor, inspect what a delivery carried).",
	"GET /v1/m/eventing/subscriptions":                               "Lists the tenant's subscriptions.",
	"GET /v1/m/eventing/subscriptions/{id}":                          "Returns one subscription (never its secret).",
	"GET /v1/m/eventing/subscriptions/{id}/revisions":                "Lists a subscription's revision ledger, keyset-paginated by the time-ordered row id (chronological by ingestion, the decision-ledger convention).",
	"GET /v1/m/finops/alerts":                                        "Lists the budget alerts, optionally filtered by budget_id.",
	"GET /v1/m/finops/analytics/team-summary":                        "Returns team-level cost aggregation with project/model breakdown and a per-calendar-day trend series for a fixed period (7d/30d/90d).",
	"GET /v1/m/finops/budgets":                                       "Lists the budgets defined for the tenant.",
	"GET /v1/m/finops/budgets/{id}":                                  "Returns one budget by id; a policy of any other kind reads as absent.",
	"GET /v1/m/finops/budgets/{id}/status":                           "Returns the live consumption of one budget against its limit.",
	"GET /v1/m/finops/comparison":                                    "Serves GET /comparison — the model cost comparison endpoint.",
	"GET /v1/m/finops/cost-centers":                                  "Lists the cost centers, optionally filtered by status.",
	"GET /v1/m/finops/cost-centers/{id}":                             "Returns one cost center with its full detail.",
	"GET /v1/m/finops/cost-centers/{id}/mappings":                    "Lists the mappings that attribute spend to one cost center.",
	"GET /v1/m/finops/forecast":                                      "Forecasts spend for a period (monthly by default), optionally over a window of days and broken down by a dimension.",
	"GET /v1/m/finops/model-rates":                                   "Lists rate catalog entries with optional provider/model filters, sorted by effective_from descending (most recent first).",
	"GET /v1/m/finops/model-rates/{id}":                              "Returns a single rate catalog entry by ID.",
	"GET /v1/m/finops/outcomes":                                      "Lists graded outcomes, optionally filtered by subject.",
	"GET /v1/m/finops/recommendations":                               "Returns the cost optimization recommendations derived from the tenant recorded spend.",
	"GET /v1/m/finops/seats/utilization":                             "Joins the seat denominators with the per-day distinct active actors for a provider over an inclusive [from, to] day range.",
	"GET /v1/m/finops/spend":                                         "Returns spend aggregated by a dimension (model by default) over an optional RFC3339 since/until window.",
	"GET /v1/m/finops/spend/allocation":                              "Returns spend allocated to its attribution dimensions over an optional RFC3339 since/until window.",
	"GET /v1/m/finops/spend/export":                                  "Streams the cost read-model as a FOCUS-conformant CSV. The default is the estimated (granular, full-coverage incl.",
	"GET /v1/m/finops/spend/reconciliation":                          "Reconciles metered spend against the ingested provider cost over an optional RFC3339 since/until window.",
	"GET /v1/m/finops/spend/summary":                                 "Summarizes spend over an optional RFC3339 since/until window.",
	"GET /v1/m/finops/spend/trend":                                   "Returns the per-day spend series over an optional RFC3339 since/until window.",
	"GET /v1/m/finops/spend/unified":                                 "Returns spend across every metered surface in one view, over an optional RFC3339 since/until window.",
	"GET /v1/m/finops/statements":                                    "Lists the chargeback statements, newest period first, optionally filtered by cost center, period and status.",
	"GET /v1/m/finops/statements/{id}":                               "Returns one chargeback statement with its full detail.",
	"GET /v1/m/finops/statements/{id}/export":                        "Exports one chargeback statement together with its line items.",
	"GET /v1/m/finops/value":                                         "Serves the cost-per-outcome breakdown by ?dimension (agent|identity| session, default agent) over the standard since/until window.",
	"GET /v1/m/finops/value/summary":                                 "Serves the CFO panel (totals + cancellation-risk list).",
	"GET /v1/m/governance/agent-risk-profiles":                       "Lists profiles, optionally filtered by tier.",
	"GET /v1/m/governance/agent-risk-profiles/{id}":                  "Returns one profile.",
	"GET /v1/m/governance/approvals":                                 "Lists requests, optionally filtered by status/action. The stored status is filtered; a request that is stored-pending but past expiry is reported with effective status \"expired\" in its DTO (the sweep materializes it).",
	"GET /v1/m/governance/approvals/{id}":                            "Returns one request with its effective status.",
	"GET /v1/m/governance/approvals/{id}/decisions":                  "Returns the immutable decision trail for a request — the action→human traceability view.",
	"GET /v1/m/governance/bindings":                                  "Lists agent↔identity bindings, flagging identities shared across agents.",
	"GET /v1/m/governance/breakglass":                                "Lists grants, optionally filtered by stored status; a grant past its expiry reads as \"expired\" in its DTO regardless.",
	"GET /v1/m/governance/breakglass/{id}":                           "Returns one grant with its effective status.",
	"GET /v1/m/governance/breakglass/{id}/uses":                      "Returns the immutable use trail for a grant — what actually proceeded under the emergency window (the post-review's evidence).",
	"GET /v1/m/governance/emerging-identity-standards":               "Surfaces the design-toward registry read-only, with an explicit disclaimer that it is tracked, not implemented (IDN-12).",
	"GET /v1/m/governance/groups":                                    "Lists the reconciled collections (groups/roles/policies).",
	"GET /v1/m/governance/groups/{ref}/members":                      "Lists a group's members. With ?transitive=true it walks nested collections (a group within a group) to the leaf identities, bounded by a visited set and a depth cap so a cyclic directory cannot loop.",
	"GET /v1/m/governance/guardian/actions":                          "Lists the containment trail, optionally by status.",
	"GET /v1/m/governance/guardian/rules":                            "Lists the guardian rules defined for the tenant.",
	"GET /v1/m/governance/identities":                                "Lists the reconciled identity roster. Viewing who exists in the estate is recon-relevant, so the read self-audits in a committed transaction before returning, exactly as the access-map graph reads do.",
	"GET /v1/m/governance/killswitch":                                "Lists stop rows, optionally filtered by stored status — the persisted, visible stop state (who, when, scope, reason).",
	"GET /v1/m/governance/killswitch/state":                          "Returns the live stop posture for the tenant.",
	"GET /v1/m/governance/killswitch/{id}":                           "Returns one stop row.",
	"GET /v1/m/governance/killswitch/{id}/evidence":                  "Builds and returns the incident evidence pack. Admin-tier; the export is self-audited BEFORE the response is written.",
	"GET /v1/m/governance/nhi":                                       "Lists the NHI lifecycle rows, optionally filtered by enforcement state or offboard state.",
	"GET /v1/m/governance/nhi/posture":                               "Aggregates the lifecycle posture. Read-tier, self-audited.",
	"GET /v1/m/governance/nhi/{ref}":                                 "Returns one NHI lifecycle row by identity_ref.",
	"GET /v1/m/governance/nhi/{ref}/events":                          "Lists the append-only lifecycle event trail for one NHI.",
	"GET /v1/m/governance/pdp/active":                                "Reports what the STORE currently selects for an engine, and — for cedar — discloses the other surfaces that are unioned into the enforced policy.",
	"GET /v1/m/governance/pdp/tests":                                 "Returns the authored decision tests for one policy engine (cedar or opa), at the current or a requested revision.",
	"GET /v1/m/governance/pdp/versions":                              "Lists the cedar + opa authored revisions (the shared revision store, kinds cedar/opa).",
	"GET /v1/m/governance/pdp/versions/{revision}":                   "Returns one authored policy revision of the engine named by the engine query parameter.",
	"GET /v1/m/governance/policies":                                  "Lists governance policies, optionally filtered by kind/enabled.",
	"GET /v1/m/governance/policies/{id}":                             "Returns one governance policy.",
	"GET /v1/m/governance/rbac/catalog":                              "Returns the vocabulary an authoring console needs: the scopeable kinds, the kinds that carry a scope tree, the grantable module permissions, the verbs, the built-in roles and the subject kinds.",
	"GET /v1/m/governance/rbac/delegation-authority":                 "Returns the domains within which the calling principal may delegate authority; a superadmin is answered with no domain restriction.",
	"GET /v1/m/governance/rbac/grants":                               "Lists the scoped RBAC grants of the tenant.",
	"GET /v1/m/governance/rbac/grants/{id}":                          "Returns one scoped RBAC grant by id.",
	"GET /v1/m/governance/rbac/permission-groups":                    "Lists the permission groups defined for the tenant.",
	"GET /v1/m/governance/rbac/permission-groups/{name}":             "Returns one permission group by name.",
	"GET /v1/m/governance/rbac/roles":                                "Lists the custom roles defined for the tenant.",
	"GET /v1/m/governance/rbac/roles/{name}":                         "Returns one custom role by name.",
	"GET /v1/m/governance/routine-policies":                          "Lists routine policies, optionally filtered by scope_kind or enabled.",
	"GET /v1/m/governance/routine-policies/posture":                  "Returns the routine governance posture: EVERY policy in the tenant (enabled or not, with the two counters splitting them), plus the COMPOSED decision for one resolution scope.",
	"GET /v1/m/governance/routine-policies/{id}":                     "Returns one routine policy by ID.",
	"GET /v1/m/health/checks":                                        "Lists declared checks, optionally filtered by ?subject_kind and ?desired_status.",
	"GET /v1/m/health/checks/{id}":                                   "Returns one check projected as status.",
	"GET /v1/m/health/dependencies":                                  "Returns the dependency map as a React-Flow node+edge graph: a page of auto-discovered dependency edges, plus the deduplicated node set derived from them, each node annotated with its subject's health.",
	"GET /v1/m/health/events":                                        "Lists the append-only reliability transition ledger, optionally filtered by ?subject_kind and ?subject_ref.",
	"GET /v1/m/health/incidents":                                     "Lists incidents, optionally filtered by ?state (open| resolved), ?subject_kind and ?subject_ref.",
	"GET /v1/m/health/incidents/{id}":                                "Returns one incident.",
	"GET /v1/m/health/sla":                                           "Reports the reliability of a subject over a trailing window (?subject_kind=&subject_ref=&window_seconds=).",
	"GET /v1/m/health/status":                                        "Lists the current health of monitored subjects (the check rows projected as status), optionally filtered by ?state and ?subject_kind.",
	"GET /v1/m/health/stream":                                        "Serves live health changes as server-sent events, pinned to the request's single authorized tenant so a client only ever sees its own tenant's subjects.",
	"GET /v1/m/identity/external-keys":                               "Serves the read-only CMEK inventory (ANT2-04).",
	"GET /v1/m/identity/residency":                                   "Serves the read-only workspace governance object (ANT2-06). ARCHIVED workspaces are omitted: an archived workspace takes no inference, so listing its missing CMEK would report a residency gap nobody can act on or exploit.",
	"GET /v1/m/identity/sso":                                         "Serves the SSO connection state. The redirect_uri is derived through core's OWN rule (api.FederationCallbackURL) rather than a copy, so what the panel shows an operator to paste into their IdP is exactly what the login leg will send — including under a trusted reverse proxy.",
	"GET /v1/m/identity/wif":                                         "Serves the declared WIF object graph. Viewing the federation graph is a privileged, recon-relevant read, so it self-audits in a committed transaction BEFORE returning — deny-closed: if the audit write fails, the graph is NOT served.",
	"GET /v1/m/inferenceproxy/config":                                "Returns the tenant's proxy config (the safe defaults when none is set).",
	"GET /v1/m/inferenceproxy/dlp/rules":                             "Lists the tenant's inference-egress DLP rules.",
	"GET /v1/m/inventory/entities":                                   "Lists catalog entries, optionally filtered by kind and status, paginated by the default id keyset cursor.",
	"GET /v1/m/inventory/entities/{kind}/{id}":                       "Returns one catalog entry plus a minimal projection of the core entity it overlays.",
	"GET /v1/m/inventory/summary":                                    "Returns the estate overview: counts by entity kind and by signal source.",
	"GET /v1/m/knowledge/context-policies":                           "Lists context policies, optionally filtered by scope.",
	"GET /v1/m/knowledge/data-products":                              "Lists the data products, optionally filtered by status, owner_ref and kb_ref.",
	"GET /v1/m/knowledge/data-products/{id}":                         "Returns one data product with its full detail.",
	"GET /v1/m/knowledge/data-products/{id}/contracts":               "Lists the data contracts authored for one data product.",
	"GET /v1/m/knowledge/data-products/{id}/contracts/active":        "Returns the data contract currently active for one data product.",
	"GET /v1/m/knowledge/data-products/{id}/contracts/{ver}":         "Returns one numbered version of a data product contract.",
	"GET /v1/m/knowledge/data-products/{id}/events":                  "Lists the recorded lifecycle events of one data product.",
	"GET /v1/m/knowledge/data-products/{id}/health":                  "Evaluates and returns the current health of one data product.",
	"GET /v1/m/knowledge/dlp/rules":                                  "Lists the tenant's DLP rules.",
	"GET /v1/m/knowledge/documents/{id}":                             "Returns one document's metadata + provenance (no body).",
	"GET /v1/m/knowledge/kbs":                                        "Lists knowledge bases, optionally filtered by status.",
	"GET /v1/m/knowledge/kbs/{id}":                                   "Returns one knowledge base with its full detail.",
	"GET /v1/m/knowledge/kbs/{id}/documents":                         "Lists a KB's ingested documents (metadata only; no body).",
	"GET /v1/m/knowledge/labels":                                     "Lists sensitivity labels, optionally filtered by subject_kind and/or kb_id (labels are governance metadata — counts and rules, no content).",
	"GET /v1/m/knowledge/lineage":                                    "Lists lineage records, optionally filtered by kb_id, agent_ref or decision.",
	"GET /v1/m/knowledge/lineage/{id}":                               "Returns one lineage record (self-audited).",
	"GET /v1/m/knowledge/memory":                                     "Lists the memory visible to the DECLARED read context (?agent_ref= plus the ?user_ref=/?session_ref= namespace), EXCLUDING expired entries (lazy expiry), entries above the reader's classification clearance, and entries that fail the integrity self-check (fail closed; withheld count reported, detection audited).",
	"GET /v1/m/knowledge/memory/all":                                 "Lists every agent memory entry in the tenant scope, optionally narrowed by agent_ref, and flags any entry whose integrity check failed.",
	"GET /v1/m/knowledge/memory/export":                              "Streams the caller's clearance-visible memory as signed JSONL: line 1 is the signed manifest, each following line one memPortRow.",
	"GET /v1/m/knowledge/memory/{id}":                                "Returns one memory entry. 404 if expired, absent, scoped OUTSIDE the declared read context (no existence leak across namespaces), or classified ABOVE the reader's clearance (E5 — the SAME classification filter handleListMemory applies, so a reader that knows/guesses a classified entry's id cannot retrieve it whole; the deny is indistinguishable from absent); 409 integrity_violation (entry withheld, detection recorded) if it fails the self-check.",
	"GET /v1/m/knowledge/prompts":                                    "Lists the stored prompts.",
	"GET /v1/m/knowledge/prompts/{id}":                               "Returns one prompt (its current pointer + status).",
	"GET /v1/m/knowledge/prompts/{id}/revisions":                     "Lists a prompt's immutable revision history.",
	"GET /v1/m/knowledge/prompts/{id}/revisions/{rev}":               "Returns one immutable revision by number.",
	"GET /v1/m/knowledge/scans":                                      "Lists the append-only discovery scan evidence. Self-audited (reading what was scanned and what it found is recon-relevant, the lineage precedent).",
	"GET /v1/m/models/access-tier-entitlements":                      "Lists operator-attested entitlement states, optionally filtered by tier or state.",
	"GET /v1/m/models/admission-policy":                              "Returns the model admission policy in force, or the observe-mode defaults when none has been authored.",
	"GET /v1/m/models/agent-artifacts":                               "Lists the registered agent artifacts, optionally filtered by artifact_class.",
	"GET /v1/m/models/agent-artifacts/aibom":                         "Returns the live agent-supply-chain BOM (read-only export, not sealed; read-tier, not audited — observer effect).",
	"GET /v1/m/models/agent-artifacts/aiboms":                        "Lists the agent-supply-chain BOM seals.",
	"GET /v1/m/models/aiboms":                                        "Lists the sealed AI bills of materials, optionally filtered by owned_ref.",
	"GET /v1/m/models/catalog":                                       "Returns the declared reference catalog.",
	"GET /v1/m/models/data-governance":                               "Returns the Claude context-management / memory-tool + ZDR-eligibility matrix (CLA-15): for each feature, whether it can clear the model's working context server-side (a forensics implication), where its data persists, and whether it is ZDR-eligible (a data-residency concern).",
	"GET /v1/m/models/datasets":                                      "Lists the registered datasets, optionally filtered by owned_ref.",
	"GET /v1/m/models/features":                                      "Returns the capability matrix: per API feature, which declared families support it.",
	"GET /v1/m/models/finetune-jobs":                                 "Lists the fine-tune jobs, optionally filtered by status.",
	"GET /v1/m/models/finetune-jobs/{id}":                            "Returns one fine-tune job with its full detail.",
	"GET /v1/m/models/gpai-posture":                                  "Lists the attested GPAI posture of brokered providers, optionally filtered by provider_ref or verified.",
	"GET /v1/m/models/inference-deployments":                         "Lists the inference deployments, optionally filtered by runtime and status.",
	"GET /v1/m/models/keys":                                          "Lists key/workspace governance references, optionally filtered by ref_kind, provider_ref or status.",
	"GET /v1/m/models/model-access":                                  "Lists the model-access rules, optionally filtered by subject and target kind and reference.",
	"GET /v1/m/models/model-admissions":                              "Lists the recorded model admission verdicts, optionally filtered by version_ref and by whether the signature verified.",
	"GET /v1/m/models/model-groups":                                  "Lists the model groups defined for the tenant.",
	"GET /v1/m/models/model-groups/{id}":                             "Returns one model group with its full detail.",
	"GET /v1/m/models/model-versions":                                "Lists the model versions, optionally filtered by owned_ref and status.",
	"GET /v1/m/models/models":                                        "Lists the governed core models (the live estate, enriched).",
	"GET /v1/m/models/models/{id}":                                   "Returns one governed model with its provider reference.",
	"GET /v1/m/models/owned-models":                                  "Lists the owned models, optionally filtered by kind and status.",
	"GET /v1/m/models/owned-models/{id}":                             "Returns one owned model with its full detail.",
	"GET /v1/m/models/owned-models/{id}/aibom":                       "Returns the live AI bill of materials for an owned model (read-only export, not sealed).",
	"GET /v1/m/models/owned-models/{id}/model-card":                  "Returns the generated model card for an owned model (read-only export from the live inventory; read-tier, not audited — observer effect, exactly like the AIBOM generate route). ?format=md renders Markdown; JSON is the default.",
	"GET /v1/m/models/platforms":                                     "Returns the declared platforms reference. It NEVER mutates and NEVER 500s: with no provider wired, or on a transient provider error, it returns available=false with a reason and empty (not null) collections.",
	"GET /v1/m/models/rate-limits":                                   "Returns the read-only rate-limit inventory. It NEVER mutates and NEVER 500s: with no provider wired, or on a transient fetch error, it returns available=false with a reason and an empty inventory.",
	"GET /v1/m/models/routing-policies":                              "Lists routing policies.",
	"GET /v1/m/models/routing-policies/{id}":                         "Returns one routing policy.",
	"GET /v1/m/models/tool-types":                                    "Returns the declared dated Anthropic tool-type catalog.",
	"GET /v1/m/models/workspace-residency":                           "Lists the persisted per-workspace residency references, optionally filtered by workspace_ref.",
	"GET /v1/m/notify/deliveries":                                    "Lists the append-only delivery ledger, optionally filtered by ?status, ?finding_kind, ?destination and ?route (a route id).",
	"GET /v1/m/notify/destinations":                                  "Returns the provisioned destination names (from the wired transport).",
	"GET /v1/m/notify/match-types":                                   "Returns the routable event types a route's match_types may name — the console's multiselect source and the reference the validation errors point at.",
	"GET /v1/m/notify/outbox":                                        "Lists the durable outbox, optionally filtered by ?status (e.g. status=dead for the dead-letter queue view) and ?destination.",
	"GET /v1/m/notify/routes":                                        "Lists routing rules, optionally filtered by ?destination and ?enabled.",
	"GET /v1/m/notify/routes/{id}":                                   "Returns one route.",
	"GET /v1/m/notify/routes/{id}/revisions":                         "Lists a route's revision ledger, keyset-paginated by the time-ordered row id (chronological by ingestion, the decision-ledger convention).",
	"GET /v1/m/observability/attestation":                            "Returns the measured truth about the RUNNING binary. Everything in the binary block is measured in-process at request time (except the self-hash, computed once); the release block is MEASURED from this binary's own link-time facts; the pipeline block is declared.",
	"GET /v1/m/observability/ingestion-health":                       "Returns the per-standard table + the live per-source counters.",
	"GET /v1/m/observability/traces":                                 "Lists the traces correlated in the ledger window, newest first. duration_ms is the LEDGER-EVENT WINDOW of each trace (max−min event time), not a span duration; status is always \"unset\"; services is the single-writer constant — see the constants above for why each is the honest value.",
	"GET /v1/m/observability/traces/{id}":                            "Returns one trace's detail: ONE span row per distinct span_id, each grouping the ledger events that engine span produced.",
	"GET /v1/m/observability/traces/{id}/export":                     "Exports one trace as OTLP-compatible JSON so the operator can import it into Jaeger, Grafana Tempo, Datadog, or any OTLP-aware tool.",
	"GET /v1/m/orchestration/decisions":                              "Lists the whole append-only fire/miss ledger for the tenant.",
	"GET /v1/m/orchestration/flows":                                  "Returns the derived multi-agent flows (a supervisor and the workers it delegates to) with a read-time-derived lifecycle state — a privileged, self-audited read.",
	"GET /v1/m/orchestration/graph":                                  "Returns the React-Flow communication/delegation graph, derived from the relation table, with honest coverage.",
	"GET /v1/m/orchestration/graph/neighbors":                        "Returns the subgraph around one node (its incoming and/or outgoing relations) — a privileged, self-audited read.",
	"GET /v1/m/orchestration/schedules":                              "Lists governed schedules. It runs the cadence scan first so a just-missed schedule is reflected, then derives each subject's last observed activity for display.",
	"GET /v1/m/orchestration/schedules/{id}":                         "Returns one schedule with its derived health and last observed activity.",
	"GET /v1/m/orchestration/schedules/{id}/decisions":               "Lists the append-only fire/miss ledger for one schedule.",
	"GET /v1/m/orchestration/schedules/{id}/revisions":               "Lists a schedule's revision ledger, keyset-paginated by the time-ordered row id (chronological by ingestion, the decision-ledger convention).",
	"GET /v1/m/orchestration/stream":                                 "Serves the live comm graph as server-sent events, pinned to the request's single authorized tenant so a client only ever sees its own tenant's relations.",
	"GET /v1/m/orchestration/timeline":                               "Returns one subject's merged orchestration history (its delegation activity and its fire/miss decisions) in reverse-chronological order — a privileged, self-audited read.",
	"GET /v1/m/orchestration/workflows":                              "Lists the tenant's workflows (list shape, no graphs).",
	"GET /v1/m/orchestration/workflows/{id}":                         "Returns one workflow with its full canonical graph.",
	"GET /v1/m/orchestration/workflows/{id}/revisions":               "Lists a workflow's append-only revision ledger.",
	"GET /v1/m/orchestration/workflows/{id}/runs":                    "Lists a workflow's runs, newest-first pagination by id.",
	"GET /v1/m/orchestration/workflows/{id}/runs/{run}":              "Returns one run's timeline. The run must belong to the path's workflow — a run of ANOTHER workflow is not found, never confirmed.",
	"GET /v1/m/posture/export":                                       "Assembles the posture projection inside ONE audited tenant scope and returns it.",
	"GET /v1/m/recording/config":                                     "Returns the resolved recording policy (defaults applied).",
	"GET /v1/m/recording/notice":                                     "Returns the caller's recording posture for this tenant: what is recorded, the consent mode, and whether THIS credential has an acknowledged active session.",
	"GET /v1/m/recording/sessions":                                   "Lists recording sessions (admin). Viewing recordings is a privileged read of operator activity: it runs in a Mutate so its self-audit persists (the accessgraph.read pattern).",
	"GET /v1/m/recording/sessions/{id}":                              "Returns one session's metadata (admin; self-audited).",
	"GET /v1/m/recording/sessions/{id}/export":                       "Returns the session and its full frame list as either a JSON document (format=json, default) or a plain-text timeline summary (format=summary).",
	"GET /v1/m/recording/sessions/{id}/replay":                       "Returns the forensic replay of one session: its session header, a page of frames (chain order), and the correlated audit-ledger events of the session's window — reconstruction, not blobs.",
	"GET /v1/m/recording/sessions/{id}/unified":                      "Returns the combined forensic + operational view of one recording session in a single call: session header, frames (paginated), ledger correlation, chain-verification verdict, and the correlated operational timeline from the sessions module (via the TimelineResolver seam).",
	"GET /v1/m/recording/sessions/{id}/verify":                       "Recomputes one session's frame chain and re-checks every ledger anchor (open, periodic, seal) against the chain — proves, never trusts.",
	"GET /v1/m/redteam/catalog":                                      "Returns the battery taxonomy (metadata + OWASP/ATLAS coverage).",
	"GET /v1/m/redteam/runs":                                         "Lists the tenant's red-team runs (newest selectable by target).",
	"GET /v1/m/redteam/runs/{id}":                                    "Returns one run with its per-family breakdown recomputed from the stored result rows (the run row holds the aggregates; the breakdown is derived).",
	"GET /v1/m/redteam/runs/{id}/results":                            "Lists one run's per-probe results.",
	"GET /v1/m/redteam/targets":                                      "Lists the tenant's registered targets.",
	"GET /v1/m/redteam/targets/{id}":                                 "Returns one target.",
	"GET /v1/m/reporting/branding":                                   "Returns the tenant report branding, and answers 501 when no branding store is wired.",
	"GET /v1/m/reporting/enterprise/bundle":                          "Builds the evidence bundle, and answers 501 when the report engine is not configured.",
	"GET /v1/m/reporting/enterprise/posture":                         "Builds the posture report, and answers 501 when the report engine is not configured.",
	"GET /v1/m/reporting/enterprise/risk":                            "Builds the risk summary, and answers 501 when the report engine is not configured.",
	"GET /v1/m/reporting/reports":                                    "Lists the report types this engine can generate, each with its available formats.",
	"GET /v1/m/reporting/reports/{type}":                             "Generates one report type in the requested format and locale over the requested window; an unavailable renderer answers 501.",
	"GET /v1/m/reporting/schedules":                                  "Lists the report schedules of the tenant, and answers 501 when report scheduling is not wired.",
	"GET /v1/m/reporting/schedules/{id}/runs":                        "Lists the recorded runs of one report schedule, metadata only; the artifact downloads through the run route.",
	"GET /v1/m/reporting/schedules/{id}/runs/{rid}":                  "Returns one recorded run of a report schedule, with its artifact when the run produced one.",
	"GET /v1/m/reporting/templates/{type}":                           "Returns the tenant custom template for one report type, or 404 when none has been authored.",
	"GET /v1/m/sandbox/comparisons":                                  "Lists the tenant's pre/post-deploy comparisons.",
	"GET /v1/m/sandbox/comparisons/{id}":                             "Returns one comparison.",
	"GET /v1/m/sandbox/runs":                                         "Lists the tenant's runs (optionally filtered by kind / scenario).",
	"GET /v1/m/sandbox/runs/{id}":                                    "Returns one run.",
	"GET /v1/m/sandbox/runs/{id}/outputs":                            "Lists one run's per-step outputs.",
	"GET /v1/m/sandbox/runs/{id}/stream":                             "Streams one sandbox run as server-sent events; a missing run answers 404 before any stream opens, and the open is recorded in the audit ledger.",
	"GET /v1/m/sandbox/scenarios":                                    "Lists the tenant's scenarios (optionally filtered by status).",
	"GET /v1/m/sandbox/scenarios/{id}":                               "Returns one scenario.",
	"GET /v1/m/security/anomalies":                                   "Computes the tenant's prioritized anomalies on demand: the permitted-vs-observed drift (consumed via the store, not recomputed), egress/exfil-relevant drift labeled from the resource, and the correlated/high security findings (incl. the joined anti_evasion mark).",
	"GET /v1/m/security/cases":                                       "Lists the tenant's forensic cases.",
	"GET /v1/m/security/cases/{id}":                                  "Returns one case.",
	"GET /v1/m/security/cases/{id}/export":                           "Exports a case's relevant ledger events in a SIEM format (every format audit.Formats() lists), re-verifiable offline.",
	"GET /v1/m/security/cases/{id}/links":                            "Lists a case's chain of custody.",
	"GET /v1/m/security/cases/{id}/timeline":                         "Reconstructs a case's timeline from the append-only ledger and VERIFIES the chain and (where a checkpoint key is wired) the signed checkpoints, then enriches it with attribution (identity), least-privilege drift and data lineage.",
	"GET /v1/m/security/enforcement":                                 "Returns the tenant's inline-enforcement posture (all classes).",
	"GET /v1/m/security/findings":                                    "Lists the tenant's findings, filterable by kind/severity/ status/source.",
	"GET /v1/m/security/findings/export":                             "Exports a complete filtered snapshot. Request pagination is deliberately ignored: export walks store cursors until exhaustion or the per-run interoperability cap.",
	"GET /v1/m/security/findings/{id}":                               "Returns one finding.",
	"GET /v1/m/security/integrity/verify":                            "Verifies the audit ledger integrity and returns the result; the verification is itself recorded in the ledger.",
	"GET /v1/m/security/safety-posture":                              "Aggregates the tenant's provider safety-posture findings (the OpenAI Moderation / AWS Bedrock Guardrails / Azure RAI read-first posture) into a per-provider-surface roll-up.",
	"GET /v1/m/sessions/decisions":                                   "Lists the recorded work decisions with keyset paging, narrowed by the allowlisted filters, which combine with AND.",
	"GET /v1/m/sessions/decisions/{id}":                              "Returns one recorded work decision by id.",
	"GET /v1/m/sessions/leases":                                      "Lists the work leases with keyset paging, narrowed by the allowlisted filters, which combine with AND.",
	"GET /v1/m/sessions/live":                                        "Lists live sessions, most-recently-active first. The recency sort is a custom order (no keyset cursor); for \"more\", raise the limit.",
	"GET /v1/m/sessions/live/{ref}":                                  "Returns the full live operation of one session by its reference.",
	"GET /v1/m/sessions/live/{ref}/timeline":                         "Returns a session's reconstructable timeline in chronological (ingestion) order, keyset-paginated by the time-ordered row id.",
	"GET /v1/m/sessions/protocol-binding-specs":                      "Lists protocol binding spec generations in one workspace. An empty page is returned as an empty array rather than null.",
	"GET /v1/m/sessions/protocol-binding-specs/{id}":                 "Returns one immutable protocol binding spec generation. Tenancy is already bound by the module middleware and the row's workspace by the entity middleware, so this handler resolves the id and no more.",
	"GET /v1/m/sessions/protocol-bindings":                           "Lists the protocol bindings visible to the caller, filtered and paginated by the query.",
	"GET /v1/m/sessions/protocol-bindings/{id}":                      "Returns one protocol binding in the caller's tenant. The response carries an ETag derived from the binding's version, so a later conditional write can be rejected instead of silently overwriting a row that moved underneath the reader.",
	"GET /v1/m/sessions/runs":                                        "Lists the session runs newest first, optionally narrowed by state or by the provider session a run drives.",
	"GET /v1/m/sessions/runs/{ref}":                                  "Returns one session run by reference.",
	"GET /v1/m/sessions/runs/{ref}/attach":                           "Streams a live session's bridged I/O as server-sent events, replaying from an optional ?from=<seq> cursor.",
	"GET /v1/m/sessions/runs/{ref}/events":                           "Lists the events of one session run in sequence order.",
	"GET /v1/m/sessions/stream":                                      "Serves the live operation as server-sent events. It clears the server's hardened write timeout for the duration of the stream (via the ResponseController unwrap the engine exposes), flushes each frame, and heartbeats.",
	"GET /v1/m/sessions/templates":                                   "Lists the session templates, optionally narrowed to built-in ones and optionally including archived ones.",
	"GET /v1/m/sessions/templates/{id}":                              "Returns one session template with its full detail.",
	"GET /v1/m/sessions/work-items":                                  "Lists the work items with keyset paging, narrowed by the allowlisted filters, which combine with AND; any other query parameter is refused.",
	"GET /v1/m/sessions/work-items/{id}":                             "Returns one work item with its data, and its version as a strong ETag for a later conditional write.",
	"GET /v1/m/sessions/work-items/{id}/acceptance":                  "Lists the acceptance criteria of one work item with keyset paging.",
	"GET /v1/m/sessions/work-items/{id}/dependencies":                "Lists the dependency edges of one work item with keyset paging.",
	"GET /v1/m/sessions/work-items/{id}/events":                      "Lists the recorded events of one work item with keyset paging.",
	"GET /v1/m/sessions/work-items/{id}/lease":                       "Returns the lease of one work item, with the PARENT work item version as the ETag a lease mutation must send back.",
	"GET /v1/m/sessions/work-stream":                                 "Streams work events as server-sent events, resuming strictly after the cursor query parameter or the Last-Event-ID header; the two must agree when both are sent.",
	"GET /v1/m/sessions/workspaces":                                  "Lists the session workspaces of the tenant.",
	"GET /v1/m/sessions/workspaces/{ref}":                            "Returns one session workspace by reference.",
	"GET /v1/m/sessions/workspaces/{ref}/files":                      "Lists the files of a workspace under an optional path prefix, with paging.",
	"GET /v1/m/sessions/workspaces/{ref}/files/raw":                  "Reads the content of one workspace file named by the path query parameter.",
	"GET /v1/m/sessions/workspaces/{ref}/files/stat":                 "Returns the metadata of one workspace entry named by the path query parameter, without its content.",
	"GET /v1/m/sourcescope/assignments":                              "Lists connector→workspace assignments, optionally filtered by ?connector_name / ?workspace_ref.",
	"GET /v1/m/sourcescope/assignments/{id}":                         "Returns one assignment.",
	"GET /v1/m/sourcescope/bindings":                                 "Lists the tenant's source→scope bindings, optionally filtered by ?source_type / ?source_ref / ?scope_tree.",
	"GET /v1/m/sourcescope/bindings/{id}":                            "Returns one binding.",
	"GET /v1/m/sourcescope/guard-postures":                           "Lists active guard-posture overrides. With no row, a KB remains ACL-aware by default, so only explicit overrides are listed.",
	"GET /v1/m/sourcescope/posture-requests":                         "Lists posture-change requests, optionally filtered by ?status / ?source_type / ?source_ref (default surfaces the pending queue for reviewers).",
	"GET /v1/m/sourcescope/posture-requests/{id}":                    "Returns one posture-change request.",
	"GET /v1/m/sourcescope/resolve":                                  "Answers \"what would the runtime resolver decide for this actor and source\" — the console's binding-effect preview.",
	"GET /v1/m/sourcescope/resources":                                "Lists the discovered resources, optionally filtered by kind, workspace, parent or subtree; a malformed id is refused before any store read.",
	"GET /v1/m/sourcescope/workspace-connectors":                     "Lists workspace-scoped connectors, optionally filtered by ?workspace_ref / ?kind.",
	"GET /v1/m/sourcescope/workspace-connectors/{id}":                "Returns one workspace connector.",
	"GET /v1/m/voice/decisions":                                      "Lists the whole append-only open/close ledger for the tenant, newest first: the page limit keeps the most recent decisions, not the oldest ones.",
	"GET /v1/m/voice/policies":                                       "Lists the voice-open policies.",
	"GET /v1/m/voice/sessions":                                       "Lists voice sessions with read-time-derived state, metadata only.",
	"GET /v1/m/voice/sessions/{ref}":                                 "Returns one session's metadata by its reference.",
	"GET /v1/m/voice/sessions/{ref}/decisions":                       "Lists the append-only open/close ledger for one session.",
	"GET /v1/m/voice/sessions/{ref}/stream":                          "Serves one session's live metadata as server-sent events, pinned to the request's single authorized tenant.",
	"GET /v1/members":                                                "Lists the members of the resolved tenant with the effective role, workspace scoping and directory groups of each.",
	"GET /v1/search":                                                 "Fans one query out across every searchable kind and returns the matches. Each kind is gated on its own read permission, so the result only narrows what the caller could already list.",
	"GET /v1/server-info":                                            "Returns the non-sensitive server facts: version, engine, whether first-run setup is still required, and the informational license status.",
	"GET /v1/system/orgs":                                            "Lists the tenant orgs of the deployment. Superadmin only.",
	"GET /v1/system/residency":                                       "Returns the residency registry this instance is configured with. An unconfigured instance answers with an empty region list, not a null one.",
	"GET /v1/tokens":                                                 "Lists the API tokens visible to the caller: every active token for a superadmin, the tokens bound to their tenant for a tenant admin. Revoked tokens are excluded unless include_revoked is set.",
	"GET /v1/users":                                                  "Lists the user accounts of the deployment. Superadmin only.",
	"GET /v1/users/superadmins":                                      "Lists the superadmin accounts with the active or inactive status of each.",
	"GET /v1/workspaces":                                             "Lists the workspaces of the resolved tenant.",
	"GET /v1/workspaces/{id}":                                        "Returns one workspace of the resolved tenant by id.",
	"PATCH /v1/agents/{id}":                                          "Updates the mutable fields of one agent from the posted document.",
	"PATCH /v1/m/orchestration/schedules/{id}":                       "Enables/disables/retargets a schedule (write-tier, self-audited).",
	"PATCH /v1/m/orchestration/workflows/{id}":                       "Updates description/enabled (write-tier, revisioned). Disabling stops NEW runs; a running run finishes (documented behavior).",
	"PATCH /v1/m/security/cases/{id}":                                "Updates a case's lifecycle (status/severity/summary), self-audited.",
	"PATCH /v1/m/security/findings/{id}":                             "Updates a finding's triage state (open/triaged/resolved/ dismissed).",
	"PATCH /v1/m/sessions/work-items/{id}":                           "Updates one work item as a validate, plan or apply command.",
	"PATCH /v1/m/sessions/work-items/{id}/acceptance/{criterion_id}": "Updates or evaluates one acceptance criterion of a work item as a validate, plan or apply command.",
	"PATCH /v1/workspaces/{id}":                                      "Renames a workspace, edits its settings or archives it. The slug is immutable, and the default workspace cannot be archived because it is the resolution target for an unset workspace.",
	"POST /v1/agents":                                                "Creates one agent in the resolved tenant from the posted document.",
	"POST /v1/auth/login":                                            "Exchanges an email and password for a session token.",
	"POST /v1/auth/logout":                                           "Revokes the calling session. It applies to session principals only, never to an API token.",
	"POST /v1/auth/refresh":                                          "Rotates the calling session credential and extends its expiry, returning a fresh token; the old one stops working. It applies to session principals only, and an API token is reissued rather than refreshed.",
	"POST /v1/console/connectors/test":                               "Tests connectivity for a candidate connector configuration without persisting it.",
	"POST /v1/console/license":                                       "Installs a commercial license from the posted document.",
	"POST /v1/console/sso/test":                                      "Validates a candidate SSO configuration by running discovery or fetching metadata, without persisting it. It answers 501 when the provider builder is not configured.",
	"POST /v1/console/support-bundle":                                "Builds and returns a redacted support archive from in-process data only; it invokes no host tooling and makes no loopback request.",
	"POST /v1/console/update-check":                                  "Runs a check now against the configured signed update channel. An air-gapped or unconfigured deployment answers 501 rather than reporting no update.",
	"POST /v1/m/capabilities/configs":                                "Registers a new managed config for an MCP server, recording the first revision snapshot and a self-audit attributed to the real principal.",
	"POST /v1/m/capabilities/toolpins/approve":                       "Approves one tool pin; the call requires an Idempotency-Key header and the version the caller expects, and answers 501 when no pin verifier is wired.",
	"POST /v1/m/capabilities/toolpins/unpin":                         "Removes one tool pin; the call requires an Idempotency-Key header and the version the caller expects, and answers 501 when no pin verifier is wired.",
	"POST /v1/m/catalog/entries":                                     "Creates a draft catalog entry.",
	"POST /v1/m/catalog/entries/{id}/admit":                          "Runs the admission decision for one catalog entry, dispatching on the entry kind; an entry of any other kind is refused.",
	"POST /v1/m/catalog/entries/{id}/approve":                        "Approves a draft/pending entry: it pins the content hash, signs it when a catalog signing key is configured, records the approver and freezes the entry.",
	"POST /v1/m/catalog/entries/{id}/deprecate":                      "Retires an approved entry. Its content hash and signature are preserved, so it remains verifiable — it is retired, not erased.",
	"POST /v1/m/catalog/entries/{id}/instantiate":                    "Creates a self-service instantiation request FROM an approved catalog entry.",
	"POST /v1/m/catalog/entries/{id}/submit":                         "Moves a draft entry to pending review.",
	"POST /v1/m/catalog/instances/{id}/transition":                   "Records a governance decision on an instance (approve/ reject/activate).",
	"POST /v1/m/claude-agents/sessions/{id}/tool-confirmation":       "Emits user.tool_confirmation, bound to the approval state machine and AUDITED with a redacted fingerprint.",
	"POST /v1/m/claude-policy/{surface}/checkin":                     "Records a distribution agent's attested check-in: it verifies the echoed artifact hash/fingerprint against the distribution record (a mismatch is a HIGH drift finding — tampered or stale artifact), upserts the scope's OBSERVED config (credential-redacted), computes drift against the latest published revision, and audits the whole thing.",
	"POST /v1/m/claude-policy/{surface}/dry-run":                     "Previews the resolved precedence/effect WITHOUT publishing or touching any host.",
	"POST /v1/m/claude-policy/{surface}/publish":                     "Persists an immutable revision and runs PERMITTED-vs-OBSERVED drift.",
	"POST /v1/m/claude-policy/{surface}/validate":                    "Validates a document server-side (defense in depth — the UI is never the security boundary).",
	"POST /v1/m/compliance/aims/pack":                                "Structures operator-supplied organizational context + the live ISO 42001 assessment into a certification-readiness pack (deny-closed: 501 without a configured packager, 422 on a document the packager rejects), persists the pack (one per tenant, replace-on-regenerate) anchored to the ledger head, and self-audits.",
	"POST /v1/m/compliance/claude-files/{id}/erase":                  "Serves the governed point delete (admin-tier; the dual-control gate + hold gate + receipt are inside eraseClaudeFile).",
	"POST /v1/m/compliance/data-subjects/{id}/erase":                 "Opens an erasure request for one data subject, optionally narrowed by subject kind and tied to a case reference.",
	"POST /v1/m/compliance/depth/ccm/drift":                          "Compares two CCM snapshots (the two latest, or a specified snapshot_id and its predecessor) and persists per-control drift findings (deny-closed: 501 without the depth packager).",
	"POST /v1/m/compliance/depth/ccm/snapshot":                       "Re-evaluates the assessment engine for a set of frameworks (or all catalog frameworks when none specified) at the current instant and persists the timestamped snapshot (deny-closed: 501 without the depth packager).",
	"POST /v1/m/compliance/depth/fedramp":                            "Structures the live assessment into FedRAMP 20x Key Security Indicators in OSCAL v1.1.3 format with DoD impact-level framing (deny-closed: 501 without the depth packager, 422 on reject).",
	"POST /v1/m/compliance/depth/sector":                             "Structures operator-supplied sector context + the live assessment of sector-overlay frameworks into a compliance pack (deny-closed: 501 without the depth packager, 422 on reject), persists it (one per tenant, replace-on-regenerate) anchored to the ledger head, and self-audits.",
	"POST /v1/m/compliance/depth/us-law":                             "Structures operator-supplied jurisdiction context + the live assessment of the 4 US state AI law frameworks into a compliance pack (deny-closed: 501 without a configured depth packager, 422 on input the packager rejects), persists it (one per tenant, replace-on-regenerate) anchored to the ledger head, and self-audits.",
	"POST /v1/m/compliance/dora/incidents":                           "Applies the DORA major-incident criteria to operator-supplied impact data and persists the structured classification + report draft (one per tenant+reference, replace-on-reclassify so the report evolves initial → intermediate → final).",
	"POST /v1/m/compliance/dora/register":                            "Ingests operator-supplied register data, structures it into the 2024/2956 Register of Information (deny-closed: 501 without a configured packager, 422 on a document the packager rejects), persists the maintained register (one per tenant+entity, replace-on-regenerate) anchored to the ledger head, and self-audits.",
	"POST /v1/m/compliance/erasure":                                  "Registers a DSR: it mints (or reuses) the subject's key-ring row, tokenizes the subject reference, derives the affected §2 classes and seals the \"received\" custody event + self-audit in one transaction.",
	"POST /v1/m/compliance/erasure/{id}/execute":                     "Runs the governed erasure for one request. Gate order is the contract §6.1: the legal-hold gate FIRST (Held=true OR error ⇒ no erasure), THEN the CRITICAL dual-control approval — two independent deny-closed gates, neither subsumes the other.",
	"POST /v1/m/compliance/frameworks/{id}/evidence":                 "Seals an immutable evidence package for a framework: it assesses the framework against the tenant's live evidence, records the ledger head + the live hash-chain verify result (the integrity proof), persists the package and its per-control results (append-only), and self-audits the seal — all in one transaction.",
	"POST /v1/m/compliance/holds":                                    "Sets an ACTIVE hold immediately — no approval gate, because preservation is the safe direction and the duty to preserve admits no waiting (§4).",
	"POST /v1/m/compliance/holds/{id}/release":                       "Releases one legal hold under dual control; the approval gate runs before any store write.",
	"POST /v1/m/compliance/nis2/incidents/classify":                  "Classifies an operator incident against the NIS 2 significance thresholds; the operator reference is required and is refused rather than truncated when it is too long.",
	"POST /v1/m/compliance/oscal/profiles":                           "Ingests an operator-supplied OSCAL profile/catalog/SSP and registers the resolved control selection for a framework (one active selection per tenant+framework; re-registering replaces it).",
	"POST /v1/m/compliance/residency":                                "Records (or updates) a per-region residency attestation.",
	"POST /v1/m/compliance/residency/scan":                           "Scans EXISTING egress signals against the self-hosted attestations and, where data left a self-hosted perimeter, records a violation, emits a core Finding + a bus signal, and updates the attestation.",
	"POST /v1/m/compliance/retention/sweep":                          "Runs the tenant's sweep NOW (the same code the engine loop runs), attributed to the calling admin, and returns the summary.",
	"POST /v1/m/compliance/risk/classify":                            "Computes (or recomputes) a suggested risk tier for a subject from observed signals and persists it as a governed, audited classification.",
	"POST /v1/m/compliance/risk/{id}/review":                         "Records a review of one risk entry, setting its tier to one of unacceptable, high, limited or minimal.",
	"POST /v1/m/consoleviews/views":                                  "Saves a new view owned by the caller. Caps are enforced in the same transaction as the insert; the (feature, owner, name) natural key refuses duplicates with a clear 409 (DB unique index as backstop).",
	"POST /v1/m/deploy/definitions":                                  "Declares a new deployment definition and its first revision.",
	"POST /v1/m/deploy/definitions/{id}/apply":                       "Reconciles the deployment to its desired spec — a GOVERNED INFRASTRUCTURE MUTATION.",
	"POST /v1/m/deploy/definitions/{id}/plan":                        "Plans one deployment definition through its executor and records the resulting plan, without applying it.",
	"POST /v1/m/deploy/definitions/{id}/retire":                      "Removes the deployment from the target — a GOVERNED MUTATION with the same two-phase, deny-by-default gating as apply.",
	"POST /v1/m/deploy/definitions/{id}/rollback":                    "Rolls the DESIRED state back to a prior revision by declaring a NEW revision whose spec equals that known-good version (forward-only, reversible history).",
	"POST /v1/m/deploy/definitions/{id}/verify":                      "Reconciles the real state against the desired spec and updates the Deployment snapshot's status (drift is reported, not mutated).",
	"POST /v1/m/evals/ab":                                            "Loads the suite, scores both variants OUTSIDE the write transaction (two-phase — judge I/O never holds the store), optionally runs the order-swapped pairwise comparison, then persists both runs atomically.",
	"POST /v1/m/evals/baselines":                                     "Find-or-creates the (suite, subject) baseline and points it at run_ref, recording the previous run_ref in the self-audit.",
	"POST /v1/m/evals/calibration/items":                             "Creates or RE-labels reference items (upsert by set+case_key: a correction is an audited update, the history lives in the ledger).",
	"POST /v1/m/evals/calibration/run":                               "Measures the wired judge against a labeled set. The judge calls run OUTSIDE any transaction (two-phase); the report + core EvalResult (+ a Finding when below target) persist atomically.",
	"POST /v1/m/evals/gate":                                          "Evaluates the gate. Three-phase like every judged path: read (suite, cases, calibration, cache), score outside any transaction, persist atomically.",
	"POST /v1/m/evals/gate/{id}/override":                            "Overrides one eval gate verdict; a written reason is required.",
	"POST /v1/m/evals/monitor":                                       "Samples sessions, scores their signals, persists one core EvalResult per sample and returns the aggregate.",
	"POST /v1/m/evals/runs":                                          "Scores a set of candidate outputs against a suite SYNCHRONOUSLY (the redteam pattern — no module goroutines) and returns 201 with the completed run + per-case.",
	"POST /v1/m/evals/suites":                                        "Creates a versioned golden suite. Write-tier + self-audited.",
	"POST /v1/m/evals/suites/{id}/archive":                           "Marks a suite archived. Admin-tier + self-audited.",
	"POST /v1/m/evals/suites/{id}/cases":                             "Appends a golden case to a suite (append-only — a fix is a new suite_version).",
	"POST /v1/m/eventing/deliveries/{id}/redeliver":                  "Requeues ONE delivery that already reached a terminal state (delivered, dead or denied — the DLQ drain primitive).",
	"POST /v1/m/eventing/egress-policy/check":                        "Answers \"would this destination be permitted\", without creating anything.",
	"POST /v1/m/eventing/subscriptions":                              "Creates a subscription. The signing secret is generated server-side (never caller-supplied: entropy is guaranteed), sealed for storage, and returned EXACTLY ONCE in the response.",
	"POST /v1/m/eventing/subscriptions/{id}/replay":                  "Re-enqueues deliveries for the subscription from a seq cursor: every retained event with seq >= from_seq (and <= to_seq, if set) that matches the subscription's type list and source filter gets a NEW queued delivery row (origin=replay).",
	"POST /v1/m/eventing/subscriptions/{id}/restore":                 "Re-applies the delivery shape of an earlier revision to an EXISTING subscription.",
	"POST /v1/m/eventing/subscriptions/{id}/rotate-auth":             "Replaces the subscription's auth credential, returning the old hint (the new cleartext is never returned — it is supplied by the caller).",
	"POST /v1/m/eventing/subscriptions/{id}/rotate-secret":           "Replaces the subscription's signing secret, returning the new cleartext exactly once.",
	"POST /v1/m/eventing/subscriptions/{id}/test":                    "Sends ONE synthetic \"eventing.test\" delivery to the subscription's endpoint, synchronously, signed with its real secret — the integrator's \"is my receiver wired correctly?\" probe.",
	"POST /v1/m/finops/budgets":                                      "Creates a budget from the posted document.",
	"POST /v1/m/finops/cost":                                         "Ingests one CostSample over HTTP (the Anthropic cost_report or a live runtime-cost sample) through the SAME canonical onCost path the bus uses — so it shares the natural-key dedup, the attribution resolution and the single-provenance CostRecord ledger; it never opens a second divergent path.",
	"POST /v1/m/finops/cost-centers":                                 "Creates a cost center from the posted document, defaulting its status to active.",
	"POST /v1/m/finops/cost-centers/{id}/mappings":                   "Creates one mapping that attributes spend to a cost center.",
	"POST /v1/m/finops/model-rates":                                  "Creates a new rate catalog entry after validating input and checking uniqueness of the (provider, model, effective_from) tuple.",
	"POST /v1/m/finops/outcomes":                                     "Ingests one graded outcome over HTTP (the operator/automation bridge), auditing the principal's privileged write atomically with its effect.",
	"POST /v1/m/finops/seats":                                        "Upserts one provider/day seat snapshot (202 Accepted: a re-posted day replaces its values — a snapshot, never additive).",
	"POST /v1/m/finops/statements/generate":                          "Generates the chargeback statements for one monthly or weekly period starting at the posted RFC3339 instant.",
	"POST /v1/m/governance/agent-risk-profiles/classify":             "Computes (or recomputes) the suggested risk tier for an agent from observed signals.",
	"POST /v1/m/governance/agent-risk-profiles/{id}/review":          "Marks a classification as human-reviewed. The reviewer accepts the current effective tier as correct.",
	"POST /v1/m/governance/agentcore-export/apply":                   "Applies a previously planned AgentCore export; the plan hash is required and must still reproduce.",
	"POST /v1/m/governance/agentcore-export/plan":                    "Plans an AgentCore export for the requested enforcement mode, without applying it.",
	"POST /v1/m/governance/agents":                                   "Creates a lifecycle row with kind=agent and mandatory sponsor.",
	"POST /v1/m/governance/agents/{agentID}/identity":                "Binds an agent to its NHI identity (Agent.IdentityID). Admin-tier and self-audited.",
	"POST /v1/m/governance/approvals":                                "Opens an approval request. Write-tier (any editor may request); self-audited.",
	"POST /v1/m/governance/approvals/sweep":                          "Materializes expiry and escalation for the tenant's pending approvals.",
	"POST /v1/m/governance/approvals/{id}/cancel":                    "Cancels a pending request. Write-tier; allowed only to the original requester (stable UserID) or a tenant admin/owner.",
	"POST /v1/m/governance/approvals/{id}/consume":                   "Consumes one single-use approval on behalf of a named consumer; the consumer id is required and rides the immutable audit trail.",
	"POST /v1/m/governance/approvals/{id}/decisions":                 "Records one human decision. Admin-tier and self-audited. It is race-safe on Postgres: every decision re-counts within its transaction and commits under a version-checked update of the approval row, so a concurrent threshold crossing resolves to exactly one winner (the loser retries).",
	"POST /v1/m/governance/breakglass":                               "Opens an emergency window. Admin-tier; a REAL human only (a system token has no stable identity for the review SoD to key on); justification required; time-boxed; blocked while any prior grant is unreviewed (the forced-post-review backpressure: you cannot stack emergencies over an unexamined one).",
	"POST /v1/m/governance/breakglass/consume":                       "Consumes one break-glass grant for a named action and subject; both ride the immutable use trail.",
	"POST /v1/m/governance/breakglass/{id}/review":                   "Records the FORCED post-review of a terminal grant. Admin-tier; a real human DIFFERENT from the activator (separation of duties — the person who opened the emergency cannot also be the one who signs it off); only once; only after the window closed (the review examines what the window was used for, so it cannot precede its closure).",
	"POST /v1/m/governance/breakglass/{id}/revoke":                   "Closes an active grant early. Admin-tier, self-audited, emitted.",
	"POST /v1/m/governance/guardian/rules":                           "Authors a containment rule. Admin-tier (authoring auto-containment is enforcement-posture authoring); self-audited.",
	"POST /v1/m/governance/killswitch":                               "Engages a stop across the estate or over one named agent scope.",
	"POST /v1/m/governance/killswitch/{id}/reenable":                 "Lifts a stop — NEVER unilaterally. The first call opens the dual-control approval (CRITICAL: 2 distinct humans, anti-self- approval, AAL3 per decision — all enforced by the engine); subsequent calls report 202 while pending, and the call that finds it approved re-verifies the two-human floor STRUCTURALLY in the same transaction and flips the stop.",
	"POST /v1/m/governance/killswitch/{id}/review":                   "Records the FORCED post-review of a re-enabled stop: a real human DIFFERENT from the engager, the re-enable requester and the re-enabler (separation of duties — nobody signs off their own incident), once, note mandatory.",
	"POST /v1/m/governance/nhi/sweep":                                "Materializes staleness/escalation, orphan and unsponsored state for the tenant's NHI lifecycle rows, and ensures a row exists for every roster NHI.",
	"POST /v1/m/governance/nhi/{ref}/offboard":                       "Runs the reversible soft-delete step of a governed offboarding: it blocks the NHI in-product (enforcement=blocked, the cascade that denies every bound agent at the PEP), opens an audited recovery window, and best-effort disables the credential at the source.",
	"POST /v1/m/governance/nhi/{ref}/offboard/finalize":              "Finalizes the offboarding of one non-human identity.",
	"POST /v1/m/governance/nhi/{ref}/restore":                        "Reverses a soft-delete within the recovery window: best-effort re-enable at the source, clear the in-product block.",
	"POST /v1/m/governance/nhi/{ref}/rotate":                         "Orchestrates a governed rotation: it opens the CRITICAL approval (two-person floor via the engine), and only on an approved/break-glass decision invokes the wired actuator, returning the minted credential ONCE.",
	"POST /v1/m/governance/pdp/dry-run":                              "Evaluates an authorization request against an unpublished candidate policy; the route is separate from explain so a console can label the intent.",
	"POST /v1/m/governance/pdp/explain":                              "Evaluates an example request against a CANDIDATE source and returns the three-valued decision chain: a matched permit GRANTS within scope, a matched forbid RESTRICTS, neither abstains (the RBAC decision stands).",
	"POST /v1/m/governance/pdp/publish":                              "Persists a versioned Cedar/OPA policy and, for Cedar, ACTIVATES it on the live hot path (recomposes the per-tenant overlay).",
	"POST /v1/m/governance/pdp/rollback":                             "Re-activates an existing immutable revision by appending an activation record.",
	"POST /v1/m/governance/pdp/validate":                             "Compiles a source WITHOUT loading it into the live evaluator.",
	"POST /v1/m/governance/policies":                                 "Authors a governance policy. Admin-tier and self-audited; the ABAC cache is invalidated AFTER the write commits.",
	"POST /v1/m/governance/rbac/grants":                              "Creates one scoped RBAC grant from the posted document.",
	"POST /v1/m/governance/rbac/permission-groups":                   "Creates one permission group; the caller must be a tenant admin and every permission is validated.",
	"POST /v1/m/governance/rbac/roles":                               "Creates one custom role; the caller must be a tenant admin, and a built-in role name is refused.",
	"POST /v1/m/governance/roster/sync":                              "Triggers a roster reconciliation for the resolved tenant from the identity providers the composition root wired (UseRosterProviders).",
	"POST /v1/m/governance/routine-policies":                         "Creates a routine policy.",
	"POST /v1/m/health/checks":                                       "Declares a monitored subject. Creating a check is a privileged write, self-audited in the same transaction.",
	"POST /v1/m/health/checks/{id}/report":                           "Ingests an active probe result for a check and folds it into the subject's health.",
	"POST /v1/m/health/incidents/{id}/resolve":                       "Resolves one open incident manually.",
	"POST /v1/m/inferenceproxy/device/approve":                       "Approves or denies one device authorization by its user code; an expired or consumed code answers 410.",
	"POST /v1/m/knowledge/context-policies":                          "Upserts a context/compaction policy by (scope_kind, scope_ref).",
	"POST /v1/m/knowledge/data-products":                             "Creates one data product from the posted document; the name must be free.",
	"POST /v1/m/knowledge/data-products/{id}/archive":                "Moves one data product to the archived state.",
	"POST /v1/m/knowledge/data-products/{id}/contracts":              "Creates one data contract version for a data product.",
	"POST /v1/m/knowledge/data-products/{id}/deprecate":              "Moves one data product to the deprecated state.",
	"POST /v1/m/knowledge/data-products/{id}/publish":                "Moves one data product to the published state.",
	"POST /v1/m/knowledge/data-products/{id}/validate":               "Validates a posted payload or metadata document against the active contract of one data product.",
	"POST /v1/m/knowledge/kbs":                                       "Declares a knowledge base. It records the embed_model/dim of the CURRENTLY wired embedder and enforces the embed-policy↔egress gate.",
	"POST /v1/m/knowledge/kbs/{id}/ingest":                           "Ingests content into a knowledge base: it gathers documents (from a registered source or inline), REDACTS each body, chunks it, embeds the chunks via the wired embedder OUTSIDE the store transaction (the network/ egress call must never hold the single SQLite writer — the deploy discipline), and persists documents + chunks in one transaction.",
	"POST /v1/m/knowledge/kbs/{id}/query":                            "Runs one retrieval query against a knowledge base and returns the ranked passages.",
	"POST /v1/m/knowledge/kbs/{id}/reindex":                          "Re-embeds a KB's chunks that have no embedding yet (the recovery path for a partial ingest where the embed call failed).",
	"POST /v1/m/knowledge/kbs/{id}/scan":                             "Runs the at-rest discovery scan over one KB's stored documents: classify every chunk's (redacted) text, label each document, count redaction markers, append the scan evidence row.",
	"POST /v1/m/knowledge/kbs/{id}/sync":                             "Handles POST /kbs/{id}/sync. Flow: 1. Validate source name and KB existence. 2.",
	"POST /v1/m/knowledge/memory":                                    "Upserts an agent memory entry (by agent_ref + namespace + key), redacting the content and enforcing the per-agent quota.",
	"POST /v1/m/knowledge/memory/import":                             "Ingests a signed memory-portability bundle. It verifies the manifest signature and the entries digest FIRST (fail closed: a bad signature, an unwired verify key, or a digest mismatch rejects the WHOLE import before any write), then routes each row through the governed write path.",
	"POST /v1/m/knowledge/memory/purge":                              "Materializes expiry: it deletes expired memory entries of BOTH scopes (optionally for one agent).",
	"POST /v1/m/knowledge/memory/verify":                             "Verifies the integrity of the stored agent memory, optionally narrowed by agent_ref; the verification is recorded in the audit ledger.",
	"POST /v1/m/knowledge/prompts":                                   "Creates a prompt and its first immutable revision.",
	"POST /v1/m/knowledge/prompts/{id}/revisions":                    "Appends an immutable revision and advances the current pointer.",
	"POST /v1/m/knowledge/prompts/{id}/rollback":                     "Points the prompt's current revision at a prior, existing revision.",
	"POST /v1/m/knowledge/sources/{name}/scan":                       "Runs discovery ACROSS a configured document store WITHOUT ingesting: it pulls the source's documents (bounded like ingest), classifies the RAW bodies in memory and persists labels + the scan evidence — the bodies themselves are never stored, logged or embedded (zero egress, minimal data).",
	"POST /v1/m/models/agent-artifacts":                              "Registers one agent artifact from the posted document.",
	"POST /v1/m/models/agent-artifacts/aibom":                        "Generates the agent-supply-chain BOM, anchors the audit-chain head, persists an append-only models.agent_aibom seal and self-audits it — the same pattern as handleSealAIBOM on the artifact inventory, so it is tamper-evident evidence (and a SEPARATE evidence class from the model-lineage seals compliance counts).",
	"POST /v1/m/models/datasets":                                     "Registers one dataset; an owned_ref, when set, must resolve.",
	"POST /v1/m/models/finetune-jobs":                                "Creates one fine-tune job; a result version, when set, must already exist.",
	"POST /v1/m/models/inference-deployments":                        "Creates one inference deployment; its model and version references must resolve, and the admission policy can refuse it.",
	"POST /v1/m/models/keys":                                         "Registers a new key/workspace governance reference.",
	"POST /v1/m/models/model-access":                                 "Creates one model-access rule; its subject and target references are validated.",
	"POST /v1/m/models/model-groups":                                 "Creates one model group from the posted document.",
	"POST /v1/m/models/model-versions":                               "Creates one model version; the owned model it belongs to must exist in the same tenant.",
	"POST /v1/m/models/model-versions/{id}/admit":                    "Verifies an OMS v1.0 signature for a model_version and records the verdict (upsert, claim-vs-verified).",
	"POST /v1/m/models/owned-models":                                 "Creates one owned model from the posted document.",
	"POST /v1/m/models/owned-models/{id}/aibom":                      "Generates the AIBOM, computes its canonical content hash, anchors the current audit-chain head, persists an append-only models.aibom seal record and self-audits the seal as the next ledger event — making the AIBOM tamper-evident compliance evidence (the same pattern as compliance's evidence package).",
	"POST /v1/m/models/routing-policies":                             "Creates a routing policy.",
	"POST /v1/m/models/routing-policies/{id}/execute":                "Resolves a stored routing policy AND executes the resolved target chain through the governed Executor, emitting the result's CostSample.",
	"POST /v1/m/models/routing-policies/{id}/resolve":                "Resolves a stored routing policy against the governed estate and returns the routing decision.",
	"POST /v1/m/notify/outbox/{id}/redeliver":                        "Requeues a TERMINAL outbox row (dead-letter drain / ack-and- retry).",
	"POST /v1/m/notify/routes":                                       "Declares a routing rule. Creating a route is a privileged, self-audited write.",
	"POST /v1/m/notify/routes/evaluate":                              "Answers \"which routes would this signal select?\" without delivering anything.",
	"POST /v1/m/notify/routes/{id}/restore":                          "Re-applies the configuration of an earlier revision to an EXISTING route (never the name — the natural key, exactly like update).",
	"POST /v1/m/notify/routes/{id}/test":                             "Sends a synthetic test notification through a route's destination and records the attempt in the ledger.",
	"POST /v1/m/orchestration/schedules":                             "Declares a governed schedule (write-tier, self-audited). The declaring principal is captured as owner_actor — the accountable principal for any later autonomous fire.",
	"POST /v1/m/orchestration/schedules/{id}/fire":                   "Fires one schedule out of band; an empty body requests approval first, and a body carries the approved run.",
	"POST /v1/m/orchestration/schedules/{id}/restore":                "Re-applies the mutable shape of an earlier revision via the patch verb's exact application path (same validation, same cadence-miss clearing, same plan_hash consequence).",
	"POST /v1/m/orchestration/workflows":                             "Declares a workflow (write-tier, self-audited, revisioned). The declaring principal is the accountable owner_actor.",
	"POST /v1/m/orchestration/workflows/{id}/dry-run":                "Computes the execution plan for a workflow as declared, with ZERO effects (read-tier).",
	"POST /v1/m/orchestration/workflows/{id}/restore":                "Re-applies the MUTABLE shape of an earlier revision — description, enabled, steps — through the exact same validation as the live verbs (the restore rule).",
	"POST /v1/m/orchestration/workflows/{id}/run":                    "Starts one run of a workflow, optionally with the posted inputs.",
	"POST /v1/m/recording/ack":                                       "Records the operator's explicit AC-8 acknowledgement: it opens (or finds) the caller's active recording session and stamps consent.",
	"POST /v1/m/recording/sessions/{id}/seal":                        "Closes one ACTIVE session explicitly (admin).",
	"POST /v1/m/recording/sessions/{id}/summarize":                   "Produces the optional Claude-backed reviewer summary of a session (admin).",
	"POST /v1/m/recording/sweep":                                     "Seals every idle ACTIVE session (admin; the lazy-seal safety net for credentials that never came back).",
	"POST /v1/m/redteam/runs":                                        "Runs a battery against an AUTHORIZED target and records the scorecard.",
	"POST /v1/m/redteam/targets":                                     "Registers a client-governed agent as a candidate target. It starts UNAUTHORIZED — registration is not consent; a separate authorize step is the explicit grant.",
	"POST /v1/m/redteam/targets/{id}/authorize":                      "Grants or revokes CONSENT to red-team a target. This is the dual-use boundary: only an authorized target may be run against.",
	"POST /v1/m/reporting/schedules":                                 "Creates one report schedule, and answers 501 when report scheduling is not wired.",
	"POST /v1/m/sandbox/compare":                                     "Runs the SAME scenario/steps against two variants, scores both (if a Scorer is wired) or compares their deterministic outputs_hash, derives a verdict + delta, and persists two runs plus a sandbox_comparison (append-only deploy-decision evidence).",
	"POST /v1/m/sandbox/replay":                                      "Reconstructs a historical session's input timeline via HistorySource and re-executes it DETERMINISTICALLY against the supplied mocks.",
	"POST /v1/m/sandbox/scenarios":                                   "Records a synthetic, operator-authored scenario fixture. All caller-supplied text is clamped before Create; the spec_hash is derived from the canonical steps+mocks so an identical fixture is recognizable.",
	"POST /v1/m/sandbox/scenarios/{id}/archive":                      "Marks a scenario archived (admin-tier + self-audited). It is idempotent: archiving an archived scenario succeeds.",
	"POST /v1/m/sandbox/scenarios/{id}/run":                          "Loads a scenario, runs its steps against the isolated runner, persists the run + per-step outputs, and (if a suite_ref is given AND a Scorer is wired) scores the outputs.",
	"POST /v1/m/security/cases":                                      "Opens an incident case, snapshotting the ledger integrity at the moment of opening (so a later tamper is detectable against the open-time state).",
	"POST /v1/m/security/cases/{id}/links":                           "Appends one immutable chain-of-custody link attaching a finding / ledger sequence / anomaly / note to a case.",
	"POST /v1/m/security/guardrails/inspect":                         "Runs the guardrails over a submitted piece of agent text. It is the inline INSPECTION seam a caller (a model gateway, an orchestrator, an agent integration) uses; the module detects, produces findings with minimal evidence, and returns a DETECTIVE verdict — or a BLOCK only when inline enforcement is enabled and governed.",
	"POST /v1/m/sessions/decisions":                                  "Records a work decision as a validate, plan or apply command.",
	"POST /v1/m/sessions/decisions/{id}/revoke":                      "Revokes one recorded work decision as a validate, plan or apply command.",
	"POST /v1/m/sessions/protocol-binding-specs":                     "Records a new draft generation of a protocol binding spec. Two properties are load-bearing and deliberate.",
	"POST /v1/m/sessions/protocol-binding-specs/{id}/activate":       "Makes one spec generation the active desired state for its binding.",
	"POST /v1/m/sessions/protocol-binding-specs/{id}/disable":        "Stops one spec generation from being the active desired state.",
	"POST /v1/m/sessions/protocol-bindings/{id}/reconcile":           "Drives one binding towards its desired state. The execution mode is REQUIRED and explicit — validate, plan, test or apply — and an unrecognized one is refused rather than defaulted, so a caller can never reach the applying path by omitting a parameter.",
	"POST /v1/m/sessions/runs":                                       "Creates one session run from the posted parameters, and answers 503 when the runtime is not available.",
	"POST /v1/m/sessions/runs/{ref}/cleanup":                         "Cleans up the resources of one finished session run.",
	"POST /v1/m/sessions/runs/{ref}/input":                           "Sends one input line or message to a live session run.",
	"POST /v1/m/sessions/runs/{ref}/resume":                          "Resumes one suspended session run.",
	"POST /v1/m/sessions/runs/{ref}/stop":                            "Stops one session run; a fenced stop carries the lease fence, and a reason without a fence is refused.",
	"POST /v1/m/sessions/templates":                                  "Creates one session template; an unknown key in the body is refused rather than dropped, so a misspelled policy key cannot silently govern nothing.",
	"POST /v1/m/sessions/templates/{id}/apply":                       "Resolves one session template into the run parameters it would produce, without starting a run.",
	"POST /v1/m/sessions/templates/{id}/duplicate":                   "Duplicates one session template under a new name, which is required.",
	"POST /v1/m/sessions/work-events/{event_id}/replay":              "Replays one dead-lettered work event as a validate, plan or apply command; a workspace-confined principal is refused because the receipt is anchored tenant-wide.",
	"POST /v1/m/sessions/work-items":                                 "Creates one work item as a validate, plan or apply command.",
	"POST /v1/m/sessions/work-items/{id}/acceptance":                 "Adds one acceptance criterion to a work item as a validate, plan or apply command.",
	"POST /v1/m/sessions/work-items/{id}/assignments":                "Assigns one work item as a validate, plan or apply command.",
	"POST /v1/m/sessions/work-items/{id}/dependencies":               "Adds one dependency edge to a work item as a validate, plan or apply command.",
	"POST /v1/m/sessions/work-items/{id}/lease/acquire":              "Acquires the lease of one work item as a validate, plan or apply command.",
	"POST /v1/m/sessions/work-items/{id}/lease/clock-rebase":         "Rebases the clock of one work item lease as a validate, plan or apply command.",
	"POST /v1/m/sessions/work-items/{id}/lease/release":              "Releases the lease of one work item as a validate, plan or apply command.",
	"POST /v1/m/sessions/work-items/{id}/lease/renew":                "Renews the lease of one work item as a validate, plan or apply command.",
	"POST /v1/m/sessions/work-items/{id}/lease/revoke":               "Revokes the lease another holder has on one work item, as a validate, plan or apply command.",
	"POST /v1/m/sessions/work-items/{id}/lease/takeover":             "Takes over the lease of one work item from its current holder, as a validate, plan or apply command.",
	"POST /v1/m/sessions/work-items/{id}/transitions":                "Transitions one work item to another status as a validate, plan or apply command.",
	"POST /v1/m/sessions/workspaces":                                 "Creates one session workspace from the posted parameters, and answers 503 when the workspace registry is not available.",
	"POST /v1/m/sessions/workspaces/{ref}/files/dir":                 "Creates one directory in a workspace at the path query parameter.",
	"POST /v1/m/sessions/workspaces/{ref}/files/move":                "Moves one workspace entry from the posted source path to the posted destination path.",
	"POST /v1/m/sourcescope/assignments":                             "Assigns a global connector to a workspace. An ENABLED assignment added to an already-assigned connector admits a workspace that could not reach it a moment earlier, so it is proposed rather than applied (202), exactly like a relaxing binding create.",
	"POST /v1/m/sourcescope/bindings":                                "Binds a connected source to a workspace/agent-group, recording a self-audit attributed to the real principal.",
	"POST /v1/m/sourcescope/posture-requests/{id}/approve":           "Approves one pending posture relaxation and applies it.",
	"POST /v1/m/sourcescope/posture-requests/{id}/reject":            "Rejects a pending relaxation (no change applied).",
	"POST /v1/m/sourcescope/sources/disable-scoping":                 "Proposes disabling ALL scoping for a source (it becomes global).",
	"POST /v1/m/sourcescope/workspace-connectors":                    "Creates a workspace-scoped connector. An inline secret value in the secrets map is sealed by the composition-root sealer under the workspace namespace; a reference value is stored verbatim.",
	"POST /v1/m/voice/sessions/open":                                 "Opens one voice session; session, agent, model and provider references are all required, and an engaged stop freezes the call.",
	"POST /v1/memberships":                                           "Grants one user a role in a tenant.",
	"POST /v1/setup":                                                 "Creates the first organization and the superadmin that owns it, using the one-time setup token. It is atomic against a concurrent setup and consumes the token on success.",
	"POST /v1/system/orgs":                                           "Provisions one tenant org. Superadmin only.",
	"POST /v1/tokens":                                                "Issues one API token. The secret is returned once, at issue time, and is never readable again.",
	"POST /v1/tokens/{id}/rotate":                                    "Issues a new token with the same specification and revokes the old one in the same step. The new secret is returned once.",
	"POST /v1/users":                                                 "Creates one user account. Superadmin only.",
	"POST /v1/users/{id}/disable":                                    "Disables one superadmin account. The action is non-destructive and reversible.",
	"POST /v1/users/{id}/enable":                                     "Enables one previously disabled superadmin account.",
	"POST /v1/workspaces":                                            "Creates one workspace. Beyond the tenant admin permission the caller must be the tenant owner or a superadmin, because a workspace admin administers within a workspace and does not mint them.",
	"PUT /v1/console/connectors":                                     "Onboards a connector instance, or replaces the configuration of an existing one.",
	"PUT /v1/console/secrets":                                        "Creates or replaces one sealed secret; the name travels in the body, so it may contain a slash.",
	"PUT /v1/console/sources":                                        "Creates or replaces one source connector and reconfigures it live.",
	"PUT /v1/console/sso":                                            "Creates or replaces the managed SSO or IdP configuration of the addressed scope; its secrets are sealed at rest.",
	"PUT /v1/m/capabilities/configs/{id}":                            "Updates a managed config in place, bumping its revision and recording an immutable revision snapshot plus a self-audit.",
	"PUT /v1/m/catalog/connector-admission/policy":                   "Upserts the per-tenant trust root. Admin, audited.",
	"PUT /v1/m/catalog/entries/{id}":                                 "Updates a DRAFT entry in place. An approved or deprecated entry is immutable (a new version is a new entry) — updating it is rejected.",
	"PUT /v1/m/catalog/mcp-admission/policy":                         "Upserts the per-tenant trust root. Admin, audited.",
	"PUT /v1/m/compliance/nis2/incidents/{id}":                       "Updates one NIS2 incident, moving it to one of the early_warning, notification, intermediate or final phases.",
	"PUT /v1/m/compliance/retention/policies/{class}":                "Upserts a per-class schedule. A retain schedule (or a purge one kept disabled) persists directly — documenting retention is evidence, not danger.",
	"PUT /v1/m/consoleviews/views/{id}":                              "Replaces the writable fields of the caller's OWN view. feature_id is immutable (a view's params only make sense on its feature).",
	"PUT /v1/m/deploy/definitions/{id}":                              "Declares a new desired revision. Write-tier; self-audited.",
	"PUT /v1/m/eventing/subscriptions/{id}":                          "Updates a subscription in place. The secret is NOT touched here (rotate-secret owns that); enabled is unchanged when absent.",
	"PUT /v1/m/finops/budgets/{id}":                                  "Replaces one budget with the posted document; the name is required.",
	"PUT /v1/m/finops/cost-centers/{id}":                             "Replaces one cost center with the posted document.",
	"PUT /v1/m/finops/model-rates/{id}":                              "Replaces a rate catalog entry by ID.",
	"PUT /v1/m/governance/agent-risk-profiles/{id}/tier":             "Sets the operator's authoritative tier for an agent. This is the \"override\" — the operator's word takes precedence over the heuristic.",
	"PUT /v1/m/governance/guardian/rules/{id}":                       "Updates a rule's mutable fields (enable/disable, matching, action, mode, note).",
	"PUT /v1/m/governance/nhi/{ref}/ownership":                       "Assigns owner/sponsor. Each reference, when non-empty, must resolve to a roster identity classified HUMAN (never an NHI/unknown — owner and sponsor are accountable people).",
	"PUT /v1/m/governance/nhi/{ref}/policy":                          "Authors the per-NHI rotation policy. Write-tier, self-audited.",
	"PUT /v1/m/governance/policies/{id}":                             "Updates a governance policy in place (kind is immutable). Admin-tier, self-audited; invalidates the ABAC cache after commit.",
	"PUT /v1/m/governance/rbac/permission-groups/{name}":             "Replaces one permission group; the caller must be a tenant admin and every permission is validated.",
	"PUT /v1/m/governance/rbac/roles/{name}":                         "Replaces one custom role; the caller must be a tenant admin, and the base role and its exclusions are validated together.",
	"PUT /v1/m/governance/routine-policies/{id}":                     "Updates a routine policy's mutable fields. Name and scope are immutable (the operator-facing identity).",
	"PUT /v1/m/health/checks/{id}":                                   "Patches a check's configuration. Privileged write, self-audited.",
	"PUT /v1/m/inferenceproxy/config":                                "Upserts the tenant's singleton config (admin-tier; self-audited).",
	"PUT /v1/m/inferenceproxy/dlp/rules":                             "Upserts one DLP rule by class (admin-tier: authorizing egress is privileged governance; self-audited).",
	"PUT /v1/m/knowledge/data-products/{id}":                         "Replaces one data product with the posted document.",
	"PUT /v1/m/knowledge/dlp/rules":                                  "Upserts one DLP rule by class (admin-tier: writing egress policy is privileged governance; self-audited).",
	"PUT /v1/m/knowledge/kbs/{id}":                                   "Updates a KB's governance settings (classification/residency/ embed policy/default ACL/status), version-checked, re-validating the embed gate.",
	"PUT /v1/m/models/access-tier-entitlements":                      "Records the current provider-side entitlement state for one restricted tier.",
	"PUT /v1/m/models/admission-policy":                              "Upserts the per-tenant trust root. Admin-tier, audited.",
	"PUT /v1/m/models/finetune-jobs/{id}":                            "Records a status/lineage transition on a fine-tune job (the control plane TRACKS the job; it does not run it).",
	"PUT /v1/m/models/gpai-posture":                                  "Records (or replaces) a provider's GPAI posture. It is an upsert keyed by provider_ref: one posture per provider per tenant.",
	"PUT /v1/m/models/inference-deployments/{id}":                    "Replaces one inference deployment; its references must resolve, and the admission policy can refuse it.",
	"PUT /v1/m/models/keys/{id}":                                     "Updates a key/workspace governance reference in place.",
	"PUT /v1/m/models/model-access/{id}":                             "Replaces one model-access rule; its subject and target references are validated.",
	"PUT /v1/m/models/model-groups/{id}":                             "Replaces one model group with the posted document.",
	"PUT /v1/m/models/owned-models/{id}":                             "Replaces one owned model with the posted document.",
	"PUT /v1/m/models/routing-policies/{id}":                         "Updates a routing policy in place.",
	"PUT /v1/m/models/workspace-residency":                           "Records (or replaces) one workspace's residency reference. It is an upsert keyed by workspace_ref: a catalog re-sync replaces the geos in place, never duplicates the row.",
	"PUT /v1/m/notify/routes/{id}":                                   "Replaces a route's predicate, destination and windows. Privileged write, self-audited.",
	"PUT /v1/m/orchestration/workflows/{id}/steps":                   "Replaces the whole step graph atomically (write-tier, revisioned).",
	"PUT /v1/m/recording/config":                                     "Replaces the tenant's recording policy (admin; self-audited).",
	"PUT /v1/m/reporting/branding":                                   "Replaces the tenant report branding, and answers 501 when no branding store is wired.",
	"PUT /v1/m/reporting/templates/{type}":                           "Replaces the tenant custom template for one report type; a body over the size ceiling is refused with 413.",
	"PUT /v1/m/security/enforcement":                                 "Sets the inline-enforcement posture for a guardrail class. This is the ONLY production-affecting action in the module, so it is admin-tier AND governed by the ApprovalGate where wired: enabling enforcement asks the ApprovalGate, and the result records whether it was governed.",
	"PUT /v1/m/sessions/templates/{id}":                              "Replaces one session template; an unknown key in the body is refused rather than dropped, and a built-in template is refused.",
	"PUT /v1/m/sessions/workspaces/{ref}/files/raw":                  "Writes the request body as the content of one workspace file named by the path query parameter; a body over the size ceiling is refused with 413.",
	"PUT /v1/m/sourcescope/assignments/{id}":                         "Updates an assignment (enabled, note). The connector_name and workspace_ref are immutable — they form the unique key.",
	"PUT /v1/m/sourcescope/bindings/{id}":                            "Updates a binding in place (scope, credential reference, enabled, note).",
	"PUT /v1/m/sourcescope/guard-postures":                           "Sets the retrieval guard posture of one source; a downgrade to public_only is queued as a posture request for approval instead of being applied.",
	"PUT /v1/m/sourcescope/workspace-connectors/{id}":                "Updates a workspace connector. The name, kind and workspace_ref are immutable (they form the natural key + unique index).",
	"PUT /v1/m/voice/policies":                                       "Upserts a voice-open policy (default-deny means a policy is what PERMITS an open).",
	"PUT /v1/system/orgs/{tenant_id}/region":                         "Sets or clears the data residency pin of one tenant. The region is validated against the residency registry, and an empty region clears the pin.",
	"PUT /v1/system/orgs/{tenant_id}/status":                         "Withdraws or restores the SERVICE of one tenant without touching its data, the step between served and deleted.",
}

// handlerDocOperationDescriptions is the set whose prose was derived directly
// from the registered handler's Go doc comment, rather than supplied by the catalog.
var handlerDocOperationDescriptions = map[string]struct{}{
	"DELETE /v1/m/capabilities/configs/{id}":                   {},
	"DELETE /v1/m/catalog/entries/{id}":                        {},
	"DELETE /v1/m/compliance/aims/pack/{id}":                   {},
	"DELETE /v1/m/compliance/dora/incidents/{id}":              {},
	"DELETE /v1/m/compliance/dora/register/{id}":               {},
	"DELETE /v1/m/compliance/oscal/profiles/{id}":              {},
	"DELETE /v1/m/compliance/retention/policies/{class}":       {},
	"DELETE /v1/m/consoleviews/views/{id}":                     {},
	"DELETE /v1/m/eventing/subscriptions/{id}":                 {},
	"DELETE /v1/m/finops/model-rates/{id}":                     {},
	"DELETE /v1/m/governance/agents/{agentID}/identity":        {},
	"DELETE /v1/m/governance/guardian/rules/{id}":              {},
	"DELETE /v1/m/governance/policies/{id}":                    {},
	"DELETE /v1/m/governance/routine-policies/{id}":            {},
	"DELETE /v1/m/health/checks/{id}":                          {},
	"DELETE /v1/m/inferenceproxy/dlp/rules/{id}":               {},
	"DELETE /v1/m/knowledge/dlp/rules/{id}":                    {},
	"DELETE /v1/m/knowledge/kbs/{id}":                          {},
	"DELETE /v1/m/knowledge/memory/{id}":                       {},
	"DELETE /v1/m/models/keys/{id}":                            {},
	"DELETE /v1/m/models/model-groups/{id}":                    {},
	"DELETE /v1/m/models/routing-policies/{id}":                {},
	"DELETE /v1/m/notify/routes/{id}":                          {},
	"DELETE /v1/m/sourcescope/assignments/{id}":                {},
	"DELETE /v1/m/sourcescope/bindings/{id}":                   {},
	"DELETE /v1/m/sourcescope/workspace-connectors/{id}":       {},
	"GET /v1/m/accessmap/attack-paths/escalation":              {},
	"GET /v1/m/accessmap/attack-paths/exfil":                   {},
	"GET /v1/m/accessmap/attack-paths/reachability":            {},
	"GET /v1/m/accessmap/attack-paths/summary":                 {},
	"GET /v1/m/accessmap/drift":                                {},
	"GET /v1/m/accessmap/graph":                                {},
	"GET /v1/m/accessmap/neighbors":                            {},
	"GET /v1/m/adoption/developers":                            {},
	"GET /v1/m/adoption/discrepancy":                           {},
	"GET /v1/m/adoption/summary":                               {},
	"GET /v1/m/adoption/teams":                                 {},
	"GET /v1/m/adoption/trend":                                 {},
	"GET /v1/m/capabilities/configs":                           {},
	"GET /v1/m/capabilities/configs/{id}":                      {},
	"GET /v1/m/capabilities/configs/{id}/revisions":            {},
	"GET /v1/m/capabilities/servers":                           {},
	"GET /v1/m/capabilities/servers/{id}":                      {},
	"GET /v1/m/capabilities/skills":                            {},
	"GET /v1/m/capabilities/tools":                             {},
	"GET /v1/m/capabilities/wiring":                            {},
	"GET /v1/m/catalog/connector-admissions":                   {},
	"GET /v1/m/catalog/entries":                                {},
	"GET /v1/m/catalog/entries/{id}":                           {},
	"GET /v1/m/catalog/entries/{id}/verify":                    {},
	"GET /v1/m/catalog/instances":                              {},
	"GET /v1/m/catalog/instances/{id}":                         {},
	"GET /v1/m/catalog/mcp-admissions":                         {},
	"GET /v1/m/catalog/pubkey":                                 {},
	"GET /v1/m/claude-agents/sessions/{id}/events":             {},
	"GET /v1/m/claude-policy/{surface}/artifact":               {},
	"GET /v1/m/claude-policy/{surface}/distribution":           {},
	"GET /v1/m/claude-policy/{surface}/versions":               {},
	"GET /v1/m/claude-policy/{surface}/versions/{revision}":    {},
	"GET /v1/m/compliance/calendar":                            {},
	"GET /v1/m/compliance/capabilities":                        {},
	"GET /v1/m/compliance/claude-files":                        {},
	"GET /v1/m/compliance/depth/fedramp/{id}/export":           {},
	"GET /v1/m/compliance/depth/sector/{id}/export":            {},
	"GET /v1/m/compliance/depth/us-law/{id}/export":            {},
	"GET /v1/m/compliance/dora":                                {},
	"GET /v1/m/compliance/dora/incidents/{id}/report":          {},
	"GET /v1/m/compliance/evidence/{id}":                       {},
	"GET /v1/m/compliance/evidence/{id}/export":                {},
	"GET /v1/m/compliance/retention/classes":                   {},
	"GET /v1/m/compliance/summary":                             {},
	"GET /v1/m/consoleviews/views":                             {},
	"GET /v1/m/consoleviews/views/{id}":                        {},
	"GET /v1/m/deploy/definitions":                             {},
	"GET /v1/m/deploy/definitions/{id}":                        {},
	"GET /v1/m/deploy/definitions/{id}/revisions":              {},
	"GET /v1/m/deploy/operations":                              {},
	"GET /v1/m/deploy/wirings":                                 {},
	"GET /v1/m/evals/calibration/items":                        {},
	"GET /v1/m/evals/calibration/reports":                      {},
	"GET /v1/m/evals/gate":                                     {},
	"GET /v1/m/evals/gate/{id}":                                {},
	"GET /v1/m/evals/runs":                                     {},
	"GET /v1/m/evals/runs/{id}":                                {},
	"GET /v1/m/evals/runs/{id}/results":                        {},
	"GET /v1/m/evals/runs/{id}/stream":                         {},
	"GET /v1/m/evals/scorecards":                               {},
	"GET /v1/m/evals/suites":                                   {},
	"GET /v1/m/evals/suites/{id}":                              {},
	"GET /v1/m/evals/suites/{id}/cases":                        {},
	"GET /v1/m/eventing/deliveries":                            {},
	"GET /v1/m/eventing/egress-policy":                         {},
	"GET /v1/m/eventing/event-types":                           {},
	"GET /v1/m/eventing/events":                                {},
	"GET /v1/m/eventing/subscriptions":                         {},
	"GET /v1/m/eventing/subscriptions/{id}":                    {},
	"GET /v1/m/eventing/subscriptions/{id}/revisions":          {},
	"GET /v1/m/finops/analytics/team-summary":                  {},
	"GET /v1/m/finops/comparison":                              {},
	"GET /v1/m/finops/model-rates":                             {},
	"GET /v1/m/finops/model-rates/{id}":                        {},
	"GET /v1/m/finops/outcomes":                                {},
	"GET /v1/m/finops/seats/utilization":                       {},
	"GET /v1/m/finops/spend/export":                            {},
	"GET /v1/m/finops/value":                                   {},
	"GET /v1/m/finops/value/summary":                           {},
	"GET /v1/m/governance/agent-risk-profiles":                 {},
	"GET /v1/m/governance/agent-risk-profiles/{id}":            {},
	"GET /v1/m/governance/approvals":                           {},
	"GET /v1/m/governance/approvals/{id}":                      {},
	"GET /v1/m/governance/approvals/{id}/decisions":            {},
	"GET /v1/m/governance/bindings":                            {},
	"GET /v1/m/governance/breakglass":                          {},
	"GET /v1/m/governance/breakglass/{id}":                     {},
	"GET /v1/m/governance/breakglass/{id}/uses":                {},
	"GET /v1/m/governance/emerging-identity-standards":         {},
	"GET /v1/m/governance/groups":                              {},
	"GET /v1/m/governance/groups/{ref}/members":                {},
	"GET /v1/m/governance/guardian/actions":                    {},
	"GET /v1/m/governance/identities":                          {},
	"GET /v1/m/governance/killswitch":                          {},
	"GET /v1/m/governance/killswitch/state":                    {},
	"GET /v1/m/governance/killswitch/{id}":                     {},
	"GET /v1/m/governance/killswitch/{id}/evidence":            {},
	"GET /v1/m/governance/nhi":                                 {},
	"GET /v1/m/governance/nhi/posture":                         {},
	"GET /v1/m/governance/nhi/{ref}":                           {},
	"GET /v1/m/governance/nhi/{ref}/events":                    {},
	"GET /v1/m/governance/pdp/active":                          {},
	"GET /v1/m/governance/pdp/versions":                        {},
	"GET /v1/m/governance/policies":                            {},
	"GET /v1/m/governance/policies/{id}":                       {},
	"GET /v1/m/governance/routine-policies":                    {},
	"GET /v1/m/governance/routine-policies/posture":            {},
	"GET /v1/m/governance/routine-policies/{id}":               {},
	"GET /v1/m/health/checks":                                  {},
	"GET /v1/m/health/checks/{id}":                             {},
	"GET /v1/m/health/dependencies":                            {},
	"GET /v1/m/health/events":                                  {},
	"GET /v1/m/health/incidents":                               {},
	"GET /v1/m/health/incidents/{id}":                          {},
	"GET /v1/m/health/sla":                                     {},
	"GET /v1/m/health/status":                                  {},
	"GET /v1/m/health/stream":                                  {},
	"GET /v1/m/identity/external-keys":                         {},
	"GET /v1/m/identity/residency":                             {},
	"GET /v1/m/identity/sso":                                   {},
	"GET /v1/m/identity/wif":                                   {},
	"GET /v1/m/inferenceproxy/config":                          {},
	"GET /v1/m/inferenceproxy/dlp/rules":                       {},
	"GET /v1/m/inventory/entities":                             {},
	"GET /v1/m/inventory/entities/{kind}/{id}":                 {},
	"GET /v1/m/inventory/summary":                              {},
	"GET /v1/m/knowledge/context-policies":                     {},
	"GET /v1/m/knowledge/dlp/rules":                            {},
	"GET /v1/m/knowledge/documents/{id}":                       {},
	"GET /v1/m/knowledge/kbs":                                  {},
	"GET /v1/m/knowledge/kbs/{id}/documents":                   {},
	"GET /v1/m/knowledge/labels":                               {},
	"GET /v1/m/knowledge/lineage":                              {},
	"GET /v1/m/knowledge/lineage/{id}":                         {},
	"GET /v1/m/knowledge/memory":                               {},
	"GET /v1/m/knowledge/memory/export":                        {},
	"GET /v1/m/knowledge/memory/{id}":                          {},
	"GET /v1/m/knowledge/prompts/{id}":                         {},
	"GET /v1/m/knowledge/prompts/{id}/revisions":               {},
	"GET /v1/m/knowledge/prompts/{id}/revisions/{rev}":         {},
	"GET /v1/m/knowledge/scans":                                {},
	"GET /v1/m/models/access-tier-entitlements":                {},
	"GET /v1/m/models/agent-artifacts/aibom":                   {},
	"GET /v1/m/models/agent-artifacts/aiboms":                  {},
	"GET /v1/m/models/catalog":                                 {},
	"GET /v1/m/models/data-governance":                         {},
	"GET /v1/m/models/features":                                {},
	"GET /v1/m/models/gpai-posture":                            {},
	"GET /v1/m/models/keys":                                    {},
	"GET /v1/m/models/models":                                  {},
	"GET /v1/m/models/models/{id}":                             {},
	"GET /v1/m/models/owned-models/{id}/aibom":                 {},
	"GET /v1/m/models/owned-models/{id}/model-card":            {},
	"GET /v1/m/models/platforms":                               {},
	"GET /v1/m/models/rate-limits":                             {},
	"GET /v1/m/models/routing-policies":                        {},
	"GET /v1/m/models/routing-policies/{id}":                   {},
	"GET /v1/m/models/tool-types":                              {},
	"GET /v1/m/models/workspace-residency":                     {},
	"GET /v1/m/notify/deliveries":                              {},
	"GET /v1/m/notify/destinations":                            {},
	"GET /v1/m/notify/match-types":                             {},
	"GET /v1/m/notify/outbox":                                  {},
	"GET /v1/m/notify/routes":                                  {},
	"GET /v1/m/notify/routes/{id}":                             {},
	"GET /v1/m/notify/routes/{id}/revisions":                   {},
	"GET /v1/m/observability/attestation":                      {},
	"GET /v1/m/observability/ingestion-health":                 {},
	"GET /v1/m/observability/traces":                           {},
	"GET /v1/m/observability/traces/{id}":                      {},
	"GET /v1/m/observability/traces/{id}/export":               {},
	"GET /v1/m/orchestration/decisions":                        {},
	"GET /v1/m/orchestration/flows":                            {},
	"GET /v1/m/orchestration/graph":                            {},
	"GET /v1/m/orchestration/graph/neighbors":                  {},
	"GET /v1/m/orchestration/schedules":                        {},
	"GET /v1/m/orchestration/schedules/{id}":                   {},
	"GET /v1/m/orchestration/schedules/{id}/decisions":         {},
	"GET /v1/m/orchestration/schedules/{id}/revisions":         {},
	"GET /v1/m/orchestration/stream":                           {},
	"GET /v1/m/orchestration/timeline":                         {},
	"GET /v1/m/orchestration/workflows":                        {},
	"GET /v1/m/orchestration/workflows/{id}":                   {},
	"GET /v1/m/orchestration/workflows/{id}/revisions":         {},
	"GET /v1/m/orchestration/workflows/{id}/runs":              {},
	"GET /v1/m/orchestration/workflows/{id}/runs/{run}":        {},
	"GET /v1/m/posture/export":                                 {},
	"GET /v1/m/recording/config":                               {},
	"GET /v1/m/recording/notice":                               {},
	"GET /v1/m/recording/sessions":                             {},
	"GET /v1/m/recording/sessions/{id}":                        {},
	"GET /v1/m/recording/sessions/{id}/export":                 {},
	"GET /v1/m/recording/sessions/{id}/replay":                 {},
	"GET /v1/m/recording/sessions/{id}/unified":                {},
	"GET /v1/m/recording/sessions/{id}/verify":                 {},
	"GET /v1/m/redteam/catalog":                                {},
	"GET /v1/m/redteam/runs":                                   {},
	"GET /v1/m/redteam/runs/{id}":                              {},
	"GET /v1/m/redteam/runs/{id}/results":                      {},
	"GET /v1/m/redteam/targets":                                {},
	"GET /v1/m/redteam/targets/{id}":                           {},
	"GET /v1/m/sandbox/comparisons":                            {},
	"GET /v1/m/sandbox/comparisons/{id}":                       {},
	"GET /v1/m/sandbox/runs":                                   {},
	"GET /v1/m/sandbox/runs/{id}":                              {},
	"GET /v1/m/sandbox/runs/{id}/outputs":                      {},
	"GET /v1/m/sandbox/scenarios":                              {},
	"GET /v1/m/sandbox/scenarios/{id}":                         {},
	"GET /v1/m/security/anomalies":                             {},
	"GET /v1/m/security/cases":                                 {},
	"GET /v1/m/security/cases/{id}":                            {},
	"GET /v1/m/security/cases/{id}/export":                     {},
	"GET /v1/m/security/cases/{id}/links":                      {},
	"GET /v1/m/security/cases/{id}/timeline":                   {},
	"GET /v1/m/security/enforcement":                           {},
	"GET /v1/m/security/findings":                              {},
	"GET /v1/m/security/findings/export":                       {},
	"GET /v1/m/security/findings/{id}":                         {},
	"GET /v1/m/security/safety-posture":                        {},
	"GET /v1/m/sessions/live":                                  {},
	"GET /v1/m/sessions/live/{ref}":                            {},
	"GET /v1/m/sessions/live/{ref}/timeline":                   {},
	"GET /v1/m/sessions/protocol-binding-specs":                {},
	"GET /v1/m/sessions/protocol-binding-specs/{id}":           {},
	"GET /v1/m/sessions/protocol-bindings":                     {},
	"GET /v1/m/sessions/protocol-bindings/{id}":                {},
	"GET /v1/m/sessions/runs/{ref}/attach":                     {},
	"GET /v1/m/sessions/stream":                                {},
	"GET /v1/m/sourcescope/assignments":                        {},
	"GET /v1/m/sourcescope/assignments/{id}":                   {},
	"GET /v1/m/sourcescope/bindings":                           {},
	"GET /v1/m/sourcescope/bindings/{id}":                      {},
	"GET /v1/m/sourcescope/guard-postures":                     {},
	"GET /v1/m/sourcescope/posture-requests":                   {},
	"GET /v1/m/sourcescope/posture-requests/{id}":              {},
	"GET /v1/m/sourcescope/resolve":                            {},
	"GET /v1/m/sourcescope/workspace-connectors":               {},
	"GET /v1/m/sourcescope/workspace-connectors/{id}":          {},
	"GET /v1/m/voice/decisions":                                {},
	"GET /v1/m/voice/policies":                                 {},
	"GET /v1/m/voice/sessions":                                 {},
	"GET /v1/m/voice/sessions/{ref}":                           {},
	"GET /v1/m/voice/sessions/{ref}/decisions":                 {},
	"GET /v1/m/voice/sessions/{ref}/stream":                    {},
	"PATCH /v1/m/orchestration/schedules/{id}":                 {},
	"PATCH /v1/m/orchestration/workflows/{id}":                 {},
	"PATCH /v1/m/security/cases/{id}":                          {},
	"PATCH /v1/m/security/findings/{id}":                       {},
	"POST /v1/m/capabilities/configs":                          {},
	"POST /v1/m/catalog/entries":                               {},
	"POST /v1/m/catalog/entries/{id}/approve":                  {},
	"POST /v1/m/catalog/entries/{id}/deprecate":                {},
	"POST /v1/m/catalog/entries/{id}/instantiate":              {},
	"POST /v1/m/catalog/entries/{id}/submit":                   {},
	"POST /v1/m/catalog/instances/{id}/transition":             {},
	"POST /v1/m/claude-agents/sessions/{id}/tool-confirmation": {},
	"POST /v1/m/claude-policy/{surface}/checkin":               {},
	"POST /v1/m/claude-policy/{surface}/dry-run":               {},
	"POST /v1/m/claude-policy/{surface}/publish":               {},
	"POST /v1/m/claude-policy/{surface}/validate":              {},
	"POST /v1/m/compliance/aims/pack":                          {},
	"POST /v1/m/compliance/claude-files/{id}/erase":            {},
	"POST /v1/m/compliance/depth/ccm/drift":                    {},
	"POST /v1/m/compliance/depth/ccm/snapshot":                 {},
	"POST /v1/m/compliance/depth/fedramp":                      {},
	"POST /v1/m/compliance/depth/sector":                       {},
	"POST /v1/m/compliance/depth/us-law":                       {},
	"POST /v1/m/compliance/dora/incidents":                     {},
	"POST /v1/m/compliance/dora/register":                      {},
	"POST /v1/m/compliance/erasure":                            {},
	"POST /v1/m/compliance/erasure/{id}/execute":               {},
	"POST /v1/m/compliance/frameworks/{id}/evidence":           {},
	"POST /v1/m/compliance/holds":                              {},
	"POST /v1/m/compliance/oscal/profiles":                     {},
	"POST /v1/m/compliance/residency":                          {},
	"POST /v1/m/compliance/residency/scan":                     {},
	"POST /v1/m/compliance/retention/sweep":                    {},
	"POST /v1/m/compliance/risk/classify":                      {},
	"POST /v1/m/consoleviews/views":                            {},
	"POST /v1/m/deploy/definitions":                            {},
	"POST /v1/m/deploy/definitions/{id}/apply":                 {},
	"POST /v1/m/deploy/definitions/{id}/retire":                {},
	"POST /v1/m/deploy/definitions/{id}/rollback":              {},
	"POST /v1/m/deploy/definitions/{id}/verify":                {},
	"POST /v1/m/evals/ab":                                      {},
	"POST /v1/m/evals/baselines":                               {},
	"POST /v1/m/evals/calibration/items":                       {},
	"POST /v1/m/evals/calibration/run":                         {},
	"POST /v1/m/evals/gate":                                    {},
	"POST /v1/m/evals/monitor":                                 {},
	"POST /v1/m/evals/runs":                                    {},
	"POST /v1/m/evals/suites":                                  {},
	"POST /v1/m/evals/suites/{id}/archive":                     {},
	"POST /v1/m/evals/suites/{id}/cases":                       {},
	"POST /v1/m/eventing/deliveries/{id}/redeliver":            {},
	"POST /v1/m/eventing/egress-policy/check":                  {},
	"POST /v1/m/eventing/subscriptions":                        {},
	"POST /v1/m/eventing/subscriptions/{id}/replay":            {},
	"POST /v1/m/eventing/subscriptions/{id}/restore":           {},
	"POST /v1/m/eventing/subscriptions/{id}/rotate-auth":       {},
	"POST /v1/m/eventing/subscriptions/{id}/rotate-secret":     {},
	"POST /v1/m/eventing/subscriptions/{id}/test":              {},
	"POST /v1/m/finops/cost":                                   {},
	"POST /v1/m/finops/model-rates":                            {},
	"POST /v1/m/finops/outcomes":                               {},
	"POST /v1/m/finops/seats":                                  {},
	"POST /v1/m/governance/agent-risk-profiles/classify":       {},
	"POST /v1/m/governance/agent-risk-profiles/{id}/review":    {},
	"POST /v1/m/governance/agents":                             {},
	"POST /v1/m/governance/agents/{agentID}/identity":          {},
	"POST /v1/m/governance/approvals":                          {},
	"POST /v1/m/governance/approvals/sweep":                    {},
	"POST /v1/m/governance/approvals/{id}/cancel":              {},
	"POST /v1/m/governance/approvals/{id}/decisions":           {},
	"POST /v1/m/governance/breakglass":                         {},
	"POST /v1/m/governance/breakglass/{id}/review":             {},
	"POST /v1/m/governance/breakglass/{id}/revoke":             {},
	"POST /v1/m/governance/guardian/rules":                     {},
	"POST /v1/m/governance/killswitch/{id}/reenable":           {},
	"POST /v1/m/governance/killswitch/{id}/review":             {},
	"POST /v1/m/governance/nhi/sweep":                          {},
	"POST /v1/m/governance/nhi/{ref}/offboard":                 {},
	"POST /v1/m/governance/nhi/{ref}/restore":                  {},
	"POST /v1/m/governance/nhi/{ref}/rotate":                   {},
	"POST /v1/m/governance/pdp/explain":                        {},
	"POST /v1/m/governance/pdp/publish":                        {},
	"POST /v1/m/governance/pdp/rollback":                       {},
	"POST /v1/m/governance/pdp/validate":                       {},
	"POST /v1/m/governance/policies":                           {},
	"POST /v1/m/governance/roster/sync":                        {},
	"POST /v1/m/governance/routine-policies":                   {},
	"POST /v1/m/health/checks":                                 {},
	"POST /v1/m/health/checks/{id}/report":                     {},
	"POST /v1/m/knowledge/context-policies":                    {},
	"POST /v1/m/knowledge/kbs":                                 {},
	"POST /v1/m/knowledge/kbs/{id}/ingest":                     {},
	"POST /v1/m/knowledge/kbs/{id}/reindex":                    {},
	"POST /v1/m/knowledge/kbs/{id}/scan":                       {},
	"POST /v1/m/knowledge/kbs/{id}/sync":                       {},
	"POST /v1/m/knowledge/memory":                              {},
	"POST /v1/m/knowledge/memory/import":                       {},
	"POST /v1/m/knowledge/memory/purge":                        {},
	"POST /v1/m/knowledge/prompts":                             {},
	"POST /v1/m/knowledge/prompts/{id}/revisions":              {},
	"POST /v1/m/knowledge/prompts/{id}/rollback":               {},
	"POST /v1/m/knowledge/sources/{name}/scan":                 {},
	"POST /v1/m/models/agent-artifacts/aibom":                  {},
	"POST /v1/m/models/keys":                                   {},
	"POST /v1/m/models/model-versions/{id}/admit":              {},
	"POST /v1/m/models/owned-models/{id}/aibom":                {},
	"POST /v1/m/models/routing-policies":                       {},
	"POST /v1/m/models/routing-policies/{id}/execute":          {},
	"POST /v1/m/models/routing-policies/{id}/resolve":          {},
	"POST /v1/m/notify/outbox/{id}/redeliver":                  {},
	"POST /v1/m/notify/routes":                                 {},
	"POST /v1/m/notify/routes/evaluate":                        {},
	"POST /v1/m/notify/routes/{id}/restore":                    {},
	"POST /v1/m/notify/routes/{id}/test":                       {},
	"POST /v1/m/orchestration/schedules":                       {},
	"POST /v1/m/orchestration/schedules/{id}/restore":          {},
	"POST /v1/m/orchestration/workflows":                       {},
	"POST /v1/m/orchestration/workflows/{id}/dry-run":          {},
	"POST /v1/m/orchestration/workflows/{id}/restore":          {},
	"POST /v1/m/recording/ack":                                 {},
	"POST /v1/m/recording/sessions/{id}/seal":                  {},
	"POST /v1/m/recording/sessions/{id}/summarize":             {},
	"POST /v1/m/recording/sweep":                               {},
	"POST /v1/m/redteam/runs":                                  {},
	"POST /v1/m/redteam/targets":                               {},
	"POST /v1/m/redteam/targets/{id}/authorize":                {},
	"POST /v1/m/sandbox/compare":                               {},
	"POST /v1/m/sandbox/replay":                                {},
	"POST /v1/m/sandbox/scenarios":                             {},
	"POST /v1/m/sandbox/scenarios/{id}/archive":                {},
	"POST /v1/m/sandbox/scenarios/{id}/run":                    {},
	"POST /v1/m/security/cases":                                {},
	"POST /v1/m/security/cases/{id}/links":                     {},
	"POST /v1/m/security/guardrails/inspect":                   {},
	"POST /v1/m/sessions/protocol-binding-specs":               {},
	"POST /v1/m/sessions/protocol-binding-specs/{id}/activate": {},
	"POST /v1/m/sessions/protocol-binding-specs/{id}/disable":  {},
	"POST /v1/m/sessions/protocol-bindings/{id}/reconcile":     {},
	"POST /v1/m/sourcescope/assignments":                       {},
	"POST /v1/m/sourcescope/bindings":                          {},
	"POST /v1/m/sourcescope/posture-requests/{id}/reject":      {},
	"POST /v1/m/sourcescope/sources/disable-scoping":           {},
	"POST /v1/m/sourcescope/workspace-connectors":              {},
	"PUT /v1/m/capabilities/configs/{id}":                      {},
	"PUT /v1/m/catalog/connector-admission/policy":             {},
	"PUT /v1/m/catalog/entries/{id}":                           {},
	"PUT /v1/m/catalog/mcp-admission/policy":                   {},
	"PUT /v1/m/compliance/retention/policies/{class}":          {},
	"PUT /v1/m/consoleviews/views/{id}":                        {},
	"PUT /v1/m/deploy/definitions/{id}":                        {},
	"PUT /v1/m/eventing/subscriptions/{id}":                    {},
	"PUT /v1/m/finops/model-rates/{id}":                        {},
	"PUT /v1/m/governance/agent-risk-profiles/{id}/tier":       {},
	"PUT /v1/m/governance/guardian/rules/{id}":                 {},
	"PUT /v1/m/governance/nhi/{ref}/ownership":                 {},
	"PUT /v1/m/governance/nhi/{ref}/policy":                    {},
	"PUT /v1/m/governance/policies/{id}":                       {},
	"PUT /v1/m/governance/routine-policies/{id}":               {},
	"PUT /v1/m/health/checks/{id}":                             {},
	"PUT /v1/m/inferenceproxy/config":                          {},
	"PUT /v1/m/inferenceproxy/dlp/rules":                       {},
	"PUT /v1/m/knowledge/dlp/rules":                            {},
	"PUT /v1/m/knowledge/kbs/{id}":                             {},
	"PUT /v1/m/models/access-tier-entitlements":                {},
	"PUT /v1/m/models/admission-policy":                        {},
	"PUT /v1/m/models/finetune-jobs/{id}":                      {},
	"PUT /v1/m/models/gpai-posture":                            {},
	"PUT /v1/m/models/keys/{id}":                               {},
	"PUT /v1/m/models/routing-policies/{id}":                   {},
	"PUT /v1/m/models/workspace-residency":                     {},
	"PUT /v1/m/notify/routes/{id}":                             {},
	"PUT /v1/m/orchestration/workflows/{id}/steps":             {},
	"PUT /v1/m/recording/config":                               {},
	"PUT /v1/m/security/enforcement":                           {},
	"PUT /v1/m/sourcescope/assignments/{id}":                   {},
	"PUT /v1/m/sourcescope/bindings/{id}":                      {},
	"PUT /v1/m/sourcescope/workspace-connectors/{id}":          {},
	"PUT /v1/m/voice/policies":                                 {},
}
