{
  "name": "olivares-repo-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Root dev tooling for the Olivares AI monorepo (commit-message linting only; the engine is Go, the web app is in /web).",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@10.33.2",
  "engines": { "node": ">=20" },
  "scripts": {
    "commitlint": "commitlint"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2"
  }
}
