// Code generated by clients/generator from the Olivares OpenAPI snapshots (stable core + beta module routes); DO NOT EDIT.
// Transport, auth, the error envelope, pagination and deprecation signaling
// live in the hand-written core (./core.ts); version constants in version.gen.ts.

import { ClientCore, type Json, type JsonInput, type RequestOptions } from "./core.js";

export class Client extends ClientCore {
  /**
   * GET /healthz — Liveness probe.
   *
   * Stability: stable.
   */
  getHealthz(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/healthz", "/healthz", undefined, opts);
  }

  /**
   * GET /livez — Liveness probe (process is up)
   *
   * Stability: stable.
   */
  getLivez(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/livez", "/livez", undefined, opts);
  }

  /**
   * GET /metrics — Prometheus exposition (text format 0.0.4) of engine metrics.
   *
   * Stability: stable.
   */
  getMetrics(opts?: RequestOptions): Promise<string> {
    return this.doRaw("GET", "/metrics", "/metrics", opts);
  }

  /**
   * GET /openapi.json — This OpenAPI document.
   *
   * Stability: stable.
   */
  getOpenapiJson(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/openapi.json", "/openapi.json", undefined, opts);
  }

  /**
   * GET /pod-readyz — Pod-health probe (store reachable), with NO leadership check — the HA readiness probe, so a hot standby is healthy; 503 only when the store is down.
   *
   * Stability: stable.
   */
  getPodReadyz(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/pod-readyz", "/pod-readyz", undefined, opts);
  }

  /**
   * GET /readyz — Readiness probe (store reachable AND this node is the active writer); 503 on a standby or when the store is down.
   *
   * Stability: stable.
   */
  getReadyz(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/readyz", "/readyz", undefined, opts);
  }

  /**
   * GET /status — Public status page summary (unauthenticated)
   *
   * Stability: stable.
   */
  getStatus(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/status", "/status", undefined, opts);
  }

  /**
   * GET /v1/access-edges — List access edges (R/RW map); self-audited.
   *
   * Stability: stable.
   */
  getV1AccessEdges(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/access-edges", "/v1/access-edges", undefined, opts);
  }

  /**
   * GET /v1/agents — List agents in the resolved tenant.
   *
   * Stability: stable.
   */
  getV1Agents(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/agents", "/v1/agents", undefined, opts);
  }

  /**
   * POST /v1/agents — Create an agent.
   *
   * Stability: stable.
   */
  postV1Agents(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/agents", "/v1/agents", body, opts);
  }

  /**
   * GET /v1/agents/{id} — Get an agent by ID.
   *
   * Stability: stable.
   */
  getV1AgentsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/agents/{id}", `/v1/agents/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PATCH /v1/agents/{id} — Update an agent.
   *
   * Stability: stable.
   */
  patchV1AgentsById(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("PATCH", "/v1/agents/{id}", `/v1/agents/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/agents/{id} — Delete an agent.
   *
   * Stability: stable.
   */
  deleteV1AgentsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/agents/{id}", `/v1/agents/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/audit — Read the tenant evidence ledger.
   *
   * Stability: stable.
   */
  getV1Audit(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/audit", "/v1/audit", undefined, opts);
  }

  /**
   * GET /v1/audit/export — Export the ledger (cef|leef|syslog|otlp|otlp_envelope|otlp_log_record|ocsf)
   *
   * Stability: stable.
   */
  getV1AuditExport(opts?: RequestOptions): Promise<string> {
    return this.doRaw("GET", "/v1/audit/export", "/v1/audit/export", opts);
  }

  /**
   * GET /v1/audit/pubkey — The Ed25519 checkpoint verification key (PEM)
   *
   * Stability: stable.
   */
  getV1AuditPubkey(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/audit/pubkey", "/v1/audit/pubkey", undefined, opts);
  }

  /**
   * GET /v1/audit/system — Read the system-tenant evidence ledger (cross-tenant ops; superadmin only)
   *
   * Stability: stable.
   */
  getV1AuditSystem(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/audit/system", "/v1/audit/system", undefined, opts);
  }

  /**
   * GET /v1/audit/verify — Verify the chain and its signed checkpoints.
   *
   * Stability: stable.
   */
  getV1AuditVerify(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/audit/verify", "/v1/audit/verify", undefined, opts);
  }

  /**
   * POST /v1/auth/login — Exchange email/password for a session token.
   *
   * Stability: stable.
   */
  postV1AuthLogin(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/auth/login", "/v1/auth/login", body, opts);
  }

  /**
   * POST /v1/auth/logout — Revoke the calling session.
   *
   * Stability: stable.
   */
  postV1AuthLogout(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/auth/logout", "/v1/auth/logout", body, opts);
  }

  /**
   * POST /v1/auth/refresh — Renew the calling session token (rotates the credential, extends expiry)
   *
   * Stability: stable.
   */
  postV1AuthRefresh(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/auth/refresh", "/v1/auth/refresh", body, opts);
  }

  /**
   * GET /v1/auth/whoami — The calling principal and its tenant grants.
   *
   * Stability: stable.
   */
  getV1AuthWhoami(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/auth/whoami", "/v1/auth/whoami", undefined, opts);
  }

  /**
   * GET /v1/connectors/health — Per-connector health metrics and fleet summary.
   *
   * Stability: stable.
   */
  getV1ConnectorsHealth(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/connectors/health", "/v1/connectors/health", undefined, opts);
  }

  /**
   * GET /v1/console/bus — Event-bus subscriber, saturation, loss, and optional bridge snapshot.
   *
   * Stability: stable.
   */
  getV1ConsoleBus(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/console/bus", "/v1/console/bus", undefined, opts);
  }

  /**
   * GET /v1/console/config/effective — Live effective configuration with secret-bearing values redacted.
   *
   * Stability: stable.
   */
  getV1ConsoleConfigEffective(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/console/config/effective", "/v1/console/config/effective", undefined, opts);
  }

  /**
   * GET /v1/console/connectors — List available connector types with their configuration fields.
   *
   * Stability: stable.
   */
  getV1ConsoleConnectors(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/console/connectors", "/v1/console/connectors", undefined, opts);
  }

  /**
   * PUT /v1/console/connectors — Onboard or update a connector instance.
   *
   * Stability: stable.
   */
  putV1ConsoleConnectors(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("PUT", "/v1/console/connectors", "/v1/console/connectors", body, opts);
  }

  /**
   * DELETE /v1/console/connectors — Remove a connector instance.
   *
   * Stability: stable.
   */
  deleteV1ConsoleConnectors(opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/console/connectors", "/v1/console/connectors", undefined, opts);
  }

  /**
   * POST /v1/console/connectors/test — Test connectivity for a connector configuration.
   *
   * Stability: stable.
   */
  postV1ConsoleConnectorsTest(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/console/connectors/test", "/v1/console/connectors/test", body, opts);
  }

  /**
   * GET /v1/console/health-summary — Operational health summary for the console dashboard.
   *
   * Stability: stable.
   */
  getV1ConsoleHealthSummary(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/console/health-summary", "/v1/console/health-summary", undefined, opts);
  }

  /**
   * GET /v1/console/keys — Non-secret signing-key and sealer custody inventory.
   *
   * Stability: stable.
   */
  getV1ConsoleKeys(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/console/keys", "/v1/console/keys", undefined, opts);
  }

  /**
   * GET /v1/console/license — Current license status and entitlements.
   *
   * Stability: stable.
   */
  getV1ConsoleLicense(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/console/license", "/v1/console/license", undefined, opts);
  }

  /**
   * POST /v1/console/license — Install a commercial license.
   *
   * Stability: stable.
   */
  postV1ConsoleLicense(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/console/license", "/v1/console/license", body, opts);
  }

  /**
   * DELETE /v1/console/license — Remove the installed license (revert to community)
   *
   * Stability: stable.
   */
  deleteV1ConsoleLicense(opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/console/license", "/v1/console/license", undefined, opts);
  }

  /**
   * GET /v1/console/secrets — List sealed secrets (names and hints, never values)
   *
   * Stability: stable.
   */
  getV1ConsoleSecrets(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/console/secrets", "/v1/console/secrets", undefined, opts);
  }

  /**
   * PUT /v1/console/secrets — Create or update a sealed secret.
   *
   * Stability: stable.
   */
  putV1ConsoleSecrets(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("PUT", "/v1/console/secrets", "/v1/console/secrets", body, opts);
  }

  /**
   * DELETE /v1/console/secrets — Delete a sealed secret.
   *
   * Stability: stable.
   */
  deleteV1ConsoleSecrets(opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/console/secrets", "/v1/console/secrets", undefined, opts);
  }

  /**
   * GET /v1/console/setup-status — First-run setup wizard progress.
   *
   * Stability: stable.
   */
  getV1ConsoleSetupStatus(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/console/setup-status", "/v1/console/setup-status", undefined, opts);
  }

  /**
   * GET /v1/console/sources — List configured source connectors and their status.
   *
   * Stability: stable.
   */
  getV1ConsoleSources(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/console/sources", "/v1/console/sources", undefined, opts);
  }

  /**
   * PUT /v1/console/sources — Create or update a source connector.
   *
   * Stability: stable.
   */
  putV1ConsoleSources(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("PUT", "/v1/console/sources", "/v1/console/sources", body, opts);
  }

  /**
   * DELETE /v1/console/sources — Remove a source connector.
   *
   * Stability: stable.
   */
  deleteV1ConsoleSources(opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/console/sources", "/v1/console/sources", undefined, opts);
  }

  /**
   * GET /v1/console/sso — Current SSO/IdP configuration.
   *
   * Stability: stable.
   */
  getV1ConsoleSso(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/console/sso", "/v1/console/sso", undefined, opts);
  }

  /**
   * PUT /v1/console/sso — Create or update SSO/IdP configuration.
   *
   * Stability: stable.
   */
  putV1ConsoleSso(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("PUT", "/v1/console/sso", "/v1/console/sso", body, opts);
  }

  /**
   * DELETE /v1/console/sso — Remove SSO/IdP configuration.
   *
   * Stability: stable.
   */
  deleteV1ConsoleSso(opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/console/sso", "/v1/console/sso", undefined, opts);
  }

  /**
   * POST /v1/console/sso/test — Test SSO/IdP connectivity.
   *
   * Stability: stable.
   */
  postV1ConsoleSsoTest(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/console/sso/test", "/v1/console/sso/test", body, opts);
  }

  /**
   * POST /v1/console/support-bundle — Build and download a redacted support bundle (AAL3 step-up)
   *
   * Stability: stable.
   */
  postV1ConsoleSupportBundle(opts?: RequestOptions): Promise<string> {
    return this.doRaw("POST", "/v1/console/support-bundle", "/v1/console/support-bundle", opts);
  }

  /**
   * POST /v1/console/update-check — Check the configured signed update channel now.
   *
   * Stability: stable.
   */
  postV1ConsoleUpdateCheck(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/console/update-check", "/v1/console/update-check", body, opts);
  }

  /**
   * GET /v1/members — List the resolved tenant's member roster (role, workspace scoping, groups)
   *
   * Stability: stable.
   */
  getV1Members(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/members", "/v1/members", undefined, opts);
  }

  /**
   * POST /v1/memberships — Grant a user a role in a tenant.
   *
   * Stability: stable.
   */
  postV1Memberships(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/memberships", "/v1/memberships", body, opts);
  }

  /**
   * GET /v1/search — Federated console search: fan out to every searchable kind, deny-closed per kind on its own read permission.
   *
   * Stability: stable.
   */
  getV1Search(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/search", "/v1/search", undefined, opts);
  }

  /**
   * GET /v1/server-info — Server version, engine, setup state and license status.
   *
   * Stability: stable.
   */
  getV1ServerInfo(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/server-info", "/v1/server-info", undefined, opts);
  }

  /**
   * POST /v1/setup — Create the first organization and the superadmin that owns it, with the one-time setup token.
   *
   * Stability: stable.
   */
  postV1Setup(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/setup", "/v1/setup", body, opts);
  }

  /**
   * GET /v1/system/orgs — List tenant orgs (superadmin)
   *
   * Stability: stable.
   */
  getV1SystemOrgs(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/system/orgs", "/v1/system/orgs", undefined, opts);
  }

  /**
   * POST /v1/system/orgs — Provision a tenant (superadmin)
   *
   * Stability: stable.
   */
  postV1SystemOrgs(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/system/orgs", "/v1/system/orgs", body, opts);
  }

  /**
   * DELETE /v1/system/orgs/{tenant_id} — Hard-delete a tenant org after the cloud grace period (superadmin)
   *
   * Stability: stable.
   */
  deleteV1SystemOrgsByTenantId(tenant_id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/system/orgs/{tenant_id}", `/v1/system/orgs/${encodeURIComponent(tenant_id)}`, undefined, opts);
  }

  /**
   * PUT /v1/system/orgs/{tenant_id}/region — Set or clear a tenant residency pin (superadmin, AAL3 step-up)
   *
   * Stability: stable.
   */
  putV1SystemOrgsByTenantIdRegion(tenant_id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("PUT", "/v1/system/orgs/{tenant_id}/region", `/v1/system/orgs/${encodeURIComponent(tenant_id)}/region`, body, opts);
  }

  /**
   * PUT /v1/system/orgs/{tenant_id}/status — Withdraw or restore a tenant's service without deleting its data (superadmin)
   *
   * Stability: stable.
   */
  putV1SystemOrgsByTenantIdStatus(tenant_id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("PUT", "/v1/system/orgs/{tenant_id}/status", `/v1/system/orgs/${encodeURIComponent(tenant_id)}/status`, body, opts);
  }

  /**
   * GET /v1/system/residency — Get the configured data-residency registry (superadmin)
   *
   * Stability: stable.
   */
  getV1SystemResidency(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/system/residency", "/v1/system/residency", undefined, opts);
  }

  /**
   * GET /v1/tokens — List API tokens for the calling user.
   *
   * Stability: stable.
   */
  getV1Tokens(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/tokens", "/v1/tokens", undefined, opts);
  }

  /**
   * POST /v1/tokens — Issue an API token.
   *
   * Stability: stable.
   */
  postV1Tokens(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/tokens", "/v1/tokens", body, opts);
  }

  /**
   * DELETE /v1/tokens/{id} — Revoke an API token.
   *
   * Stability: stable.
   */
  deleteV1TokensById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/tokens/{id}", `/v1/tokens/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/tokens/{id}/rotate — Rotate an API token (issue new secret, revoke old)
   *
   * Stability: stable.
   */
  postV1TokensByIdRotate(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/tokens/{id}/rotate", `/v1/tokens/${encodeURIComponent(id)}/rotate`, body, opts);
  }

  /**
   * GET /v1/users — List users (superadmin)
   *
   * Stability: stable.
   */
  getV1Users(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/users", "/v1/users", undefined, opts);
  }

  /**
   * POST /v1/users — Create a user (superadmin)
   *
   * Stability: stable.
   */
  postV1Users(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/users", "/v1/users", body, opts);
  }

  /**
   * GET /v1/users/superadmins — List superadmin accounts and their active/inactive status (superadmin)
   *
   * Stability: stable.
   */
  getV1UsersSuperadmins(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/users/superadmins", "/v1/users/superadmins", undefined, opts);
  }

  /**
   * POST /v1/users/{id}/disable — Disable a superadmin account — non-destructive, reversible (superadmin, AAL3 step-up)
   *
   * Stability: stable.
   */
  postV1UsersByIdDisable(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/users/{id}/disable", `/v1/users/${encodeURIComponent(id)}/disable`, body, opts);
  }

  /**
   * POST /v1/users/{id}/enable — Enable a previously disabled superadmin account (superadmin, AAL3 step-up)
   *
   * Stability: stable.
   */
  postV1UsersByIdEnable(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/users/{id}/enable", `/v1/users/${encodeURIComponent(id)}/enable`, body, opts);
  }

  /**
   * GET /v1/workspaces — List workspaces in the resolved tenant.
   *
   * Stability: stable.
   */
  getV1Workspaces(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/workspaces", "/v1/workspaces", undefined, opts);
  }

  /**
   * POST /v1/workspaces — Create a workspace (tenant admin, AAL3 step-up)
   *
   * Stability: stable.
   */
  postV1Workspaces(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/workspaces", "/v1/workspaces", body, opts);
  }

  /**
   * GET /v1/workspaces/{id} — Get a workspace by ID.
   *
   * Stability: stable.
   */
  getV1WorkspacesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/workspaces/{id}", `/v1/workspaces/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PATCH /v1/workspaces/{id} — Update a workspace.
   *
   * Stability: stable.
   */
  patchV1WorkspacesById(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("PATCH", "/v1/workspaces/{id}", `/v1/workspaces/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * GET /v1/m/accessmap/attack-paths/escalation — Serves GET /attack-paths/escalation?agent_id=...
   *
   * Stability: beta.
   */
  getV1MAccessmapAttackPathsEscalation(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/accessmap/attack-paths/escalation", "/v1/m/accessmap/attack-paths/escalation", undefined, opts);
  }

  /**
   * GET /v1/m/accessmap/attack-paths/exfil — Serves GET /attack-paths/exfil?resource_id=...
   *
   * Stability: beta.
   */
  getV1MAccessmapAttackPathsExfil(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/accessmap/attack-paths/exfil", "/v1/m/accessmap/attack-paths/exfil", undefined, opts);
  }

  /**
   * GET /v1/m/accessmap/attack-paths/reachability — Serves GET /attack-paths/reachability?agent_id=...
   *
   * Stability: beta.
   */
  getV1MAccessmapAttackPathsReachability(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/accessmap/attack-paths/reachability", "/v1/m/accessmap/attack-paths/reachability", undefined, opts);
  }

  /**
   * GET /v1/m/accessmap/attack-paths/summary — Serves GET /attack-paths/summary.
   *
   * Stability: beta.
   */
  getV1MAccessmapAttackPathsSummary(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/accessmap/attack-paths/summary", "/v1/m/accessmap/attack-paths/summary", undefined, opts);
  }

  /**
   * GET /v1/m/accessmap/drift — Returns the permitted-vs-observed least-privilege drift: unexpected accesses (observed, not permitted — the headline) and unused grants.
   *
   * Stability: beta.
   */
  getV1MAccessmapDrift(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/accessmap/drift", "/v1/m/accessmap/drift", undefined, opts);
  }

  /**
   * GET /v1/m/accessmap/graph — Returns the R/RW graph as a React Flow node+edge contract, filtered by any of the supported edge columns.
   *
   * Stability: beta.
   */
  getV1MAccessmapGraph(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/accessmap/graph", "/v1/m/accessmap/graph", undefined, opts);
  }

  /**
   * GET /v1/m/accessmap/neighbors — Returns the edges touching one node (id required) in a direction (outgoing|incoming|both, default both), as the same node+edge contract.
   *
   * Stability: beta.
   */
  getV1MAccessmapNeighbors(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/accessmap/neighbors", "/v1/m/accessmap/neighbors", undefined, opts);
  }

  /**
   * GET /v1/m/adoption/developers — Returns the per-developer ROI breakdown from the admin Analytics lens.
   *
   * Stability: beta.
   */
  getV1MAdoptionDevelopers(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/adoption/developers", "/v1/m/adoption/developers", undefined, opts);
  }

  /**
   * GET /v1/m/adoption/discrepancy — Returns the official-vs-observed daily comparison over the requested window.
   *
   * Stability: beta.
   */
  getV1MAdoptionDiscrepancy(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/adoption/discrepancy", "/v1/m/adoption/discrepancy", undefined, opts);
  }

  /**
   * GET /v1/m/adoption/summary — Returns BOTH non-additive lenses over the window — the per-developer admin Analytics lens and the per-session OTLP telemetry lens — plus distinct developer/ team counts and the Claude-API-only boundary.
   *
   * Stability: beta.
   */
  getV1MAdoptionSummary(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/adoption/summary", "/v1/m/adoption/summary", undefined, opts);
  }

  /**
   * GET /v1/m/adoption/teams — Returns the per-team breakdown from the OTLP telemetry lens (the only lens that carries team labels).
   *
   * Stability: beta.
   */
  getV1MAdoptionTeams(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/adoption/teams", "/v1/m/adoption/teams", undefined, opts);
  }

  /**
   * GET /v1/m/adoption/trend — Returns a per-day series for ONE lens (default analytics).
   *
   * Stability: beta.
   */
  getV1MAdoptionTrend(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/adoption/trend", "/v1/m/adoption/trend", undefined, opts);
  }

  /**
   * GET /v1/m/capabilities/configs — Lists managed MCP-server configs, optionally filtered.
   *
   * Stability: beta.
   */
  getV1MCapabilitiesConfigs(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/capabilities/configs", "/v1/m/capabilities/configs", undefined, opts);
  }

  /**
   * POST /v1/m/capabilities/configs — Registers a new managed config for an MCP server, recording the first revision snapshot and a self-audit attributed to the real principal.
   *
   * Stability: beta.
   */
  postV1MCapabilitiesConfigs(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/capabilities/configs", "/v1/m/capabilities/configs", body, opts);
  }

  /**
   * GET /v1/m/capabilities/configs/{id} — Returns one managed config.
   *
   * Stability: beta.
   */
  getV1MCapabilitiesConfigsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/capabilities/configs/{id}", `/v1/m/capabilities/configs/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/capabilities/configs/{id} — Updates a managed config in place, bumping its revision and recording an immutable revision snapshot plus a self-audit.
   *
   * Stability: beta.
   */
  putV1MCapabilitiesConfigsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/capabilities/configs/{id}", `/v1/m/capabilities/configs/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/capabilities/configs/{id} — Removes a managed config, recording a final revision snapshot (the immutable history survives the deletion) and a self-audit.
   *
   * Stability: beta.
   */
  deleteV1MCapabilitiesConfigsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/capabilities/configs/{id}", `/v1/m/capabilities/configs/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/capabilities/configs/{id}/revisions — Returns the immutable version history of a config, newest revision first.
   *
   * Stability: beta.
   */
  getV1MCapabilitiesConfigsByIdRevisions(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/capabilities/configs/{id}/revisions", `/v1/m/capabilities/configs/${encodeURIComponent(id)}/revisions`, undefined, opts);
  }

  /**
   * GET /v1/m/capabilities/servers — Lists the live MCP-server catalog: every discovered MCPServer core entity enriched with its derived connection health, managed-config marker and tool count.
   *
   * Stability: beta.
   */
  getV1MCapabilitiesServers(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/capabilities/servers", "/v1/m/capabilities/servers", undefined, opts);
  }

  /**
   * GET /v1/m/capabilities/servers/{id} — Returns the full management view of one MCP server.
   *
   * Stability: beta.
   */
  getV1MCapabilitiesServersById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/capabilities/servers/{id}", `/v1/m/capabilities/servers/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/capabilities/skills — Lists the discovered skills (and the surface through which plugins/subagents appear), optionally filtered by ?server_id.
   *
   * Stability: beta.
   */
  getV1MCapabilitiesSkills(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/capabilities/skills", "/v1/m/capabilities/skills", undefined, opts);
  }

  /**
   * GET /v1/m/capabilities/toolpins — capabilities module route (requires capabilities:config:read)
   *
   * Stability: beta.
   */
  getV1MCapabilitiesToolpins(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/capabilities/toolpins", "/v1/m/capabilities/toolpins", undefined, opts);
  }

  /**
   * POST /v1/m/capabilities/toolpins/approve — capabilities module route (requires capabilities:config:write)
   *
   * Stability: beta.
   */
  postV1MCapabilitiesToolpinsApprove(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/capabilities/toolpins/approve", "/v1/m/capabilities/toolpins/approve", body, opts);
  }

  /**
   * POST /v1/m/capabilities/toolpins/unpin — capabilities module route (requires capabilities:config:write)
   *
   * Stability: beta.
   */
  postV1MCapabilitiesToolpinsUnpin(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/capabilities/toolpins/unpin", "/v1/m/capabilities/toolpins/unpin", body, opts);
  }

  /**
   * GET /v1/m/capabilities/tools — Lists the discovered tools with their UNTRUSTED annotation hints, optionally filtered by ?server_id.
   *
   * Stability: beta.
   */
  getV1MCapabilitiesTools(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/capabilities/tools", "/v1/m/capabilities/tools", undefined, opts);
  }

  /**
   * GET /v1/m/capabilities/wiring — Returns the capability-connection graph, optionally filtered by origin or capability.
   *
   * Stability: beta.
   */
  getV1MCapabilitiesWiring(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/capabilities/wiring", "/v1/m/capabilities/wiring", undefined, opts);
  }

  /**
   * GET /v1/m/catalog/connector-admission/policy — catalog module route (requires catalog:entry:read)
   *
   * Stability: beta.
   */
  getV1MCatalogConnectorAdmissionPolicy(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/catalog/connector-admission/policy", "/v1/m/catalog/connector-admission/policy", undefined, opts);
  }

  /**
   * PUT /v1/m/catalog/connector-admission/policy — Upserts the per-tenant trust root. Admin, audited.
   *
   * Stability: beta.
   */
  putV1MCatalogConnectorAdmissionPolicy(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/catalog/connector-admission/policy", "/v1/m/catalog/connector-admission/policy", body, opts);
  }

  /**
   * GET /v1/m/catalog/connector-admissions — Lists the recorded connector-entry admission verdicts.
   *
   * Stability: beta.
   */
  getV1MCatalogConnectorAdmissions(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/catalog/connector-admissions", "/v1/m/catalog/connector-admissions", undefined, opts);
  }

  /**
   * GET /v1/m/catalog/entries — Lists catalog entries, optionally filtered by kind/status/slug.
   *
   * Stability: beta.
   */
  getV1MCatalogEntries(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/catalog/entries", "/v1/m/catalog/entries", undefined, opts);
  }

  /**
   * POST /v1/m/catalog/entries — Creates a draft catalog entry.
   *
   * Stability: beta.
   */
  postV1MCatalogEntries(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/catalog/entries", "/v1/m/catalog/entries", body, opts);
  }

  /**
   * GET /v1/m/catalog/entries/{id} — Returns one catalog entry.
   *
   * Stability: beta.
   */
  getV1MCatalogEntriesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/catalog/entries/{id}", `/v1/m/catalog/entries/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/catalog/entries/{id} — Updates a DRAFT entry in place.
   *
   * Stability: beta.
   */
  putV1MCatalogEntriesById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/catalog/entries/{id}", `/v1/m/catalog/entries/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/catalog/entries/{id} — Deletes a DRAFT entry.
   *
   * Stability: beta.
   */
  deleteV1MCatalogEntriesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/catalog/entries/{id}", `/v1/m/catalog/entries/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/catalog/entries/{id}/admit — catalog module route (requires catalog:entry:admin)
   *
   * Stability: beta.
   */
  postV1MCatalogEntriesByIdAdmit(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/catalog/entries/{id}/admit", `/v1/m/catalog/entries/${encodeURIComponent(id)}/admit`, body, opts);
  }

  /**
   * POST /v1/m/catalog/entries/{id}/approve — Approves a draft/pending entry: it pins the content hash, signs it when a catalog signing key is configured, records the approver and freezes the entry.
   *
   * Stability: beta.
   */
  postV1MCatalogEntriesByIdApprove(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/catalog/entries/{id}/approve", `/v1/m/catalog/entries/${encodeURIComponent(id)}/approve`, undefined, opts);
  }

  /**
   * POST /v1/m/catalog/entries/{id}/deprecate — Retires an approved entry.
   *
   * Stability: beta.
   */
  postV1MCatalogEntriesByIdDeprecate(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/catalog/entries/{id}/deprecate", `/v1/m/catalog/entries/${encodeURIComponent(id)}/deprecate`, undefined, opts);
  }

  /**
   * POST /v1/m/catalog/entries/{id}/instantiate — Creates a self-service instantiation request FROM an approved catalog entry.
   *
   * Stability: beta.
   */
  postV1MCatalogEntriesByIdInstantiate(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/catalog/entries/{id}/instantiate", `/v1/m/catalog/entries/${encodeURIComponent(id)}/instantiate`, body, opts);
  }

  /**
   * POST /v1/m/catalog/entries/{id}/submit — Moves a draft entry to pending review.
   *
   * Stability: beta.
   */
  postV1MCatalogEntriesByIdSubmit(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/catalog/entries/{id}/submit", `/v1/m/catalog/entries/${encodeURIComponent(id)}/submit`, undefined, opts);
  }

  /**
   * GET /v1/m/catalog/entries/{id}/verify — Recomputes and verifies an approved entry's integrity (hash + signature).
   *
   * Stability: beta.
   */
  getV1MCatalogEntriesByIdVerify(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/catalog/entries/{id}/verify", `/v1/m/catalog/entries/${encodeURIComponent(id)}/verify`, undefined, opts);
  }

  /**
   * GET /v1/m/catalog/instances — Lists instantiations, optionally filtered by status/entry_id.
   *
   * Stability: beta.
   */
  getV1MCatalogInstances(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/catalog/instances", "/v1/m/catalog/instances", undefined, opts);
  }

  /**
   * GET /v1/m/catalog/instances/{id} — Returns one instance.
   *
   * Stability: beta.
   */
  getV1MCatalogInstancesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/catalog/instances/{id}", `/v1/m/catalog/instances/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/catalog/instances/{id}/transition — Records a governance decision on an instance (approve/ reject/activate).
   *
   * Stability: beta.
   */
  postV1MCatalogInstancesByIdTransition(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/catalog/instances/{id}/transition", `/v1/m/catalog/instances/${encodeURIComponent(id)}/transition`, body, opts);
  }

  /**
   * GET /v1/m/catalog/mcp-admission/policy — catalog module route (requires catalog:entry:read)
   *
   * Stability: beta.
   */
  getV1MCatalogMcpAdmissionPolicy(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/catalog/mcp-admission/policy", "/v1/m/catalog/mcp-admission/policy", undefined, opts);
  }

  /**
   * PUT /v1/m/catalog/mcp-admission/policy — Upserts the per-tenant trust root. Admin, audited.
   *
   * Stability: beta.
   */
  putV1MCatalogMcpAdmissionPolicy(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/catalog/mcp-admission/policy", "/v1/m/catalog/mcp-admission/policy", body, opts);
  }

  /**
   * GET /v1/m/catalog/mcp-admissions — Lists the recorded MCP-entry admission verdicts.
   *
   * Stability: beta.
   */
  getV1MCatalogMcpAdmissions(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/catalog/mcp-admissions", "/v1/m/catalog/mcp-admissions", undefined, opts);
  }

  /**
   * GET /v1/m/catalog/pubkey — Returns the catalog signing public key (for external verification), or reports that signing is not configured.
   *
   * Stability: beta.
   */
  getV1MCatalogPubkey(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/catalog/pubkey", "/v1/m/catalog/pubkey", undefined, opts);
  }

  /**
   * GET /v1/m/claude-agents/sessions/{id}/events — Serves a session's thread events from the wired events reader.
   *
   * Stability: beta.
   */
  getV1MClaudeAgentsSessionsByIdEvents(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/claude-agents/sessions/{id}/events", `/v1/m/claude-agents/sessions/${encodeURIComponent(id)}/events`, undefined, opts);
  }

  /**
   * POST /v1/m/claude-agents/sessions/{id}/tool-confirmation — Emits user.tool_confirmation, bound to the approval state machine and AUDITED with a redacted fingerprint.
   *
   * Stability: beta.
   */
  postV1MClaudeAgentsSessionsByIdToolConfirmation(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/claude-agents/sessions/{id}/tool-confirmation", `/v1/m/claude-agents/sessions/${encodeURIComponent(id)}/tool-confirmation`, body, opts);
  }

  /**
   * GET /v1/m/claude-policy/{surface}/artifact — Serves the latest (or ?revision=N) SIGNED artifact for a surface — the agent-pull half of the decided distribution mechanism.
   *
   * Stability: beta.
   */
  getV1MClaudePolicyBySurfaceArtifact(surface: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/claude-policy/{surface}/artifact", `/v1/m/claude-policy/${encodeURIComponent(surface)}/artifact`, undefined, opts);
  }

  /**
   * POST /v1/m/claude-policy/{surface}/checkin — Records a distribution agent's attested check-in: it verifies the echoed artifact hash/fingerprint against the distribution record (a mismatch is a HIGH drift finding — tampered or stale artifact), upserts the scope's OBSERVED config (credential-redacted), computes drift against the latest published revision, and audits the whole thing.
   *
   * Stability: beta.
   */
  postV1MClaudePolicyBySurfaceCheckin(surface: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/claude-policy/{surface}/checkin", `/v1/m/claude-policy/${encodeURIComponent(surface)}/checkin`, body, opts);
  }

  /**
   * GET /v1/m/claude-policy/{surface}/distribution — Serves the per-surface truth view (read tier): published vs signed vs observed, scope by scope — real state only, every absence named.
   *
   * Stability: beta.
   */
  getV1MClaudePolicyBySurfaceDistribution(surface: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/claude-policy/{surface}/distribution", `/v1/m/claude-policy/${encodeURIComponent(surface)}/distribution`, undefined, opts);
  }

  /**
   * POST /v1/m/claude-policy/{surface}/dry-run — Previews the resolved precedence/effect WITHOUT publishing or touching any host.
   *
   * Stability: beta.
   */
  postV1MClaudePolicyBySurfaceDryRun(surface: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/claude-policy/{surface}/dry-run", `/v1/m/claude-policy/${encodeURIComponent(surface)}/dry-run`, body, opts);
  }

  /**
   * POST /v1/m/claude-policy/{surface}/publish — Persists an immutable revision and runs PERMITTED-vs-OBSERVED drift.
   *
   * Stability: beta.
   */
  postV1MClaudePolicyBySurfacePublish(surface: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/claude-policy/{surface}/publish", `/v1/m/claude-policy/${encodeURIComponent(surface)}/publish`, body, opts);
  }

  /**
   * POST /v1/m/claude-policy/{surface}/validate — Validates a document server-side (defense in depth — the UI is never the security boundary).
   *
   * Stability: beta.
   */
  postV1MClaudePolicyBySurfaceValidate(surface: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/claude-policy/{surface}/validate", `/v1/m/claude-policy/${encodeURIComponent(surface)}/validate`, body, opts);
  }

  /**
   * GET /v1/m/claude-policy/{surface}/versions — Returns a surface's revision history (metadata only).
   *
   * Stability: beta.
   */
  getV1MClaudePolicyBySurfaceVersions(surface: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/claude-policy/{surface}/versions", `/v1/m/claude-policy/${encodeURIComponent(surface)}/versions`, undefined, opts);
  }

  /**
   * GET /v1/m/claude-policy/{surface}/versions/{revision} — Returns one revision with its content.
   *
   * Stability: beta.
   */
  getV1MClaudePolicyBySurfaceVersionsByRevision(surface: string, revision: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/claude-policy/{surface}/versions/{revision}", `/v1/m/claude-policy/${encodeURIComponent(surface)}/versions/${encodeURIComponent(revision)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/aims/pack — compliance module route (requires compliance:aims:read)
   *
   * Stability: beta.
   */
  getV1MComplianceAimsPack(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/aims/pack", "/v1/m/compliance/aims/pack", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/aims/pack — Structures operator-supplied organizational context + the live ISO 42001 assessment into a certification-readiness pack (deny-closed: 501 without a configured packager, 422 on a document the packager rejects), persists the pack (one per tenant, replace-on-regenerate) anchored to the ledger head, and self-audits.
   *
   * Stability: beta.
   */
  postV1MComplianceAimsPack(body: Uint8Array, opts?: RequestOptions): Promise<Json> {
    return this.doReqRawWithType("POST", "/v1/m/compliance/aims/pack", "/v1/m/compliance/aims/pack", body, "application/json", opts);
  }

  /**
   * GET /v1/m/compliance/aims/pack/{id} — compliance module route (requires compliance:aims:read)
   *
   * Stability: beta.
   */
  getV1MComplianceAimsPackById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/aims/pack/{id}", `/v1/m/compliance/aims/pack/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * DELETE /v1/m/compliance/aims/pack/{id} — Removes a maintained pack; admin-tier and self-audited.
   *
   * Stability: beta.
   */
  deleteV1MComplianceAimsPackById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/compliance/aims/pack/{id}", `/v1/m/compliance/aims/pack/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/aims/pack/{id}/export — compliance module route (requires compliance:aims:read)
   *
   * Stability: beta.
   */
  getV1MComplianceAimsPackByIdExport(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/aims/pack/{id}/export", `/v1/m/compliance/aims/pack/${encodeURIComponent(id)}/export`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/calendar — Returns the regulatory calendar + watchlist (read-tier; static verified data, not tenant evidence).
   *
   * Stability: beta.
   */
  getV1MComplianceCalendar(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/calendar", "/v1/m/compliance/calendar", undefined, opts);
  }

  /**
   * GET /v1/m/compliance/capabilities — Returns the capability catalog with the tenant's live evidence state — the "what the platform can evidence right now" map.
   *
   * Stability: beta.
   */
  getV1MComplianceCapabilities(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/capabilities", "/v1/m/compliance/capabilities", undefined, opts);
  }

  /**
   * GET /v1/m/compliance/claude-files — Serves the observe inventory (read-tier).
   *
   * Stability: beta.
   */
  getV1MComplianceClaudeFiles(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/claude-files", "/v1/m/compliance/claude-files", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/claude-files/{id}/erase — Serves the governed point delete (admin-tier; the dual-control gate + hold gate + receipt are inside eraseClaudeFile).
   *
   * Stability: beta.
   */
  postV1MComplianceClaudeFilesByIdErase(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/compliance/claude-files/{id}/erase", `/v1/m/compliance/claude-files/${encodeURIComponent(id)}/erase`, body, opts);
  }

  /**
   * POST /v1/m/compliance/data-subjects/{id}/erase — compliance module route (requires compliance:erasure:admin)
   *
   * Stability: beta.
   */
  postV1MComplianceDataSubjectsByIdErase(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/compliance/data-subjects/{id}/erase", `/v1/m/compliance/data-subjects/${encodeURIComponent(id)}/erase`, body, opts);
  }

  /**
   * GET /v1/m/compliance/data-subjects/{id}/erasure-status — compliance module route (requires compliance:erasure:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDataSubjectsByIdErasureStatus(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/data-subjects/{id}/erasure-status", `/v1/m/compliance/data-subjects/${encodeURIComponent(id)}/erasure-status`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/depth/ccm/drift — compliance module route (requires compliance:ccm:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDepthCcmDrift(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/depth/ccm/drift", "/v1/m/compliance/depth/ccm/drift", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/depth/ccm/drift — Compares two CCM snapshots (the two latest, or a specified snapshot_id and its predecessor) and persists per-control drift findings (deny-closed: 501 without the depth packager).
   *
   * Stability: beta.
   */
  postV1MComplianceDepthCcmDrift(opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/compliance/depth/ccm/drift", "/v1/m/compliance/depth/ccm/drift", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/depth/ccm/snapshot — Re-evaluates the assessment engine for a set of frameworks (or all catalog frameworks when none specified) at the current instant and persists the timestamped snapshot (deny-closed: 501 without the depth packager).
   *
   * Stability: beta.
   */
  postV1MComplianceDepthCcmSnapshot(body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/compliance/depth/ccm/snapshot", "/v1/m/compliance/depth/ccm/snapshot", body, opts);
  }

  /**
   * GET /v1/m/compliance/depth/ccm/snapshots — compliance module route (requires compliance:ccm:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDepthCcmSnapshots(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/depth/ccm/snapshots", "/v1/m/compliance/depth/ccm/snapshots", undefined, opts);
  }

  /**
   * GET /v1/m/compliance/depth/ccm/snapshots/{id} — compliance module route (requires compliance:ccm:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDepthCcmSnapshotsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/depth/ccm/snapshots/{id}", `/v1/m/compliance/depth/ccm/snapshots/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/depth/fedramp — compliance module route (requires compliance:depth:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDepthFedramp(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/depth/fedramp", "/v1/m/compliance/depth/fedramp", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/depth/fedramp — Structures the live assessment into FedRAMP 20x Key Security Indicators in OSCAL v1.1.3 format with DoD impact-level framing (deny-closed: 501 without the depth packager, 422 on reject).
   *
   * Stability: beta.
   */
  postV1MComplianceDepthFedramp(body: Uint8Array, opts?: RequestOptions): Promise<Json> {
    return this.doReqRawWithType("POST", "/v1/m/compliance/depth/fedramp", "/v1/m/compliance/depth/fedramp", body, "application/json", opts);
  }

  /**
   * GET /v1/m/compliance/depth/fedramp/{id} — compliance module route (requires compliance:depth:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDepthFedrampById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/depth/fedramp/{id}", `/v1/m/compliance/depth/fedramp/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * DELETE /v1/m/compliance/depth/fedramp/{id} — compliance module route (requires compliance:depth:admin)
   *
   * Stability: beta.
   */
  deleteV1MComplianceDepthFedrampById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/compliance/depth/fedramp/{id}", `/v1/m/compliance/depth/fedramp/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/depth/fedramp/{id}/export — Exports the maintained FedRAMP KSI document with a LIVE ledger integrity proof.
   *
   * Stability: beta.
   */
  getV1MComplianceDepthFedrampByIdExport(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/depth/fedramp/{id}/export", `/v1/m/compliance/depth/fedramp/${encodeURIComponent(id)}/export`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/depth/sector — compliance module route (requires compliance:depth:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDepthSector(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/depth/sector", "/v1/m/compliance/depth/sector", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/depth/sector — Structures operator-supplied sector context + the live assessment of sector-overlay frameworks into a compliance pack (deny-closed: 501 without the depth packager, 422 on reject), persists it (one per tenant, replace-on-regenerate) anchored to the ledger head, and self-audits.
   *
   * Stability: beta.
   */
  postV1MComplianceDepthSector(body: Uint8Array, opts?: RequestOptions): Promise<Json> {
    return this.doReqRawWithType("POST", "/v1/m/compliance/depth/sector", "/v1/m/compliance/depth/sector", body, "application/json", opts);
  }

  /**
   * GET /v1/m/compliance/depth/sector/{id} — compliance module route (requires compliance:depth:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDepthSectorById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/depth/sector/{id}", `/v1/m/compliance/depth/sector/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * DELETE /v1/m/compliance/depth/sector/{id} — compliance module route (requires compliance:depth:admin)
   *
   * Stability: beta.
   */
  deleteV1MComplianceDepthSectorById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/compliance/depth/sector/{id}", `/v1/m/compliance/depth/sector/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/depth/sector/{id}/export — Exports the maintained sector overlay pack with a LIVE ledger integrity proof.
   *
   * Stability: beta.
   */
  getV1MComplianceDepthSectorByIdExport(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/depth/sector/{id}/export", `/v1/m/compliance/depth/sector/${encodeURIComponent(id)}/export`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/depth/us-law — compliance module route (requires compliance:depth:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDepthUsLaw(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/depth/us-law", "/v1/m/compliance/depth/us-law", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/depth/us-law — Structures operator-supplied jurisdiction context + the live assessment of the 4 US state AI law frameworks into a compliance pack (deny-closed: 501 without a configured depth packager, 422 on input the packager rejects), persists it (one per tenant, replace-on-regenerate) anchored to the ledger head, and self-audits.
   *
   * Stability: beta.
   */
  postV1MComplianceDepthUsLaw(body: Uint8Array, opts?: RequestOptions): Promise<Json> {
    return this.doReqRawWithType("POST", "/v1/m/compliance/depth/us-law", "/v1/m/compliance/depth/us-law", body, "application/json", opts);
  }

  /**
   * GET /v1/m/compliance/depth/us-law/{id} — compliance module route (requires compliance:depth:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDepthUsLawById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/depth/us-law/{id}", `/v1/m/compliance/depth/us-law/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * DELETE /v1/m/compliance/depth/us-law/{id} — compliance module route (requires compliance:depth:admin)
   *
   * Stability: beta.
   */
  deleteV1MComplianceDepthUsLawById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/compliance/depth/us-law/{id}", `/v1/m/compliance/depth/us-law/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/depth/us-law/{id}/export — Exports the maintained US state law pack with a LIVE ledger integrity proof so the export proves the pack was anchored to a tamper-evident ledger.
   *
   * Stability: beta.
   */
  getV1MComplianceDepthUsLawByIdExport(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/depth/us-law/{id}/export", `/v1/m/compliance/depth/us-law/${encodeURIComponent(id)}/export`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/dora — Assembles and returns the DORA export.
   *
   * Stability: beta.
   */
  getV1MComplianceDora(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/dora", "/v1/m/compliance/dora", undefined, opts);
  }

  /**
   * GET /v1/m/compliance/dora/incidents — compliance module route (requires compliance:dora:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDoraIncidents(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/dora/incidents", "/v1/m/compliance/dora/incidents", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/dora/incidents — Applies the DORA major-incident criteria to operator-supplied impact data and persists the structured classification + report draft (one per tenant+reference, replace-on-reclassify so the report evolves initial → intermediate → final).
   *
   * Stability: beta.
   */
  postV1MComplianceDoraIncidents(body: Uint8Array, opts?: RequestOptions): Promise<Json> {
    return this.doReqRawWithType("POST", "/v1/m/compliance/dora/incidents", "/v1/m/compliance/dora/incidents", body, "application/json", opts);
  }

  /**
   * GET /v1/m/compliance/dora/incidents/{id} — compliance module route (requires compliance:dora:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDoraIncidentsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/dora/incidents/{id}", `/v1/m/compliance/dora/incidents/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * DELETE /v1/m/compliance/dora/incidents/{id} — Removes a stored classification; admin-tier and self-audited.
   *
   * Stability: beta.
   */
  deleteV1MComplianceDoraIncidentsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/compliance/dora/incidents/{id}", `/v1/m/compliance/dora/incidents/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/dora/incidents/{id}/report — Exports the structured major-incident report draft with a LIVE ledger integrity proof.
   *
   * Stability: beta.
   */
  getV1MComplianceDoraIncidentsByIdReport(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/dora/incidents/{id}/report", `/v1/m/compliance/dora/incidents/${encodeURIComponent(id)}/report`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/dora/register — compliance module route (requires compliance:dora:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDoraRegister(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/dora/register", "/v1/m/compliance/dora/register", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/dora/register — Ingests operator-supplied register data, structures it into the 2024/2956 Register of Information (deny-closed: 501 without a configured packager, 422 on a document the packager rejects), persists the maintained register (one per tenant+entity, replace-on-regenerate) anchored to the ledger head, and self-audits.
   *
   * Stability: beta.
   */
  postV1MComplianceDoraRegister(body: Uint8Array, opts?: RequestOptions): Promise<Json> {
    return this.doReqRawWithType("POST", "/v1/m/compliance/dora/register", "/v1/m/compliance/dora/register", body, "application/json", opts);
  }

  /**
   * GET /v1/m/compliance/dora/register/{id} — compliance module route (requires compliance:dora:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDoraRegisterById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/dora/register/{id}", `/v1/m/compliance/dora/register/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * DELETE /v1/m/compliance/dora/register/{id} — Removes a maintained register; admin-tier and self-audited (a register is a governance artifact).
   *
   * Stability: beta.
   */
  deleteV1MComplianceDoraRegisterById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/compliance/dora/register/{id}", `/v1/m/compliance/dora/register/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/dora/register/{id}/export — compliance module route (requires compliance:dora:read)
   *
   * Stability: beta.
   */
  getV1MComplianceDoraRegisterByIdExport(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/dora/register/{id}/export", `/v1/m/compliance/dora/register/${encodeURIComponent(id)}/export`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/erasure — compliance module route (requires compliance:erasure:read)
   *
   * Stability: beta.
   */
  getV1MComplianceErasure(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/erasure", "/v1/m/compliance/erasure", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/erasure — Registers a DSR: it mints (or reuses) the subject's key-ring row, tokenizes the subject reference, derives the affected §2 classes and seals the "received" custody event + self-audit in one transaction.
   *
   * Stability: beta.
   */
  postV1MComplianceErasure(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/compliance/erasure", "/v1/m/compliance/erasure", body, opts);
  }

  /**
   * GET /v1/m/compliance/erasure/{id} — compliance module route (requires compliance:erasure:read)
   *
   * Stability: beta.
   */
  getV1MComplianceErasureById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/erasure/{id}", `/v1/m/compliance/erasure/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/erasure/{id}/events — compliance module route (requires compliance:erasure:read)
   *
   * Stability: beta.
   */
  getV1MComplianceErasureByIdEvents(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/erasure/{id}/events", `/v1/m/compliance/erasure/${encodeURIComponent(id)}/events`, undefined, opts);
  }

  /**
   * POST /v1/m/compliance/erasure/{id}/execute — Runs the governed erasure for one request.
   *
   * Stability: beta.
   */
  postV1MComplianceErasureByIdExecute(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/compliance/erasure/{id}/execute", `/v1/m/compliance/erasure/${encodeURIComponent(id)}/execute`, body, opts);
  }

  /**
   * GET /v1/m/compliance/erasure/{id}/receipt — compliance module route (requires compliance:erasure:read)
   *
   * Stability: beta.
   */
  getV1MComplianceErasureByIdReceipt(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/erasure/{id}/receipt", `/v1/m/compliance/erasure/${encodeURIComponent(id)}/receipt`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/evidence — compliance module route (requires compliance:framework:read)
   *
   * Stability: beta.
   */
  getV1MComplianceEvidence(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/evidence", "/v1/m/compliance/evidence", undefined, opts);
  }

  /**
   * GET /v1/m/compliance/evidence/{id} — Returns a sealed package + its per-control results.
   *
   * Stability: beta.
   */
  getV1MComplianceEvidenceById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/evidence/{id}", `/v1/m/compliance/evidence/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/evidence/{id}/export — Exports a sealed package as an auditor-consumable dataset + manifest.
   *
   * Stability: beta.
   */
  getV1MComplianceEvidenceByIdExport(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/evidence/{id}/export", `/v1/m/compliance/evidence/${encodeURIComponent(id)}/export`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/frameworks — compliance module route (requires compliance:framework:read)
   *
   * Stability: beta.
   */
  getV1MComplianceFrameworks(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/frameworks", "/v1/m/compliance/frameworks", undefined, opts);
  }

  /**
   * GET /v1/m/compliance/frameworks/{id} — compliance module route (requires compliance:framework:read)
   *
   * Stability: beta.
   */
  getV1MComplianceFrameworksById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/frameworks/{id}", `/v1/m/compliance/frameworks/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/compliance/frameworks/{id}/evidence — Seals an immutable evidence package for a framework: it assesses the framework against the tenant's live evidence, records the ledger head + the live hash-chain verify result (the integrity proof), persists the package and its per-control results (append-only), and self-audits the seal — all in one transaction.
   *
   * Stability: beta.
   */
  postV1MComplianceFrameworksByIdEvidence(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/compliance/frameworks/{id}/evidence", `/v1/m/compliance/frameworks/${encodeURIComponent(id)}/evidence`, body, opts);
  }

  /**
   * GET /v1/m/compliance/frameworks/{id}/gaps — compliance module route (requires compliance:framework:read)
   *
   * Stability: beta.
   */
  getV1MComplianceFrameworksByIdGaps(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/frameworks/{id}/gaps", `/v1/m/compliance/frameworks/${encodeURIComponent(id)}/gaps`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/frameworks/{id}/status — compliance module route (requires compliance:framework:read)
   *
   * Stability: beta.
   */
  getV1MComplianceFrameworksByIdStatus(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/frameworks/{id}/status", `/v1/m/compliance/frameworks/${encodeURIComponent(id)}/status`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/hipaa/gap-report — compliance module route (requires compliance:framework:read)
   *
   * Stability: beta.
   */
  getV1MComplianceHipaaGapReport(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/hipaa/gap-report", "/v1/m/compliance/hipaa/gap-report", undefined, opts);
  }

  /**
   * GET /v1/m/compliance/holds — compliance module route (requires compliance:hold:read)
   *
   * Stability: beta.
   */
  getV1MComplianceHolds(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/holds", "/v1/m/compliance/holds", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/holds — Sets an ACTIVE hold immediately — no approval gate, because preservation is the safe direction and the duty to preserve admits no waiting (§4).
   *
   * Stability: beta.
   */
  postV1MComplianceHolds(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/compliance/holds", "/v1/m/compliance/holds", body, opts);
  }

  /**
   * GET /v1/m/compliance/holds/check — compliance module route (requires compliance:hold:read)
   *
   * Stability: beta.
   */
  getV1MComplianceHoldsCheck(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/holds/check", "/v1/m/compliance/holds/check", undefined, opts);
  }

  /**
   * GET /v1/m/compliance/holds/{id} — compliance module route (requires compliance:hold:read)
   *
   * Stability: beta.
   */
  getV1MComplianceHoldsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/holds/{id}", `/v1/m/compliance/holds/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/holds/{id}/events — compliance module route (requires compliance:hold:read)
   *
   * Stability: beta.
   */
  getV1MComplianceHoldsByIdEvents(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/holds/{id}/events", `/v1/m/compliance/holds/${encodeURIComponent(id)}/events`, undefined, opts);
  }

  /**
   * POST /v1/m/compliance/holds/{id}/release — compliance module route (requires compliance:hold:admin)
   *
   * Stability: beta.
   */
  postV1MComplianceHoldsByIdRelease(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/compliance/holds/{id}/release", `/v1/m/compliance/holds/${encodeURIComponent(id)}/release`, body, opts);
  }

  /**
   * GET /v1/m/compliance/nis2/incidents — compliance module route (requires compliance:nis2:read)
   *
   * Stability: beta.
   */
  getV1MComplianceNis2Incidents(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/nis2/incidents", "/v1/m/compliance/nis2/incidents", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/nis2/incidents/classify — compliance module route (requires compliance:nis2:admin)
   *
   * Stability: beta.
   */
  postV1MComplianceNis2IncidentsClassify(body: Uint8Array, opts?: RequestOptions): Promise<Json> {
    return this.doReqRawWithType("POST", "/v1/m/compliance/nis2/incidents/classify", "/v1/m/compliance/nis2/incidents/classify", body, "application/json", opts);
  }

  /**
   * GET /v1/m/compliance/nis2/incidents/{id} — compliance module route (requires compliance:nis2:read)
   *
   * Stability: beta.
   */
  getV1MComplianceNis2IncidentsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/nis2/incidents/{id}", `/v1/m/compliance/nis2/incidents/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/compliance/nis2/incidents/{id} — compliance module route (requires compliance:nis2:admin)
   *
   * Stability: beta.
   */
  putV1MComplianceNis2IncidentsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/compliance/nis2/incidents/{id}", `/v1/m/compliance/nis2/incidents/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/compliance/nis2/incidents/{id} — compliance module route (requires compliance:nis2:admin)
   *
   * Stability: beta.
   */
  deleteV1MComplianceNis2IncidentsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/compliance/nis2/incidents/{id}", `/v1/m/compliance/nis2/incidents/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/nis2/incidents/{id}/export — compliance module route (requires compliance:nis2:read)
   *
   * Stability: beta.
   */
  getV1MComplianceNis2IncidentsByIdExport(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/nis2/incidents/{id}/export", `/v1/m/compliance/nis2/incidents/${encodeURIComponent(id)}/export`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/oscal/profiles — compliance module route (requires compliance:oscal:read)
   *
   * Stability: beta.
   */
  getV1MComplianceOscalProfiles(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/oscal/profiles", "/v1/m/compliance/oscal/profiles", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/oscal/profiles — Ingests an operator-supplied OSCAL profile/catalog/SSP and registers the resolved control selection for a framework (one active selection per tenant+framework; re-registering replaces it).
   *
   * Stability: beta.
   */
  postV1MComplianceOscalProfiles(body: Uint8Array, opts?: RequestOptions): Promise<Json> {
    return this.doReqRawWithType("POST", "/v1/m/compliance/oscal/profiles", "/v1/m/compliance/oscal/profiles", body, "application/json", opts);
  }

  /**
   * GET /v1/m/compliance/oscal/profiles/{id} — compliance module route (requires compliance:oscal:read)
   *
   * Stability: beta.
   */
  getV1MComplianceOscalProfilesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/oscal/profiles/{id}", `/v1/m/compliance/oscal/profiles/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * DELETE /v1/m/compliance/oscal/profiles/{id} — Unregisters a profile — the export reverts to include-all for that framework.
   *
   * Stability: beta.
   */
  deleteV1MComplianceOscalProfilesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/compliance/oscal/profiles/{id}", `/v1/m/compliance/oscal/profiles/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/residency — compliance module route (requires compliance:residency:read)
   *
   * Stability: beta.
   */
  getV1MComplianceResidency(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/residency", "/v1/m/compliance/residency", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/residency — Records (or updates) a per-region residency attestation.
   *
   * Stability: beta.
   */
  postV1MComplianceResidency(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/compliance/residency", "/v1/m/compliance/residency", body, opts);
  }

  /**
   * POST /v1/m/compliance/residency/scan — Scans EXISTING egress signals against the self-hosted attestations and, where data left a self-hosted perimeter, records a violation, emits a core Finding + a bus signal, and updates the attestation.
   *
   * Stability: beta.
   */
  postV1MComplianceResidencyScan(opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/compliance/residency/scan", "/v1/m/compliance/residency/scan", undefined, opts);
  }

  /**
   * GET /v1/m/compliance/retention/classes — Returns the fixed §2 registry with the advisory recommendations and the §7 provider-floor annotation.
   *
   * Stability: beta.
   */
  getV1MComplianceRetentionClasses(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/retention/classes", "/v1/m/compliance/retention/classes", undefined, opts);
  }

  /**
   * GET /v1/m/compliance/retention/policies — compliance module route (requires compliance:retention:read)
   *
   * Stability: beta.
   */
  getV1MComplianceRetentionPolicies(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/retention/policies", "/v1/m/compliance/retention/policies", undefined, opts);
  }

  /**
   * PUT /v1/m/compliance/retention/policies/{class} — Upserts a per-class schedule.
   *
   * Stability: beta.
   */
  putV1MComplianceRetentionPoliciesByClass(class_: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/compliance/retention/policies/{class}", `/v1/m/compliance/retention/policies/${encodeURIComponent(class_)}`, body, opts);
  }

  /**
   * DELETE /v1/m/compliance/retention/policies/{class} — Removes a schedule.
   *
   * Stability: beta.
   */
  deleteV1MComplianceRetentionPoliciesByClass(class_: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/compliance/retention/policies/{class}", `/v1/m/compliance/retention/policies/${encodeURIComponent(class_)}`, undefined, opts);
  }

  /**
   * GET /v1/m/compliance/retention/runs — compliance module route (requires compliance:retention:read)
   *
   * Stability: beta.
   */
  getV1MComplianceRetentionRuns(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/retention/runs", "/v1/m/compliance/retention/runs", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/retention/sweep — Runs the tenant's sweep NOW (the same code the engine loop runs), attributed to the calling admin, and returns the summary.
   *
   * Stability: beta.
   */
  postV1MComplianceRetentionSweep(opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/compliance/retention/sweep", "/v1/m/compliance/retention/sweep", undefined, opts);
  }

  /**
   * GET /v1/m/compliance/risk — compliance module route (requires compliance:risk:read)
   *
   * Stability: beta.
   */
  getV1MComplianceRisk(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/risk", "/v1/m/compliance/risk", undefined, opts);
  }

  /**
   * POST /v1/m/compliance/risk/classify — Computes (or recomputes) a suggested risk tier for a subject from observed signals and persists it as a governed, audited classification.
   *
   * Stability: beta.
   */
  postV1MComplianceRiskClassify(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/compliance/risk/classify", "/v1/m/compliance/risk/classify", body, opts);
  }

  /**
   * POST /v1/m/compliance/risk/{id}/review — compliance module route (requires compliance:risk:admin)
   *
   * Stability: beta.
   */
  postV1MComplianceRiskByIdReview(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/compliance/risk/{id}/review", `/v1/m/compliance/risk/${encodeURIComponent(id)}/review`, body, opts);
  }

  /**
   * GET /v1/m/compliance/summary — Returns a cross-framework posture summary for the tenant (the org/ tenant roll-up).
   *
   * Stability: beta.
   */
  getV1MComplianceSummary(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/compliance/summary", "/v1/m/compliance/summary", undefined, opts);
  }

  /**
   * GET /v1/m/consoleviews/views — Returns the caller's own views plus the tenant's shared views, optionally scoped with ?feature_id=.
   *
   * Stability: beta.
   */
  getV1MConsoleviewsViews(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/consoleviews/views", "/v1/m/consoleviews/views", undefined, opts);
  }

  /**
   * POST /v1/m/consoleviews/views — Saves a new view owned by the caller.
   *
   * Stability: beta.
   */
  postV1MConsoleviewsViews(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/consoleviews/views", "/v1/m/consoleviews/views", body, opts);
  }

  /**
   * GET /v1/m/consoleviews/views/{id} — Returns one view.
   *
   * Stability: beta.
   */
  getV1MConsoleviewsViewsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/consoleviews/views/{id}", `/v1/m/consoleviews/views/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/consoleviews/views/{id} — Replaces the writable fields of the caller's OWN view.
   *
   * Stability: beta.
   */
  putV1MConsoleviewsViewsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/consoleviews/views/{id}", `/v1/m/consoleviews/views/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/consoleviews/views/{id} — Removes a view: the owner always may; a tenant admin/owner (or superadmin) may delete ANY view — the cleanup power for views left behind by departed users.
   *
   * Stability: beta.
   */
  deleteV1MConsoleviewsViewsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/consoleviews/views/{id}", `/v1/m/consoleviews/views/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/deploy/definitions — Lists definitions, optionally filtered by environment or desired_status.
   *
   * Stability: beta.
   */
  getV1MDeployDefinitions(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/deploy/definitions", "/v1/m/deploy/definitions", undefined, opts);
  }

  /**
   * POST /v1/m/deploy/definitions — Declares a new deployment definition and its first revision.
   *
   * Stability: beta.
   */
  postV1MDeployDefinitions(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/deploy/definitions", "/v1/m/deploy/definitions", body, opts);
  }

  /**
   * GET /v1/m/deploy/definitions/{id} — Returns one definition with its current desired spec and, when applied, the real-state snapshot from the core Deployment entity.
   *
   * Stability: beta.
   */
  getV1MDeployDefinitionsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/deploy/definitions/{id}", `/v1/m/deploy/definitions/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/deploy/definitions/{id} — Declares a new desired revision.
   *
   * Stability: beta.
   */
  putV1MDeployDefinitionsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/deploy/definitions/{id}", `/v1/m/deploy/definitions/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/deploy/definitions/{id} — deploy module route (requires deploy:deployment:write)
   *
   * Stability: beta.
   */
  deleteV1MDeployDefinitionsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/deploy/definitions/{id}", `/v1/m/deploy/definitions/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/deploy/definitions/{id}/apply — Reconciles the deployment to its desired spec — a GOVERNED INFRASTRUCTURE MUTATION.
   *
   * Stability: beta.
   */
  postV1MDeployDefinitionsByIdApply(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/deploy/definitions/{id}/apply", `/v1/m/deploy/definitions/${encodeURIComponent(id)}/apply`, body, opts);
  }

  /**
   * POST /v1/m/deploy/definitions/{id}/plan — deploy module route (requires deploy:deployment:write)
   *
   * Stability: beta.
   */
  postV1MDeployDefinitionsByIdPlan(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/deploy/definitions/{id}/plan", `/v1/m/deploy/definitions/${encodeURIComponent(id)}/plan`, undefined, opts);
  }

  /**
   * POST /v1/m/deploy/definitions/{id}/retire — Removes the deployment from the target — a GOVERNED MUTATION with the same two-phase, deny-by-default gating as apply.
   *
   * Stability: beta.
   */
  postV1MDeployDefinitionsByIdRetire(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/deploy/definitions/{id}/retire", `/v1/m/deploy/definitions/${encodeURIComponent(id)}/retire`, body, opts);
  }

  /**
   * GET /v1/m/deploy/definitions/{id}/revisions — Returns the append-only revision history of a definition — the rollback targets.
   *
   * Stability: beta.
   */
  getV1MDeployDefinitionsByIdRevisions(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/deploy/definitions/{id}/revisions", `/v1/m/deploy/definitions/${encodeURIComponent(id)}/revisions`, undefined, opts);
  }

  /**
   * POST /v1/m/deploy/definitions/{id}/rollback — Rolls the DESIRED state back to a prior revision by declaring a NEW revision whose spec equals that known-good version (forward-only, reversible history).
   *
   * Stability: beta.
   */
  postV1MDeployDefinitionsByIdRollback(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/deploy/definitions/{id}/rollback", `/v1/m/deploy/definitions/${encodeURIComponent(id)}/rollback`, body, opts);
  }

  /**
   * POST /v1/m/deploy/definitions/{id}/verify — Reconciles the real state against the desired spec and updates the Deployment snapshot's status (drift is reported, not mutated).
   *
   * Stability: beta.
   */
  postV1MDeployDefinitionsByIdVerify(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/deploy/definitions/{id}/verify", `/v1/m/deploy/definitions/${encodeURIComponent(id)}/verify`, undefined, opts);
  }

  /**
   * GET /v1/m/deploy/operations — Lists the append-only operation ledger, optionally filtered by definition_id, op or status.
   *
   * Stability: beta.
   */
  getV1MDeployOperations(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/deploy/operations", "/v1/m/deploy/operations", undefined, opts);
  }

  /**
   * GET /v1/m/deploy/wirings — Lists the declared PERMITTED wirings, optionally filtered by definition_id or status.
   *
   * Stability: beta.
   */
  getV1MDeployWirings(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/deploy/wirings", "/v1/m/deploy/wirings", undefined, opts);
  }

  /**
   * POST /v1/m/evals/ab — Loads the suite, scores both variants OUTSIDE the write transaction (two-phase — judge I/O never holds the store), optionally runs the order-swapped pairwise comparison, then persists both runs atomically.
   *
   * Stability: beta.
   */
  postV1MEvalsAb(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/evals/ab", "/v1/m/evals/ab", body, opts);
  }

  /**
   * POST /v1/m/evals/baselines — Find-or-creates the (suite, subject) baseline and points it at run_ref, recording the previous run_ref in the self-audit.
   *
   * Stability: beta.
   */
  postV1MEvalsBaselines(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/evals/baselines", "/v1/m/evals/baselines", body, opts);
  }

  /**
   * GET /v1/m/evals/calibration/items — Lists a set's reference items (?set=, default all sets).
   *
   * Stability: beta.
   */
  getV1MEvalsCalibrationItems(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/evals/calibration/items", "/v1/m/evals/calibration/items", undefined, opts);
  }

  /**
   * POST /v1/m/evals/calibration/items — Creates or RE-labels reference items (upsert by set+case_key: a correction is an audited update, the history lives in the ledger).
   *
   * Stability: beta.
   */
  postV1MEvalsCalibrationItems(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/evals/calibration/items", "/v1/m/evals/calibration/items", body, opts);
  }

  /**
   * GET /v1/m/evals/calibration/reports — Lists calibration reports (?set=, ?judge_model=), newest first per the occurred_at index order of the store's default listing.
   *
   * Stability: beta.
   */
  getV1MEvalsCalibrationReports(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/evals/calibration/reports", "/v1/m/evals/calibration/reports", undefined, opts);
  }

  /**
   * POST /v1/m/evals/calibration/run — Measures the wired judge against a labeled set.
   *
   * Stability: beta.
   */
  postV1MEvalsCalibrationRun(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/evals/calibration/run", "/v1/m/evals/calibration/run", body, opts);
  }

  /**
   * GET /v1/m/evals/gate — Lists gate evaluations (?suite_ref=, ?verdict=).
   *
   * Stability: beta.
   */
  getV1MEvalsGate(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/evals/gate", "/v1/m/evals/gate", undefined, opts);
  }

  /**
   * POST /v1/m/evals/gate — Evaluates the gate.
   *
   * Stability: beta.
   */
  postV1MEvalsGate(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/evals/gate", "/v1/m/evals/gate", body, opts);
  }

  /**
   * GET /v1/m/evals/gate/{id} — Returns one gate evaluation (CI re-checks it after an override).
   *
   * Stability: beta.
   */
  getV1MEvalsGateById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/evals/gate/{id}", `/v1/m/evals/gate/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/evals/gate/{id}/override — evals module route (requires evals:run:admin)
   *
   * Stability: beta.
   */
  postV1MEvalsGateByIdOverride(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/evals/gate/{id}/override", `/v1/m/evals/gate/${encodeURIComponent(id)}/override`, body, opts);
  }

  /**
   * POST /v1/m/evals/monitor — Samples sessions, scores their signals, persists one core EvalResult per sample and returns the aggregate.
   *
   * Stability: beta.
   */
  postV1MEvalsMonitor(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/evals/monitor", "/v1/m/evals/monitor", body, opts);
  }

  /**
   * GET /v1/m/evals/runs — Lists the tenant's runs (filterable by suite_ref/subject_ref).
   *
   * Stability: beta.
   */
  getV1MEvalsRuns(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/evals/runs", "/v1/m/evals/runs", undefined, opts);
  }

  /**
   * POST /v1/m/evals/runs — Scores a set of candidate outputs against a suite SYNCHRONOUSLY (the redteam pattern — no module goroutines) and returns 201 with the completed run + per-case.
   *
   * Stability: beta.
   */
  postV1MEvalsRuns(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/evals/runs", "/v1/m/evals/runs", body, opts);
  }

  /**
   * GET /v1/m/evals/runs/{id} — Returns one run with its per-case results attached.
   *
   * Stability: beta.
   */
  getV1MEvalsRunsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/evals/runs/{id}", `/v1/m/evals/runs/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/evals/runs/{id}/results — Lists one run's per-case results.
   *
   * Stability: beta.
   */
  getV1MEvalsRunsByIdResults(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/evals/runs/{id}/results", `/v1/m/evals/runs/${encodeURIComponent(id)}/results`, undefined, opts);
  }

  /**
   * GET /v1/m/evals/runs/{id}/stream — Streams a completed run's per-case results, summary and a done sentinel, then closes.
   *
   * Stability: beta.
   */
  getV1MEvalsRunsByIdStream(id: string, opts?: RequestOptions): Promise<string> {
    return this.doRaw("GET", "/v1/m/evals/runs/{id}/stream", `/v1/m/evals/runs/${encodeURIComponent(id)}/stream`, opts);
  }

  /**
   * GET /v1/m/evals/scorecards — Aggregates the tenant's runs into scorecards.
   *
   * Stability: beta.
   */
  getV1MEvalsScorecards(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/evals/scorecards", "/v1/m/evals/scorecards", undefined, opts);
  }

  /**
   * GET /v1/m/evals/suites — Lists the tenant's suites (filterable by name/status).
   *
   * Stability: beta.
   */
  getV1MEvalsSuites(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/evals/suites", "/v1/m/evals/suites", undefined, opts);
  }

  /**
   * POST /v1/m/evals/suites — Creates a versioned golden suite.
   *
   * Stability: beta.
   */
  postV1MEvalsSuites(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/evals/suites", "/v1/m/evals/suites", body, opts);
  }

  /**
   * GET /v1/m/evals/suites/{id} — Returns one suite.
   *
   * Stability: beta.
   */
  getV1MEvalsSuitesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/evals/suites/{id}", `/v1/m/evals/suites/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/evals/suites/{id}/archive — Marks a suite archived.
   *
   * Stability: beta.
   */
  postV1MEvalsSuitesByIdArchive(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/evals/suites/{id}/archive", `/v1/m/evals/suites/${encodeURIComponent(id)}/archive`, undefined, opts);
  }

  /**
   * GET /v1/m/evals/suites/{id}/cases — Lists a suite's cases (ordered by case_key for a stable view).
   *
   * Stability: beta.
   */
  getV1MEvalsSuitesByIdCases(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/evals/suites/{id}/cases", `/v1/m/evals/suites/${encodeURIComponent(id)}/cases`, undefined, opts);
  }

  /**
   * POST /v1/m/evals/suites/{id}/cases — Appends a golden case to a suite (append-only — a fix is a new suite_version).
   *
   * Stability: beta.
   */
  postV1MEvalsSuitesByIdCases(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/evals/suites/{id}/cases", `/v1/m/evals/suites/${encodeURIComponent(id)}/cases`, body, opts);
  }

  /**
   * GET /v1/m/eventing/dead-letters — eventing module route (requires eventing:delivery:read)
   *
   * Stability: beta.
   */
  getV1MEventingDeadLetters(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/eventing/dead-letters", "/v1/m/eventing/dead-letters", undefined, opts);
  }

  /**
   * GET /v1/m/eventing/deliveries — Lists delivery state, optionally filtered by ?subscription, ?status (status=dead is the DLQ view), ?origin (live|replay) and ?event_type.
   *
   * Stability: beta.
   */
  getV1MEventingDeliveries(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/eventing/deliveries", "/v1/m/eventing/deliveries", undefined, opts);
  }

  /**
   * POST /v1/m/eventing/deliveries/{id}/redeliver — Requeues ONE delivery that already reached a terminal state (delivered, dead or denied — the DLQ drain primitive).
   *
   * Stability: beta.
   */
  postV1MEventingDeliveriesByIdRedeliver(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/eventing/deliveries/{id}/redeliver", `/v1/m/eventing/deliveries/${encodeURIComponent(id)}/redeliver`, undefined, opts);
  }

  /**
   * GET /v1/m/eventing/egress-policy — Reports whether a destination policy is in force here, and what this deployment's disposition for the control is.
   *
   * Stability: beta.
   */
  getV1MEventingEgressPolicy(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/eventing/egress-policy", "/v1/m/eventing/egress-policy", undefined, opts);
  }

  /**
   * POST /v1/m/eventing/egress-policy/check — Answers "would this destination be permitted", without creating anything.
   *
   * Stability: beta.
   */
  postV1MEventingEgressPolicyCheck(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/eventing/egress-policy/check", "/v1/m/eventing/egress-policy/check", body, opts);
  }

  /**
   * GET /v1/m/eventing/egress-policy/compat — eventing module route (requires eventing:subscription:admin)
   *
   * Stability: beta.
   */
  getV1MEventingEgressPolicyCompat(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/eventing/egress-policy/compat", "/v1/m/eventing/egress-policy/compat", undefined, opts);
  }

  /**
   * GET /v1/m/eventing/event-types — Returns the public event-type catalog: the subscribable types, their stability tier and the permission gating receipt.
   *
   * Stability: beta.
   */
  getV1MEventingEventTypes(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/eventing/event-types", "/v1/m/eventing/event-types", undefined, opts);
  }

  /**
   * GET /v1/m/eventing/events — Lists the tenant's captured event log from a seq cursor — the pull-side companion of replay (find your cursor, inspect what a delivery carried).
   *
   * Stability: beta.
   */
  getV1MEventingEvents(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/eventing/events", "/v1/m/eventing/events", undefined, opts);
  }

  /**
   * GET /v1/m/eventing/subscriptions — Lists the tenant's subscriptions.
   *
   * Stability: beta.
   */
  getV1MEventingSubscriptions(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/eventing/subscriptions", "/v1/m/eventing/subscriptions", undefined, opts);
  }

  /**
   * POST /v1/m/eventing/subscriptions — Creates a subscription.
   *
   * Stability: beta.
   */
  postV1MEventingSubscriptions(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/eventing/subscriptions", "/v1/m/eventing/subscriptions", body, opts);
  }

  /**
   * GET /v1/m/eventing/subscriptions/{id} — Returns one subscription (never its secret).
   *
   * Stability: beta.
   */
  getV1MEventingSubscriptionsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/eventing/subscriptions/{id}", `/v1/m/eventing/subscriptions/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/eventing/subscriptions/{id} — Updates a subscription in place.
   *
   * Stability: beta.
   */
  putV1MEventingSubscriptionsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/eventing/subscriptions/{id}", `/v1/m/eventing/subscriptions/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/eventing/subscriptions/{id} — Removes a subscription.
   *
   * Stability: beta.
   */
  deleteV1MEventingSubscriptionsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/eventing/subscriptions/{id}", `/v1/m/eventing/subscriptions/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/eventing/subscriptions/{id}/replay — Re-enqueues deliveries for the subscription from a seq cursor: every retained event with seq >= from_seq (and <= to_seq, if set) that matches the subscription's type list and source filter gets a NEW queued delivery row (origin=replay).
   *
   * Stability: beta.
   */
  postV1MEventingSubscriptionsByIdReplay(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/eventing/subscriptions/{id}/replay", `/v1/m/eventing/subscriptions/${encodeURIComponent(id)}/replay`, body, opts);
  }

  /**
   * POST /v1/m/eventing/subscriptions/{id}/restore — Re-applies the delivery shape of an earlier revision to an EXISTING subscription.
   *
   * Stability: beta.
   */
  postV1MEventingSubscriptionsByIdRestore(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/eventing/subscriptions/{id}/restore", `/v1/m/eventing/subscriptions/${encodeURIComponent(id)}/restore`, body, opts);
  }

  /**
   * GET /v1/m/eventing/subscriptions/{id}/revisions — Lists a subscription's revision ledger, keyset-paginated by the time-ordered row id (chronological by ingestion, the decision-ledger convention).
   *
   * Stability: beta.
   */
  getV1MEventingSubscriptionsByIdRevisions(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/eventing/subscriptions/{id}/revisions", `/v1/m/eventing/subscriptions/${encodeURIComponent(id)}/revisions`, undefined, opts);
  }

  /**
   * POST /v1/m/eventing/subscriptions/{id}/rotate-auth — Replaces the subscription's auth credential, returning the old hint (the new cleartext is never returned — it is supplied by the caller).
   *
   * Stability: beta.
   */
  postV1MEventingSubscriptionsByIdRotateAuth(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/eventing/subscriptions/{id}/rotate-auth", `/v1/m/eventing/subscriptions/${encodeURIComponent(id)}/rotate-auth`, body, opts);
  }

  /**
   * POST /v1/m/eventing/subscriptions/{id}/rotate-secret — Replaces the subscription's signing secret, returning the new cleartext exactly once.
   *
   * Stability: beta.
   */
  postV1MEventingSubscriptionsByIdRotateSecret(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/eventing/subscriptions/{id}/rotate-secret", `/v1/m/eventing/subscriptions/${encodeURIComponent(id)}/rotate-secret`, undefined, opts);
  }

  /**
   * POST /v1/m/eventing/subscriptions/{id}/test — Sends ONE synthetic "eventing.test" delivery to the subscription's endpoint, synchronously, signed with its real secret — the integrator's "is my receiver wired correctly?" probe.
   *
   * Stability: beta.
   */
  postV1MEventingSubscriptionsByIdTest(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/eventing/subscriptions/{id}/test", `/v1/m/eventing/subscriptions/${encodeURIComponent(id)}/test`, undefined, opts);
  }

  /**
   * GET /v1/m/finops/alerts — finops module route (requires finops:budget:read)
   *
   * Stability: beta.
   */
  getV1MFinopsAlerts(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/alerts", "/v1/m/finops/alerts", undefined, opts);
  }

  /**
   * GET /v1/m/finops/analytics/team-summary — Returns team-level cost aggregation with project/model breakdown and a per-calendar-day trend series for a fixed period (7d/30d/90d).
   *
   * Stability: beta.
   */
  getV1MFinopsAnalyticsTeamSummary(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/analytics/team-summary", "/v1/m/finops/analytics/team-summary", undefined, opts);
  }

  /**
   * GET /v1/m/finops/budgets — finops module route (requires finops:budget:read)
   *
   * Stability: beta.
   */
  getV1MFinopsBudgets(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/budgets", "/v1/m/finops/budgets", undefined, opts);
  }

  /**
   * POST /v1/m/finops/budgets — finops module route (requires finops:budget:write)
   *
   * Stability: beta.
   */
  postV1MFinopsBudgets(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/finops/budgets", "/v1/m/finops/budgets", body, opts);
  }

  /**
   * GET /v1/m/finops/budgets/{id} — finops module route (requires finops:budget:read)
   *
   * Stability: beta.
   */
  getV1MFinopsBudgetsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/budgets/{id}", `/v1/m/finops/budgets/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/finops/budgets/{id} — finops module route (requires finops:budget:write)
   *
   * Stability: beta.
   */
  putV1MFinopsBudgetsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/finops/budgets/{id}", `/v1/m/finops/budgets/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/finops/budgets/{id} — finops module route (requires finops:budget:write)
   *
   * Stability: beta.
   */
  deleteV1MFinopsBudgetsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/finops/budgets/{id}", `/v1/m/finops/budgets/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/finops/budgets/{id}/status — finops module route (requires finops:budget:read)
   *
   * Stability: beta.
   */
  getV1MFinopsBudgetsByIdStatus(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/budgets/{id}/status", `/v1/m/finops/budgets/${encodeURIComponent(id)}/status`, undefined, opts);
  }

  /**
   * GET /v1/m/finops/comparison — Serves GET /comparison — the model cost comparison endpoint.
   *
   * Stability: beta.
   */
  getV1MFinopsComparison(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/comparison", "/v1/m/finops/comparison", undefined, opts);
  }

  /**
   * POST /v1/m/finops/cost — Ingests one CostSample over HTTP (the Anthropic cost_report or a live runtime-cost sample) through the SAME canonical onCost path the bus uses — so it shares the natural-key dedup, the attribution resolution and the single-provenance CostRecord ledger; it never opens a second divergent path.
   *
   * Stability: beta.
   */
  postV1MFinopsCost(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/finops/cost", "/v1/m/finops/cost", body, opts);
  }

  /**
   * GET /v1/m/finops/cost-centers — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsCostCenters(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/cost-centers", "/v1/m/finops/cost-centers", undefined, opts);
  }

  /**
   * POST /v1/m/finops/cost-centers — finops module route (requires finops:budget:write)
   *
   * Stability: beta.
   */
  postV1MFinopsCostCenters(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/finops/cost-centers", "/v1/m/finops/cost-centers", body, opts);
  }

  /**
   * GET /v1/m/finops/cost-centers/{id} — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsCostCentersById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/cost-centers/{id}", `/v1/m/finops/cost-centers/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/finops/cost-centers/{id} — finops module route (requires finops:budget:write)
   *
   * Stability: beta.
   */
  putV1MFinopsCostCentersById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/finops/cost-centers/{id}", `/v1/m/finops/cost-centers/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/finops/cost-centers/{id} — finops module route (requires finops:budget:write)
   *
   * Stability: beta.
   */
  deleteV1MFinopsCostCentersById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/finops/cost-centers/{id}", `/v1/m/finops/cost-centers/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/finops/cost-centers/{id}/mappings — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsCostCentersByIdMappings(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/cost-centers/{id}/mappings", `/v1/m/finops/cost-centers/${encodeURIComponent(id)}/mappings`, undefined, opts);
  }

  /**
   * POST /v1/m/finops/cost-centers/{id}/mappings — finops module route (requires finops:budget:write)
   *
   * Stability: beta.
   */
  postV1MFinopsCostCentersByIdMappings(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/finops/cost-centers/{id}/mappings", `/v1/m/finops/cost-centers/${encodeURIComponent(id)}/mappings`, body, opts);
  }

  /**
   * DELETE /v1/m/finops/cost-centers/{id}/mappings/{mid} — finops module route (requires finops:budget:write)
   *
   * Stability: beta.
   */
  deleteV1MFinopsCostCentersByIdMappingsByMid(id: string, mid: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/finops/cost-centers/{id}/mappings/{mid}", `/v1/m/finops/cost-centers/${encodeURIComponent(id)}/mappings/${encodeURIComponent(mid)}`, undefined, opts);
  }

  /**
   * GET /v1/m/finops/forecast — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsForecast(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/forecast", "/v1/m/finops/forecast", undefined, opts);
  }

  /**
   * GET /v1/m/finops/model-rates — Lists rate catalog entries with optional provider/model filters, sorted by effective_from descending (most recent first).
   *
   * Stability: beta.
   */
  getV1MFinopsModelRates(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/model-rates", "/v1/m/finops/model-rates", undefined, opts);
  }

  /**
   * POST /v1/m/finops/model-rates — Creates a new rate catalog entry after validating input and checking uniqueness of the (provider, model, effective_from) tuple.
   *
   * Stability: beta.
   */
  postV1MFinopsModelRates(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/finops/model-rates", "/v1/m/finops/model-rates", body, opts);
  }

  /**
   * GET /v1/m/finops/model-rates/{id} — Returns a single rate catalog entry by ID.
   *
   * Stability: beta.
   */
  getV1MFinopsModelRatesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/model-rates/{id}", `/v1/m/finops/model-rates/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/finops/model-rates/{id} — Replaces a rate catalog entry by ID.
   *
   * Stability: beta.
   */
  putV1MFinopsModelRatesById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/finops/model-rates/{id}", `/v1/m/finops/model-rates/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/finops/model-rates/{id} — Removes a rate catalog entry by ID.
   *
   * Stability: beta.
   */
  deleteV1MFinopsModelRatesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/finops/model-rates/{id}", `/v1/m/finops/model-rates/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/finops/outcomes — Lists graded outcomes, optionally filtered by subject.
   *
   * Stability: beta.
   */
  getV1MFinopsOutcomes(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/outcomes", "/v1/m/finops/outcomes", undefined, opts);
  }

  /**
   * POST /v1/m/finops/outcomes — Ingests one graded outcome over HTTP (the operator/automation bridge), auditing the principal's privileged write atomically with its effect.
   *
   * Stability: beta.
   */
  postV1MFinopsOutcomes(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/finops/outcomes", "/v1/m/finops/outcomes", body, opts);
  }

  /**
   * GET /v1/m/finops/recommendations — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsRecommendations(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/recommendations", "/v1/m/finops/recommendations", undefined, opts);
  }

  /**
   * POST /v1/m/finops/seats — Upserts one provider/day seat snapshot (202 Accepted: a re-posted day replaces its values — a snapshot, never additive).
   *
   * Stability: beta.
   */
  postV1MFinopsSeats(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/finops/seats", "/v1/m/finops/seats", body, opts);
  }

  /**
   * GET /v1/m/finops/seats/utilization — Joins the seat denominators with the per-day distinct active actors for a provider over an inclusive [from, to] day range.
   *
   * Stability: beta.
   */
  getV1MFinopsSeatsUtilization(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/seats/utilization", "/v1/m/finops/seats/utilization", undefined, opts);
  }

  /**
   * GET /v1/m/finops/spend — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsSpend(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/spend", "/v1/m/finops/spend", undefined, opts);
  }

  /**
   * GET /v1/m/finops/spend/allocation — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsSpendAllocation(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/spend/allocation", "/v1/m/finops/spend/allocation", undefined, opts);
  }

  /**
   * GET /v1/m/finops/spend/export — Streams the cost read-model as a FOCUS-conformant CSV.
   *
   * Stability: beta.
   */
  getV1MFinopsSpendExport(opts?: RequestOptions): Promise<string> {
    return this.doRaw("GET", "/v1/m/finops/spend/export", "/v1/m/finops/spend/export", opts);
  }

  /**
   * GET /v1/m/finops/spend/reconciliation — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsSpendReconciliation(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/spend/reconciliation", "/v1/m/finops/spend/reconciliation", undefined, opts);
  }

  /**
   * GET /v1/m/finops/spend/summary — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsSpendSummary(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/spend/summary", "/v1/m/finops/spend/summary", undefined, opts);
  }

  /**
   * GET /v1/m/finops/spend/trend — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsSpendTrend(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/spend/trend", "/v1/m/finops/spend/trend", undefined, opts);
  }

  /**
   * GET /v1/m/finops/spend/unified — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsSpendUnified(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/spend/unified", "/v1/m/finops/spend/unified", undefined, opts);
  }

  /**
   * GET /v1/m/finops/statements — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsStatements(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/statements", "/v1/m/finops/statements", undefined, opts);
  }

  /**
   * POST /v1/m/finops/statements/generate — finops module route (requires finops:budget:write)
   *
   * Stability: beta.
   */
  postV1MFinopsStatementsGenerate(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/finops/statements/generate", "/v1/m/finops/statements/generate", body, opts);
  }

  /**
   * GET /v1/m/finops/statements/{id} — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsStatementsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/statements/{id}", `/v1/m/finops/statements/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/finops/statements/{id}/export — finops module route (requires finops:spend:read)
   *
   * Stability: beta.
   */
  getV1MFinopsStatementsByIdExport(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/statements/{id}/export", `/v1/m/finops/statements/${encodeURIComponent(id)}/export`, undefined, opts);
  }

  /**
   * GET /v1/m/finops/value — Serves the cost-per-outcome breakdown by ?dimension (agent|identity| session, default agent) over the standard since/until window.
   *
   * Stability: beta.
   */
  getV1MFinopsValue(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/value", "/v1/m/finops/value", undefined, opts);
  }

  /**
   * GET /v1/m/finops/value/summary — Serves the CFO panel (totals + cancellation-risk list).
   *
   * Stability: beta.
   */
  getV1MFinopsValueSummary(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/finops/value/summary", "/v1/m/finops/value/summary", undefined, opts);
  }

  /**
   * GET /v1/m/governance/agent-risk-profiles — Lists profiles, optionally filtered by tier.
   *
   * Stability: beta.
   */
  getV1MGovernanceAgentRiskProfiles(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/agent-risk-profiles", "/v1/m/governance/agent-risk-profiles", undefined, opts);
  }

  /**
   * POST /v1/m/governance/agent-risk-profiles/classify — Computes (or recomputes) the suggested risk tier for an agent from observed signals.
   *
   * Stability: beta.
   */
  postV1MGovernanceAgentRiskProfilesClassify(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/agent-risk-profiles/classify", "/v1/m/governance/agent-risk-profiles/classify", body, opts);
  }

  /**
   * GET /v1/m/governance/agent-risk-profiles/{id} — Returns one profile.
   *
   * Stability: beta.
   */
  getV1MGovernanceAgentRiskProfilesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/agent-risk-profiles/{id}", `/v1/m/governance/agent-risk-profiles/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/governance/agent-risk-profiles/{id}/review — Marks a classification as human-reviewed.
   *
   * Stability: beta.
   */
  postV1MGovernanceAgentRiskProfilesByIdReview(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/governance/agent-risk-profiles/{id}/review", `/v1/m/governance/agent-risk-profiles/${encodeURIComponent(id)}/review`, undefined, opts);
  }

  /**
   * PUT /v1/m/governance/agent-risk-profiles/{id}/tier — Sets the operator's authoritative tier for an agent.
   *
   * Stability: beta.
   */
  putV1MGovernanceAgentRiskProfilesByIdTier(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/governance/agent-risk-profiles/{id}/tier", `/v1/m/governance/agent-risk-profiles/${encodeURIComponent(id)}/tier`, body, opts);
  }

  /**
   * POST /v1/m/governance/agentcore-export/apply — governance module route (requires governance:agentcore-export:admin)
   *
   * Stability: beta.
   */
  postV1MGovernanceAgentcoreExportApply(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/agentcore-export/apply", "/v1/m/governance/agentcore-export/apply", body, opts);
  }

  /**
   * POST /v1/m/governance/agentcore-export/plan — governance module route (requires governance:agentcore-export:admin)
   *
   * Stability: beta.
   */
  postV1MGovernanceAgentcoreExportPlan(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/agentcore-export/plan", "/v1/m/governance/agentcore-export/plan", body, opts);
  }

  /**
   * POST /v1/m/governance/agents — Creates a lifecycle row with kind=agent and mandatory sponsor.
   *
   * Stability: beta.
   */
  postV1MGovernanceAgents(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/agents", "/v1/m/governance/agents", body, opts);
  }

  /**
   * POST /v1/m/governance/agents/{agentID}/identity — Binds an agent to its NHI identity (Agent.IdentityID).
   *
   * Stability: beta.
   */
  postV1MGovernanceAgentsByAgentidIdentity(agentid: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/agents/{agentID}/identity", `/v1/m/governance/agents/${encodeURIComponent(agentid)}/identity`, body, opts);
  }

  /**
   * DELETE /v1/m/governance/agents/{agentID}/identity — Clears an agent's identity binding.
   *
   * Stability: beta.
   */
  deleteV1MGovernanceAgentsByAgentidIdentity(agentid: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/governance/agents/{agentID}/identity", `/v1/m/governance/agents/${encodeURIComponent(agentid)}/identity`, undefined, opts);
  }

  /**
   * GET /v1/m/governance/approvals — Lists requests, optionally filtered by status/action.
   *
   * Stability: beta.
   */
  getV1MGovernanceApprovals(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/approvals", "/v1/m/governance/approvals", undefined, opts);
  }

  /**
   * POST /v1/m/governance/approvals — Opens an approval request.
   *
   * Stability: beta.
   */
  postV1MGovernanceApprovals(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/approvals", "/v1/m/governance/approvals", body, opts);
  }

  /**
   * POST /v1/m/governance/approvals/sweep — Materializes expiry and escalation for the tenant's pending approvals.
   *
   * Stability: beta.
   */
  postV1MGovernanceApprovalsSweep(opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/governance/approvals/sweep", "/v1/m/governance/approvals/sweep", undefined, opts);
  }

  /**
   * GET /v1/m/governance/approvals/{id} — Returns one request with its effective status.
   *
   * Stability: beta.
   */
  getV1MGovernanceApprovalsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/approvals/{id}", `/v1/m/governance/approvals/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/governance/approvals/{id}/cancel — Cancels a pending request.
   *
   * Stability: beta.
   */
  postV1MGovernanceApprovalsByIdCancel(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/governance/approvals/{id}/cancel", `/v1/m/governance/approvals/${encodeURIComponent(id)}/cancel`, undefined, opts);
  }

  /**
   * POST /v1/m/governance/approvals/{id}/consume — governance module route (requires governance:approval:write)
   *
   * Stability: beta.
   */
  postV1MGovernanceApprovalsByIdConsume(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/approvals/{id}/consume", `/v1/m/governance/approvals/${encodeURIComponent(id)}/consume`, body, opts);
  }

  /**
   * GET /v1/m/governance/approvals/{id}/decisions — Returns the immutable decision trail for a request — the action→human traceability view.
   *
   * Stability: beta.
   */
  getV1MGovernanceApprovalsByIdDecisions(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/approvals/{id}/decisions", `/v1/m/governance/approvals/${encodeURIComponent(id)}/decisions`, undefined, opts);
  }

  /**
   * POST /v1/m/governance/approvals/{id}/decisions — Records one human decision.
   *
   * Stability: beta.
   */
  postV1MGovernanceApprovalsByIdDecisions(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/approvals/{id}/decisions", `/v1/m/governance/approvals/${encodeURIComponent(id)}/decisions`, body, opts);
  }

  /**
   * GET /v1/m/governance/bindings — Lists agent↔identity bindings, flagging identities shared across agents.
   *
   * Stability: beta.
   */
  getV1MGovernanceBindings(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/bindings", "/v1/m/governance/bindings", undefined, opts);
  }

  /**
   * GET /v1/m/governance/breakglass — Lists grants, optionally filtered by stored status; a grant past its expiry reads as "expired" in its DTO regardless.
   *
   * Stability: beta.
   */
  getV1MGovernanceBreakglass(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/breakglass", "/v1/m/governance/breakglass", undefined, opts);
  }

  /**
   * POST /v1/m/governance/breakglass — Opens an emergency window.
   *
   * Stability: beta.
   */
  postV1MGovernanceBreakglass(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/breakglass", "/v1/m/governance/breakglass", body, opts);
  }

  /**
   * POST /v1/m/governance/breakglass/consume — governance module route (requires governance:breakglass:write)
   *
   * Stability: beta.
   */
  postV1MGovernanceBreakglassConsume(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/breakglass/consume", "/v1/m/governance/breakglass/consume", body, opts);
  }

  /**
   * GET /v1/m/governance/breakglass/{id} — Returns one grant with its effective status.
   *
   * Stability: beta.
   */
  getV1MGovernanceBreakglassById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/breakglass/{id}", `/v1/m/governance/breakglass/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/governance/breakglass/{id}/review — Records the FORCED post-review of a terminal grant.
   *
   * Stability: beta.
   */
  postV1MGovernanceBreakglassByIdReview(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/breakglass/{id}/review", `/v1/m/governance/breakglass/${encodeURIComponent(id)}/review`, body, opts);
  }

  /**
   * POST /v1/m/governance/breakglass/{id}/revoke — Closes an active grant early.
   *
   * Stability: beta.
   */
  postV1MGovernanceBreakglassByIdRevoke(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/governance/breakglass/{id}/revoke", `/v1/m/governance/breakglass/${encodeURIComponent(id)}/revoke`, undefined, opts);
  }

  /**
   * GET /v1/m/governance/breakglass/{id}/uses — Returns the immutable use trail for a grant — what actually proceeded under the emergency window (the post-review's evidence).
   *
   * Stability: beta.
   */
  getV1MGovernanceBreakglassByIdUses(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/breakglass/{id}/uses", `/v1/m/governance/breakglass/${encodeURIComponent(id)}/uses`, undefined, opts);
  }

  /**
   * GET /v1/m/governance/emerging-identity-standards — Surfaces the design-toward registry read-only, with an explicit disclaimer that it is tracked, not implemented (IDN-12).
   *
   * Stability: beta.
   */
  getV1MGovernanceEmergingIdentityStandards(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/emerging-identity-standards", "/v1/m/governance/emerging-identity-standards", undefined, opts);
  }

  /**
   * GET /v1/m/governance/groups — Lists the reconciled collections (groups/roles/policies).
   *
   * Stability: beta.
   */
  getV1MGovernanceGroups(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/groups", "/v1/m/governance/groups", undefined, opts);
  }

  /**
   * GET /v1/m/governance/groups/{ref}/members — Lists a group's members.
   *
   * Stability: beta.
   */
  getV1MGovernanceGroupsByRefMembers(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/groups/{ref}/members", `/v1/m/governance/groups/${encodeURIComponent(ref)}/members`, undefined, opts);
  }

  /**
   * GET /v1/m/governance/guardian/actions — Lists the containment trail, optionally by status.
   *
   * Stability: beta.
   */
  getV1MGovernanceGuardianActions(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/guardian/actions", "/v1/m/governance/guardian/actions", undefined, opts);
  }

  /**
   * GET /v1/m/governance/guardian/rules — governance module route (requires governance:guardian:read)
   *
   * Stability: beta.
   */
  getV1MGovernanceGuardianRules(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/guardian/rules", "/v1/m/governance/guardian/rules", undefined, opts);
  }

  /**
   * POST /v1/m/governance/guardian/rules — Authors a containment rule.
   *
   * Stability: beta.
   */
  postV1MGovernanceGuardianRules(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/guardian/rules", "/v1/m/governance/guardian/rules", body, opts);
  }

  /**
   * PUT /v1/m/governance/guardian/rules/{id} — Updates a rule's mutable fields (enable/disable, matching, action, mode, note).
   *
   * Stability: beta.
   */
  putV1MGovernanceGuardianRulesById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/governance/guardian/rules/{id}", `/v1/m/governance/guardian/rules/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/governance/guardian/rules/{id} — Deletes a rule only after terminalizing every queued action that could otherwise execute from its denormalized action row.
   *
   * Stability: beta.
   */
  deleteV1MGovernanceGuardianRulesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/governance/guardian/rules/{id}", `/v1/m/governance/guardian/rules/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/governance/identities — Lists the reconciled identity roster.
   *
   * Stability: beta.
   */
  getV1MGovernanceIdentities(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/identities", "/v1/m/governance/identities", undefined, opts);
  }

  /**
   * GET /v1/m/governance/killswitch — Lists stop rows, optionally filtered by stored status — the persisted, visible stop state (who, when, scope, reason).
   *
   * Stability: beta.
   */
  getV1MGovernanceKillswitch(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/killswitch", "/v1/m/governance/killswitch", undefined, opts);
  }

  /**
   * POST /v1/m/governance/killswitch — governance module route (requires governance:killswitch:admin)
   *
   * Stability: beta.
   */
  postV1MGovernanceKillswitch(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/killswitch", "/v1/m/governance/killswitch", body, opts);
  }

  /**
   * GET /v1/m/governance/killswitch/state — Returns the live stop posture for the tenant.
   *
   * Stability: beta.
   */
  getV1MGovernanceKillswitchState(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/killswitch/state", "/v1/m/governance/killswitch/state", undefined, opts);
  }

  /**
   * GET /v1/m/governance/killswitch/{id} — Returns one stop row.
   *
   * Stability: beta.
   */
  getV1MGovernanceKillswitchById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/killswitch/{id}", `/v1/m/governance/killswitch/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/governance/killswitch/{id}/evidence — Builds and returns the incident evidence pack.
   *
   * Stability: beta.
   */
  getV1MGovernanceKillswitchByIdEvidence(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/killswitch/{id}/evidence", `/v1/m/governance/killswitch/${encodeURIComponent(id)}/evidence`, undefined, opts);
  }

  /**
   * POST /v1/m/governance/killswitch/{id}/reenable — Lifts a stop — NEVER unilaterally.
   *
   * Stability: beta.
   */
  postV1MGovernanceKillswitchByIdReenable(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/killswitch/{id}/reenable", `/v1/m/governance/killswitch/${encodeURIComponent(id)}/reenable`, body, opts);
  }

  /**
   * POST /v1/m/governance/killswitch/{id}/review — Records the FORCED post-review of a re-enabled stop: a real human DIFFERENT from the engager, the re-enable requester and the re-enabler (separation of duties — nobody signs off their own incident), once, note mandatory.
   *
   * Stability: beta.
   */
  postV1MGovernanceKillswitchByIdReview(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/killswitch/{id}/review", `/v1/m/governance/killswitch/${encodeURIComponent(id)}/review`, body, opts);
  }

  /**
   * GET /v1/m/governance/nhi — Lists the NHI lifecycle rows, optionally filtered by enforcement state or offboard state.
   *
   * Stability: beta.
   */
  getV1MGovernanceNhi(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/nhi", "/v1/m/governance/nhi", undefined, opts);
  }

  /**
   * GET /v1/m/governance/nhi/posture — Aggregates the lifecycle posture.
   *
   * Stability: beta.
   */
  getV1MGovernanceNhiPosture(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/nhi/posture", "/v1/m/governance/nhi/posture", undefined, opts);
  }

  /**
   * POST /v1/m/governance/nhi/sweep — Materializes staleness/escalation, orphan and unsponsored state for the tenant's NHI lifecycle rows, and ensures a row exists for every roster NHI.
   *
   * Stability: beta.
   */
  postV1MGovernanceNhiSweep(opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/governance/nhi/sweep", "/v1/m/governance/nhi/sweep", undefined, opts);
  }

  /**
   * GET /v1/m/governance/nhi/{ref} — Returns one NHI lifecycle row by identity_ref.
   *
   * Stability: beta.
   */
  getV1MGovernanceNhiByRef(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/nhi/{ref}", `/v1/m/governance/nhi/${encodeURIComponent(ref)}`, undefined, opts);
  }

  /**
   * GET /v1/m/governance/nhi/{ref}/events — Lists the append-only lifecycle event trail for one NHI.
   *
   * Stability: beta.
   */
  getV1MGovernanceNhiByRefEvents(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/nhi/{ref}/events", `/v1/m/governance/nhi/${encodeURIComponent(ref)}/events`, undefined, opts);
  }

  /**
   * POST /v1/m/governance/nhi/{ref}/offboard — Runs the reversible soft-delete step of a governed offboarding: it blocks the NHI in-product (enforcement=blocked, the cascade that denies every bound agent at the PEP), opens an audited recovery window, and best-effort disables the credential at the source.
   *
   * Stability: beta.
   */
  postV1MGovernanceNhiByRefOffboard(ref: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/governance/nhi/{ref}/offboard", `/v1/m/governance/nhi/${encodeURIComponent(ref)}/offboard`, body, opts);
  }

  /**
   * POST /v1/m/governance/nhi/{ref}/offboard/finalize — governance module route (requires governance:nhi:admin)
   *
   * Stability: beta.
   */
  postV1MGovernanceNhiByRefOffboardFinalize(ref: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/governance/nhi/{ref}/offboard/finalize", `/v1/m/governance/nhi/${encodeURIComponent(ref)}/offboard/finalize`, body, opts);
  }

  /**
   * PUT /v1/m/governance/nhi/{ref}/ownership — Assigns owner/sponsor.
   *
   * Stability: beta.
   */
  putV1MGovernanceNhiByRefOwnership(ref: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/governance/nhi/{ref}/ownership", `/v1/m/governance/nhi/${encodeURIComponent(ref)}/ownership`, body, opts);
  }

  /**
   * PUT /v1/m/governance/nhi/{ref}/policy — Authors the per-NHI rotation policy.
   *
   * Stability: beta.
   */
  putV1MGovernanceNhiByRefPolicy(ref: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/governance/nhi/{ref}/policy", `/v1/m/governance/nhi/${encodeURIComponent(ref)}/policy`, body, opts);
  }

  /**
   * POST /v1/m/governance/nhi/{ref}/restore — Reverses a soft-delete within the recovery window: best-effort re-enable at the source, clear the in-product block.
   *
   * Stability: beta.
   */
  postV1MGovernanceNhiByRefRestore(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/governance/nhi/{ref}/restore", `/v1/m/governance/nhi/${encodeURIComponent(ref)}/restore`, undefined, opts);
  }

  /**
   * POST /v1/m/governance/nhi/{ref}/rotate — Orchestrates a governed rotation: it opens the CRITICAL approval (two-person floor via the engine), and only on an approved/break-glass decision invokes the wired actuator, returning the minted credential ONCE.
   *
   * Stability: beta.
   */
  postV1MGovernanceNhiByRefRotate(ref: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/governance/nhi/{ref}/rotate", `/v1/m/governance/nhi/${encodeURIComponent(ref)}/rotate`, body, opts);
  }

  /**
   * GET /v1/m/governance/pdp/active — Reports what the STORE currently selects for an engine, and — for cedar — discloses the other surfaces that are unioned into the enforced policy.
   *
   * Stability: beta.
   */
  getV1MGovernancePdpActive(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/pdp/active", "/v1/m/governance/pdp/active", undefined, opts);
  }

  /**
   * POST /v1/m/governance/pdp/dry-run — governance module route (requires governance:policy:read)
   *
   * Stability: beta.
   */
  postV1MGovernancePdpDryRun(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/pdp/dry-run", "/v1/m/governance/pdp/dry-run", body, opts);
  }

  /**
   * POST /v1/m/governance/pdp/explain — Evaluates an example request against a CANDIDATE source and returns the three-valued decision chain: a matched permit GRANTS within scope, a matched forbid RESTRICTS, neither abstains (the RBAC decision stands).
   *
   * Stability: beta.
   */
  postV1MGovernancePdpExplain(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/pdp/explain", "/v1/m/governance/pdp/explain", body, opts);
  }

  /**
   * POST /v1/m/governance/pdp/publish — Persists a versioned Cedar/OPA policy and, for Cedar, ACTIVATES it on the live hot path (recomposes the per-tenant overlay).
   *
   * Stability: beta.
   */
  postV1MGovernancePdpPublish(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/pdp/publish", "/v1/m/governance/pdp/publish", body, opts);
  }

  /**
   * POST /v1/m/governance/pdp/rollback — Re-activates an existing immutable revision by appending an activation record.
   *
   * Stability: beta.
   */
  postV1MGovernancePdpRollback(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/pdp/rollback", "/v1/m/governance/pdp/rollback", body, opts);
  }

  /**
   * GET /v1/m/governance/pdp/tests — governance module route (requires governance:policy:read)
   *
   * Stability: beta.
   */
  getV1MGovernancePdpTests(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/pdp/tests", "/v1/m/governance/pdp/tests", undefined, opts);
  }

  /**
   * POST /v1/m/governance/pdp/validate — Compiles a source WITHOUT loading it into the live evaluator.
   *
   * Stability: beta.
   */
  postV1MGovernancePdpValidate(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/pdp/validate", "/v1/m/governance/pdp/validate", body, opts);
  }

  /**
   * GET /v1/m/governance/pdp/versions — Lists the cedar + opa authored revisions (the shared revision store, kinds cedar/opa).
   *
   * Stability: beta.
   */
  getV1MGovernancePdpVersions(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/pdp/versions", "/v1/m/governance/pdp/versions", undefined, opts);
  }

  /**
   * GET /v1/m/governance/pdp/versions/{revision} — governance module route (requires governance:policy:read)
   *
   * Stability: beta.
   */
  getV1MGovernancePdpVersionsByRevision(revision: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/pdp/versions/{revision}", `/v1/m/governance/pdp/versions/${encodeURIComponent(revision)}`, undefined, opts);
  }

  /**
   * GET /v1/m/governance/policies — Lists governance policies, optionally filtered by kind/enabled.
   *
   * Stability: beta.
   */
  getV1MGovernancePolicies(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/policies", "/v1/m/governance/policies", undefined, opts);
  }

  /**
   * POST /v1/m/governance/policies — Authors a governance policy.
   *
   * Stability: beta.
   */
  postV1MGovernancePolicies(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/policies", "/v1/m/governance/policies", body, opts);
  }

  /**
   * GET /v1/m/governance/policies/{id} — Returns one governance policy.
   *
   * Stability: beta.
   */
  getV1MGovernancePoliciesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/policies/{id}", `/v1/m/governance/policies/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/governance/policies/{id} — Updates a governance policy in place (kind is immutable).
   *
   * Stability: beta.
   */
  putV1MGovernancePoliciesById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/governance/policies/{id}", `/v1/m/governance/policies/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/governance/policies/{id} — Deletes a governance policy.
   *
   * Stability: beta.
   */
  deleteV1MGovernancePoliciesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/governance/policies/{id}", `/v1/m/governance/policies/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/governance/rbac/catalog — governance module route (requires governance:rbac:read)
   *
   * Stability: beta.
   */
  getV1MGovernanceRbacCatalog(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/rbac/catalog", "/v1/m/governance/rbac/catalog", undefined, opts);
  }

  /**
   * GET /v1/m/governance/rbac/delegation-authority — governance module route (requires governance:rbac:read)
   *
   * Stability: beta.
   */
  getV1MGovernanceRbacDelegationAuthority(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/rbac/delegation-authority", "/v1/m/governance/rbac/delegation-authority", undefined, opts);
  }

  /**
   * GET /v1/m/governance/rbac/grants — governance module route (requires governance:rbac:read)
   *
   * Stability: beta.
   */
  getV1MGovernanceRbacGrants(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/rbac/grants", "/v1/m/governance/rbac/grants", undefined, opts);
  }

  /**
   * POST /v1/m/governance/rbac/grants — governance module route (requires governance:rbac:admin)
   *
   * Stability: beta.
   */
  postV1MGovernanceRbacGrants(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/rbac/grants", "/v1/m/governance/rbac/grants", body, opts);
  }

  /**
   * GET /v1/m/governance/rbac/grants/{id} — governance module route (requires governance:rbac:read)
   *
   * Stability: beta.
   */
  getV1MGovernanceRbacGrantsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/rbac/grants/{id}", `/v1/m/governance/rbac/grants/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * DELETE /v1/m/governance/rbac/grants/{id} — governance module route (requires governance:rbac:admin)
   *
   * Stability: beta.
   */
  deleteV1MGovernanceRbacGrantsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/governance/rbac/grants/{id}", `/v1/m/governance/rbac/grants/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/governance/rbac/permission-groups — governance module route (requires governance:rbac:read)
   *
   * Stability: beta.
   */
  getV1MGovernanceRbacPermissionGroups(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/rbac/permission-groups", "/v1/m/governance/rbac/permission-groups", undefined, opts);
  }

  /**
   * POST /v1/m/governance/rbac/permission-groups — governance module route (requires governance:rbac:admin)
   *
   * Stability: beta.
   */
  postV1MGovernanceRbacPermissionGroups(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/rbac/permission-groups", "/v1/m/governance/rbac/permission-groups", body, opts);
  }

  /**
   * GET /v1/m/governance/rbac/permission-groups/{name} — governance module route (requires governance:rbac:read)
   *
   * Stability: beta.
   */
  getV1MGovernanceRbacPermissionGroupsByName(name: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/rbac/permission-groups/{name}", `/v1/m/governance/rbac/permission-groups/${encodeURIComponent(name)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/governance/rbac/permission-groups/{name} — governance module route (requires governance:rbac:admin)
   *
   * Stability: beta.
   */
  putV1MGovernanceRbacPermissionGroupsByName(name: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/governance/rbac/permission-groups/{name}", `/v1/m/governance/rbac/permission-groups/${encodeURIComponent(name)}`, body, opts);
  }

  /**
   * DELETE /v1/m/governance/rbac/permission-groups/{name} — governance module route (requires governance:rbac:admin)
   *
   * Stability: beta.
   */
  deleteV1MGovernanceRbacPermissionGroupsByName(name: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/governance/rbac/permission-groups/{name}", `/v1/m/governance/rbac/permission-groups/${encodeURIComponent(name)}`, undefined, opts);
  }

  /**
   * GET /v1/m/governance/rbac/roles — governance module route (requires governance:rbac:read)
   *
   * Stability: beta.
   */
  getV1MGovernanceRbacRoles(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/rbac/roles", "/v1/m/governance/rbac/roles", undefined, opts);
  }

  /**
   * POST /v1/m/governance/rbac/roles — governance module route (requires governance:rbac:admin)
   *
   * Stability: beta.
   */
  postV1MGovernanceRbacRoles(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/rbac/roles", "/v1/m/governance/rbac/roles", body, opts);
  }

  /**
   * GET /v1/m/governance/rbac/roles/{name} — governance module route (requires governance:rbac:read)
   *
   * Stability: beta.
   */
  getV1MGovernanceRbacRolesByName(name: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/rbac/roles/{name}", `/v1/m/governance/rbac/roles/${encodeURIComponent(name)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/governance/rbac/roles/{name} — governance module route (requires governance:rbac:admin)
   *
   * Stability: beta.
   */
  putV1MGovernanceRbacRolesByName(name: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/governance/rbac/roles/{name}", `/v1/m/governance/rbac/roles/${encodeURIComponent(name)}`, body, opts);
  }

  /**
   * DELETE /v1/m/governance/rbac/roles/{name} — governance module route (requires governance:rbac:admin)
   *
   * Stability: beta.
   */
  deleteV1MGovernanceRbacRolesByName(name: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/governance/rbac/roles/{name}", `/v1/m/governance/rbac/roles/${encodeURIComponent(name)}`, undefined, opts);
  }

  /**
   * POST /v1/m/governance/roster/sync — Triggers a roster reconciliation for the resolved tenant from the identity providers the composition root wired (UseRosterProviders).
   *
   * Stability: beta.
   */
  postV1MGovernanceRosterSync(opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/governance/roster/sync", "/v1/m/governance/roster/sync", undefined, opts);
  }

  /**
   * GET /v1/m/governance/routine-policies — Lists routine policies, optionally filtered by scope_kind or enabled.
   *
   * Stability: beta.
   */
  getV1MGovernanceRoutinePolicies(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/routine-policies", "/v1/m/governance/routine-policies", undefined, opts);
  }

  /**
   * POST /v1/m/governance/routine-policies — Creates a routine policy.
   *
   * Stability: beta.
   */
  postV1MGovernanceRoutinePolicies(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/governance/routine-policies", "/v1/m/governance/routine-policies", body, opts);
  }

  /**
   * GET /v1/m/governance/routine-policies/posture — Returns the routine governance posture: EVERY policy in the tenant (enabled or not, with the two counters splitting them), plus the COMPOSED decision for one resolution scope.
   *
   * Stability: beta.
   */
  getV1MGovernanceRoutinePoliciesPosture(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/routine-policies/posture", "/v1/m/governance/routine-policies/posture", undefined, opts);
  }

  /**
   * GET /v1/m/governance/routine-policies/{id} — Returns one routine policy by ID.
   *
   * Stability: beta.
   */
  getV1MGovernanceRoutinePoliciesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/governance/routine-policies/{id}", `/v1/m/governance/routine-policies/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/governance/routine-policies/{id} — Updates a routine policy's mutable fields.
   *
   * Stability: beta.
   */
  putV1MGovernanceRoutinePoliciesById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/governance/routine-policies/{id}", `/v1/m/governance/routine-policies/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/governance/routine-policies/{id} — Deletes a routine policy.
   *
   * Stability: beta.
   */
  deleteV1MGovernanceRoutinePoliciesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/governance/routine-policies/{id}", `/v1/m/governance/routine-policies/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/health/checks — Lists declared checks, optionally filtered by ?subject_kind and ?desired_status.
   *
   * Stability: beta.
   */
  getV1MHealthChecks(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/health/checks", "/v1/m/health/checks", undefined, opts);
  }

  /**
   * POST /v1/m/health/checks — Declares a monitored subject.
   *
   * Stability: beta.
   */
  postV1MHealthChecks(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/health/checks", "/v1/m/health/checks", body, opts);
  }

  /**
   * GET /v1/m/health/checks/{id} — Returns one check projected as status.
   *
   * Stability: beta.
   */
  getV1MHealthChecksById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/health/checks/{id}", `/v1/m/health/checks/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/health/checks/{id} — Patches a check's configuration.
   *
   * Stability: beta.
   */
  putV1MHealthChecksById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/health/checks/{id}", `/v1/m/health/checks/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/health/checks/{id} — Removes a check (hard delete).
   *
   * Stability: beta.
   */
  deleteV1MHealthChecksById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/health/checks/{id}", `/v1/m/health/checks/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/health/checks/{id}/report — Ingests an active probe result for a check and folds it into the subject's health.
   *
   * Stability: beta.
   */
  postV1MHealthChecksByIdReport(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/health/checks/{id}/report", `/v1/m/health/checks/${encodeURIComponent(id)}/report`, body, opts);
  }

  /**
   * GET /v1/m/health/dependencies — Returns the dependency map as a React-Flow node+edge graph: a page of auto-discovered dependency edges, plus the deduplicated node set derived from them, each node annotated with its subject's health.
   *
   * Stability: beta.
   */
  getV1MHealthDependencies(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/health/dependencies", "/v1/m/health/dependencies", undefined, opts);
  }

  /**
   * GET /v1/m/health/events — Lists the append-only reliability transition ledger, optionally filtered by ?subject_kind and ?subject_ref.
   *
   * Stability: beta.
   */
  getV1MHealthEvents(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/health/events", "/v1/m/health/events", undefined, opts);
  }

  /**
   * GET /v1/m/health/incidents — Lists incidents, optionally filtered by ?state (open| resolved), ?subject_kind and ?subject_ref.
   *
   * Stability: beta.
   */
  getV1MHealthIncidents(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/health/incidents", "/v1/m/health/incidents", undefined, opts);
  }

  /**
   * GET /v1/m/health/incidents/{id} — Returns one incident.
   *
   * Stability: beta.
   */
  getV1MHealthIncidentsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/health/incidents/{id}", `/v1/m/health/incidents/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/health/incidents/{id}/resolve — health module route (requires health:check:admin)
   *
   * Stability: beta.
   */
  postV1MHealthIncidentsByIdResolve(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/health/incidents/{id}/resolve", `/v1/m/health/incidents/${encodeURIComponent(id)}/resolve`, undefined, opts);
  }

  /**
   * GET /v1/m/health/sla — Reports the reliability of a subject over a trailing window (?subject_kind=&subject_ref=&window_seconds=).
   *
   * Stability: beta.
   */
  getV1MHealthSla(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/health/sla", "/v1/m/health/sla", undefined, opts);
  }

  /**
   * GET /v1/m/health/status — Lists the current health of monitored subjects (the check rows projected as status), optionally filtered by ?state and ?subject_kind.
   *
   * Stability: beta.
   */
  getV1MHealthStatus(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/health/status", "/v1/m/health/status", undefined, opts);
  }

  /**
   * GET /v1/m/health/stream — Serves live health changes as server-sent events, pinned to the request's single authorized tenant so a client only ever sees its own tenant's subjects.
   *
   * Stability: beta.
   */
  getV1MHealthStream(opts?: RequestOptions): Promise<string> {
    return this.doRaw("GET", "/v1/m/health/stream", "/v1/m/health/stream", opts);
  }

  /**
   * GET /v1/m/identity/external-keys — Serves the read-only CMEK inventory (ANT2-04).
   *
   * Stability: beta.
   */
  getV1MIdentityExternalKeys(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/identity/external-keys", "/v1/m/identity/external-keys", undefined, opts);
  }

  /**
   * GET /v1/m/identity/residency — Serves the read-only workspace governance object (ANT2-06).
   *
   * Stability: beta.
   */
  getV1MIdentityResidency(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/identity/residency", "/v1/m/identity/residency", undefined, opts);
  }

  /**
   * GET /v1/m/identity/sso — Serves the SSO connection state.
   *
   * Stability: beta.
   */
  getV1MIdentitySso(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/identity/sso", "/v1/m/identity/sso", undefined, opts);
  }

  /**
   * GET /v1/m/identity/wif — Serves the declared WIF object graph.
   *
   * Stability: beta.
   */
  getV1MIdentityWif(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/identity/wif", "/v1/m/identity/wif", undefined, opts);
  }

  /**
   * GET /v1/m/inferenceproxy/config — Returns the tenant's proxy config (the safe defaults when none is set).
   *
   * Stability: beta.
   */
  getV1MInferenceproxyConfig(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/inferenceproxy/config", "/v1/m/inferenceproxy/config", undefined, opts);
  }

  /**
   * PUT /v1/m/inferenceproxy/config — Upserts the tenant's singleton config (admin-tier; self-audited).
   *
   * Stability: beta.
   */
  putV1MInferenceproxyConfig(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/inferenceproxy/config", "/v1/m/inferenceproxy/config", body, opts);
  }

  /**
   * POST /v1/m/inferenceproxy/device/approve — inferenceproxy module route (requires inferenceproxy:config:admin)
   *
   * Stability: beta.
   */
  postV1MInferenceproxyDeviceApprove(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/inferenceproxy/device/approve", "/v1/m/inferenceproxy/device/approve", body, opts);
  }

  /**
   * GET /v1/m/inferenceproxy/dlp/rules — Lists the tenant's inference-egress DLP rules.
   *
   * Stability: beta.
   */
  getV1MInferenceproxyDlpRules(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/inferenceproxy/dlp/rules", "/v1/m/inferenceproxy/dlp/rules", undefined, opts);
  }

  /**
   * PUT /v1/m/inferenceproxy/dlp/rules — Upserts one DLP rule by class (admin-tier: authorizing egress is privileged governance; self-audited).
   *
   * Stability: beta.
   */
  putV1MInferenceproxyDlpRules(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/inferenceproxy/dlp/rules", "/v1/m/inferenceproxy/dlp/rules", body, opts);
  }

  /**
   * DELETE /v1/m/inferenceproxy/dlp/rules/{id} — Removes one DLP rule by id (admin-tier; self-audited).
   *
   * Stability: beta.
   */
  deleteV1MInferenceproxyDlpRulesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/inferenceproxy/dlp/rules/{id}", `/v1/m/inferenceproxy/dlp/rules/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/inventory/entities — Lists catalog entries, optionally filtered by kind and status, paginated by the default id keyset cursor.
   *
   * Stability: beta.
   */
  getV1MInventoryEntities(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/inventory/entities", "/v1/m/inventory/entities", undefined, opts);
  }

  /**
   * GET /v1/m/inventory/entities/{kind}/{id} — Returns one catalog entry plus a minimal projection of the core entity it overlays.
   *
   * Stability: beta.
   */
  getV1MInventoryEntitiesByKindById(kind: string, id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/inventory/entities/{kind}/{id}", `/v1/m/inventory/entities/${encodeURIComponent(kind)}/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/inventory/summary — Returns the estate overview: counts by entity kind and by signal source.
   *
   * Stability: beta.
   */
  getV1MInventorySummary(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/inventory/summary", "/v1/m/inventory/summary", undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/context-policies — Lists context policies, optionally filtered by scope.
   *
   * Stability: beta.
   */
  getV1MKnowledgeContextPolicies(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/context-policies", "/v1/m/knowledge/context-policies", undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/context-policies — Upserts a context/compaction policy by (scope_kind, scope_ref).
   *
   * Stability: beta.
   */
  postV1MKnowledgeContextPolicies(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/knowledge/context-policies", "/v1/m/knowledge/context-policies", body, opts);
  }

  /**
   * GET /v1/m/knowledge/data-products — knowledge module route (requires knowledge:data_product:read)
   *
   * Stability: beta.
   */
  getV1MKnowledgeDataProducts(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/data-products", "/v1/m/knowledge/data-products", undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/data-products — knowledge module route (requires knowledge:data_product:write)
   *
   * Stability: beta.
   */
  postV1MKnowledgeDataProducts(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/knowledge/data-products", "/v1/m/knowledge/data-products", body, opts);
  }

  /**
   * GET /v1/m/knowledge/data-products/{id} — knowledge module route (requires knowledge:data_product:read)
   *
   * Stability: beta.
   */
  getV1MKnowledgeDataProductsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/data-products/{id}", `/v1/m/knowledge/data-products/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/knowledge/data-products/{id} — knowledge module route (requires knowledge:data_product:write)
   *
   * Stability: beta.
   */
  putV1MKnowledgeDataProductsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/knowledge/data-products/{id}", `/v1/m/knowledge/data-products/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/knowledge/data-products/{id} — knowledge module route (requires knowledge:data_product:admin)
   *
   * Stability: beta.
   */
  deleteV1MKnowledgeDataProductsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/knowledge/data-products/{id}", `/v1/m/knowledge/data-products/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/data-products/{id}/archive — knowledge module route (requires knowledge:data_product:admin)
   *
   * Stability: beta.
   */
  postV1MKnowledgeDataProductsByIdArchive(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/knowledge/data-products/{id}/archive", `/v1/m/knowledge/data-products/${encodeURIComponent(id)}/archive`, undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/data-products/{id}/contracts — knowledge module route (requires knowledge:data_product:read)
   *
   * Stability: beta.
   */
  getV1MKnowledgeDataProductsByIdContracts(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/data-products/{id}/contracts", `/v1/m/knowledge/data-products/${encodeURIComponent(id)}/contracts`, undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/data-products/{id}/contracts — knowledge module route (requires knowledge:data_product:write)
   *
   * Stability: beta.
   */
  postV1MKnowledgeDataProductsByIdContracts(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/knowledge/data-products/{id}/contracts", `/v1/m/knowledge/data-products/${encodeURIComponent(id)}/contracts`, body, opts);
  }

  /**
   * GET /v1/m/knowledge/data-products/{id}/contracts/active — knowledge module route (requires knowledge:data_product:read)
   *
   * Stability: beta.
   */
  getV1MKnowledgeDataProductsByIdContractsActive(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/data-products/{id}/contracts/active", `/v1/m/knowledge/data-products/${encodeURIComponent(id)}/contracts/active`, undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/data-products/{id}/contracts/{ver} — knowledge module route (requires knowledge:data_product:read)
   *
   * Stability: beta.
   */
  getV1MKnowledgeDataProductsByIdContractsByVer(id: string, ver: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/data-products/{id}/contracts/{ver}", `/v1/m/knowledge/data-products/${encodeURIComponent(id)}/contracts/${encodeURIComponent(ver)}`, undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/data-products/{id}/deprecate — knowledge module route (requires knowledge:data_product:write)
   *
   * Stability: beta.
   */
  postV1MKnowledgeDataProductsByIdDeprecate(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/knowledge/data-products/{id}/deprecate", `/v1/m/knowledge/data-products/${encodeURIComponent(id)}/deprecate`, undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/data-products/{id}/events — knowledge module route (requires knowledge:data_product:read)
   *
   * Stability: beta.
   */
  getV1MKnowledgeDataProductsByIdEvents(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/data-products/{id}/events", `/v1/m/knowledge/data-products/${encodeURIComponent(id)}/events`, undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/data-products/{id}/health — knowledge module route (requires knowledge:data_product:read)
   *
   * Stability: beta.
   */
  getV1MKnowledgeDataProductsByIdHealth(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/data-products/{id}/health", `/v1/m/knowledge/data-products/${encodeURIComponent(id)}/health`, undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/data-products/{id}/publish — knowledge module route (requires knowledge:data_product:write)
   *
   * Stability: beta.
   */
  postV1MKnowledgeDataProductsByIdPublish(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/knowledge/data-products/{id}/publish", `/v1/m/knowledge/data-products/${encodeURIComponent(id)}/publish`, undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/data-products/{id}/validate — knowledge module route (requires knowledge:data_product:write)
   *
   * Stability: beta.
   */
  postV1MKnowledgeDataProductsByIdValidate(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/knowledge/data-products/{id}/validate", `/v1/m/knowledge/data-products/${encodeURIComponent(id)}/validate`, body, opts);
  }

  /**
   * GET /v1/m/knowledge/dlp/rules — Lists the tenant's DLP rules.
   *
   * Stability: beta.
   */
  getV1MKnowledgeDlpRules(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/dlp/rules", "/v1/m/knowledge/dlp/rules", undefined, opts);
  }

  /**
   * PUT /v1/m/knowledge/dlp/rules — Upserts one DLP rule by class (admin-tier: writing egress policy is privileged governance; self-audited).
   *
   * Stability: beta.
   */
  putV1MKnowledgeDlpRules(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/knowledge/dlp/rules", "/v1/m/knowledge/dlp/rules", body, opts);
  }

  /**
   * DELETE /v1/m/knowledge/dlp/rules/{id} — Removes one DLP rule by id (admin-tier; self-audited). Removing the last rule disables the gate entirely — the audit row is the record of WHO opened the perimeter.
   *
   * Stability: beta.
   */
  deleteV1MKnowledgeDlpRulesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/knowledge/dlp/rules/{id}", `/v1/m/knowledge/dlp/rules/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/documents/{id} — Returns one document's metadata + provenance (no body).
   *
   * Stability: beta.
   */
  getV1MKnowledgeDocumentsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/documents/{id}", `/v1/m/knowledge/documents/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/kbs — Lists knowledge bases, optionally filtered by status.
   *
   * Stability: beta.
   */
  getV1MKnowledgeKbs(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/kbs", "/v1/m/knowledge/kbs", undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/kbs — Declares a knowledge base.
   *
   * Stability: beta.
   */
  postV1MKnowledgeKbs(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/knowledge/kbs", "/v1/m/knowledge/kbs", body, opts);
  }

  /**
   * GET /v1/m/knowledge/kbs/{id} — knowledge module route (requires knowledge:kb:read)
   *
   * Stability: beta.
   */
  getV1MKnowledgeKbsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/kbs/{id}", `/v1/m/knowledge/kbs/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/knowledge/kbs/{id} — Updates a KB's governance settings (classification/residency/ embed policy/default ACL/status), version-checked, re-validating the embed gate.
   *
   * Stability: beta.
   */
  putV1MKnowledgeKbsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/knowledge/kbs/{id}", `/v1/m/knowledge/kbs/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/knowledge/kbs/{id} — Deletes a KB and CASCADES its documents, chunks and document sensitivity labels in one transaction (no orphans).
   *
   * Stability: beta.
   */
  deleteV1MKnowledgeKbsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/knowledge/kbs/{id}", `/v1/m/knowledge/kbs/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/kbs/{id}/documents — Lists a KB's ingested documents (metadata only; no body).
   *
   * Stability: beta.
   */
  getV1MKnowledgeKbsByIdDocuments(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/kbs/{id}/documents", `/v1/m/knowledge/kbs/${encodeURIComponent(id)}/documents`, undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/kbs/{id}/ingest — Ingests content into a knowledge base: it gathers documents (from a registered source or inline), REDACTS each body, chunks it, embeds the chunks via the wired embedder OUTSIDE the store transaction (the network/ egress call must never hold the single SQLite writer — the deploy discipline), and persists documents + chunks in one transaction.
   *
   * Stability: beta.
   */
  postV1MKnowledgeKbsByIdIngest(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/knowledge/kbs/{id}/ingest", `/v1/m/knowledge/kbs/${encodeURIComponent(id)}/ingest`, body, opts);
  }

  /**
   * POST /v1/m/knowledge/kbs/{id}/query — knowledge module route (requires knowledge:retrieval:read)
   *
   * Stability: beta.
   */
  postV1MKnowledgeKbsByIdQuery(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/knowledge/kbs/{id}/query", `/v1/m/knowledge/kbs/${encodeURIComponent(id)}/query`, body, opts);
  }

  /**
   * POST /v1/m/knowledge/kbs/{id}/reindex — Re-embeds a KB's chunks that have no embedding yet (the recovery path for a partial ingest where the embed call failed).
   *
   * Stability: beta.
   */
  postV1MKnowledgeKbsByIdReindex(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/knowledge/kbs/{id}/reindex", `/v1/m/knowledge/kbs/${encodeURIComponent(id)}/reindex`, undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/kbs/{id}/scan — Runs the at-rest discovery scan over one KB's stored documents: classify every chunk's (redacted) text, label each document, count redaction markers, append the scan evidence row.
   *
   * Stability: beta.
   */
  postV1MKnowledgeKbsByIdScan(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/knowledge/kbs/{id}/scan", `/v1/m/knowledge/kbs/${encodeURIComponent(id)}/scan`, undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/kbs/{id}/sync — Handles POST /kbs/{id}/sync.
   *
   * Stability: beta.
   */
  postV1MKnowledgeKbsByIdSync(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/knowledge/kbs/{id}/sync", `/v1/m/knowledge/kbs/${encodeURIComponent(id)}/sync`, body, opts);
  }

  /**
   * GET /v1/m/knowledge/labels — Lists sensitivity labels, optionally filtered by subject_kind and/or kb_id (labels are governance metadata — counts and rules, no content).
   *
   * Stability: beta.
   */
  getV1MKnowledgeLabels(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/labels", "/v1/m/knowledge/labels", undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/lineage — Lists lineage records, optionally filtered by kb_id, agent_ref or decision.
   *
   * Stability: beta.
   */
  getV1MKnowledgeLineage(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/lineage", "/v1/m/knowledge/lineage", undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/lineage/{id} — Returns one lineage record (self-audited).
   *
   * Stability: beta.
   */
  getV1MKnowledgeLineageById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/lineage/{id}", `/v1/m/knowledge/lineage/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/memory — Lists the memory visible to the DECLARED read context (?agent_ref= plus the ?user_ref=/?session_ref= namespace), EXCLUDING expired entries (lazy expiry), entries above the reader's classification clearance, and entries that fail the integrity self-check (fail closed; withheld count reported, detection audited).
   *
   * Stability: beta.
   */
  getV1MKnowledgeMemory(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/memory", "/v1/m/knowledge/memory", undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/memory — Upserts an agent memory entry (by agent_ref + namespace + key), redacting the content and enforcing the per-agent quota.
   *
   * Stability: beta.
   */
  postV1MKnowledgeMemory(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/knowledge/memory", "/v1/m/knowledge/memory", body, opts);
  }

  /**
   * GET /v1/m/knowledge/memory/all — knowledge module route (requires knowledge:memory:admin)
   *
   * Stability: beta.
   */
  getV1MKnowledgeMemoryAll(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/memory/all", "/v1/m/knowledge/memory/all", undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/memory/export — Streams the caller's clearance-visible memory as signed JSONL: line 1 is the signed manifest, each following line one memPortRow.
   *
   * Stability: beta.
   */
  getV1MKnowledgeMemoryExport(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/memory/export", "/v1/m/knowledge/memory/export", undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/memory/import — Ingests a signed memory-portability bundle.
   *
   * Stability: beta.
   */
  postV1MKnowledgeMemoryImport(body: Uint8Array, opts?: RequestOptions): Promise<Json> {
    return this.doReqRawWithType("POST", "/v1/m/knowledge/memory/import", "/v1/m/knowledge/memory/import", body, "application/x-ndjson", opts);
  }

  /**
   * POST /v1/m/knowledge/memory/purge — Materializes expiry: it deletes expired memory entries of BOTH scopes (optionally for one agent).
   *
   * Stability: beta.
   */
  postV1MKnowledgeMemoryPurge(opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/knowledge/memory/purge", "/v1/m/knowledge/memory/purge", undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/memory/verify — knowledge module route (requires knowledge:memory:admin)
   *
   * Stability: beta.
   */
  postV1MKnowledgeMemoryVerify(opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/knowledge/memory/verify", "/v1/m/knowledge/memory/verify", undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/memory/{id} — Returns one memory entry.
   *
   * Stability: beta.
   */
  getV1MKnowledgeMemoryById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/memory/{id}", `/v1/m/knowledge/memory/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * DELETE /v1/m/knowledge/memory/{id} — Removes one memory entry.
   *
   * Stability: beta.
   */
  deleteV1MKnowledgeMemoryById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/knowledge/memory/{id}", `/v1/m/knowledge/memory/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/prompts — knowledge module route (requires knowledge:prompt:read)
   *
   * Stability: beta.
   */
  getV1MKnowledgePrompts(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/prompts", "/v1/m/knowledge/prompts", undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/prompts — Creates a prompt and its first immutable revision.
   *
   * Stability: beta.
   */
  postV1MKnowledgePrompts(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/knowledge/prompts", "/v1/m/knowledge/prompts", body, opts);
  }

  /**
   * GET /v1/m/knowledge/prompts/{id} — Returns one prompt (its current pointer + status).
   *
   * Stability: beta.
   */
  getV1MKnowledgePromptsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/prompts/{id}", `/v1/m/knowledge/prompts/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/knowledge/prompts/{id}/revisions — Lists a prompt's immutable revision history.
   *
   * Stability: beta.
   */
  getV1MKnowledgePromptsByIdRevisions(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/prompts/{id}/revisions", `/v1/m/knowledge/prompts/${encodeURIComponent(id)}/revisions`, undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/prompts/{id}/revisions — Appends an immutable revision and advances the current pointer.
   *
   * Stability: beta.
   */
  postV1MKnowledgePromptsByIdRevisions(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/knowledge/prompts/{id}/revisions", `/v1/m/knowledge/prompts/${encodeURIComponent(id)}/revisions`, body, opts);
  }

  /**
   * GET /v1/m/knowledge/prompts/{id}/revisions/{rev} — Returns one immutable revision by number.
   *
   * Stability: beta.
   */
  getV1MKnowledgePromptsByIdRevisionsByRev(id: string, rev: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/prompts/{id}/revisions/{rev}", `/v1/m/knowledge/prompts/${encodeURIComponent(id)}/revisions/${encodeURIComponent(rev)}`, undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/prompts/{id}/rollback — Points the prompt's current revision at a prior, existing revision.
   *
   * Stability: beta.
   */
  postV1MKnowledgePromptsByIdRollback(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/knowledge/prompts/{id}/rollback", `/v1/m/knowledge/prompts/${encodeURIComponent(id)}/rollback`, body, opts);
  }

  /**
   * GET /v1/m/knowledge/scans — Lists the append-only discovery scan evidence.
   *
   * Stability: beta.
   */
  getV1MKnowledgeScans(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/knowledge/scans", "/v1/m/knowledge/scans", undefined, opts);
  }

  /**
   * POST /v1/m/knowledge/sources/{name}/scan — Runs discovery ACROSS a configured document store WITHOUT ingesting: it pulls the source's documents (bounded like ingest), classifies the RAW bodies in memory and persists labels + the scan evidence — the bodies themselves are never stored, logged or embedded (zero egress, minimal data).
   *
   * Stability: beta.
   */
  postV1MKnowledgeSourcesByNameScan(name: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/knowledge/sources/{name}/scan", `/v1/m/knowledge/sources/${encodeURIComponent(name)}/scan`, undefined, opts);
  }

  /**
   * GET /v1/m/models/access-tier-entitlements — Lists operator-attested entitlement states, optionally filtered by tier or state.
   *
   * Stability: beta.
   */
  getV1MModelsAccessTierEntitlements(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/access-tier-entitlements", "/v1/m/models/access-tier-entitlements", undefined, opts);
  }

  /**
   * PUT /v1/m/models/access-tier-entitlements — Records the current provider-side entitlement state for one restricted tier.
   *
   * Stability: beta.
   */
  putV1MModelsAccessTierEntitlements(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/models/access-tier-entitlements", "/v1/m/models/access-tier-entitlements", body, opts);
  }

  /**
   * GET /v1/m/models/admission-policy — models module route (requires models:admission:read)
   *
   * Stability: beta.
   */
  getV1MModelsAdmissionPolicy(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/admission-policy", "/v1/m/models/admission-policy", undefined, opts);
  }

  /**
   * PUT /v1/m/models/admission-policy — Upserts the per-tenant trust root. Admin-tier, audited.
   *
   * Stability: beta.
   */
  putV1MModelsAdmissionPolicy(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/models/admission-policy", "/v1/m/models/admission-policy", body, opts);
  }

  /**
   * GET /v1/m/models/agent-artifacts — models module route (requires models:registry:read)
   *
   * Stability: beta.
   */
  getV1MModelsAgentArtifacts(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/agent-artifacts", "/v1/m/models/agent-artifacts", undefined, opts);
  }

  /**
   * POST /v1/m/models/agent-artifacts — models module route (requires models:registry:write)
   *
   * Stability: beta.
   */
  postV1MModelsAgentArtifacts(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/models/agent-artifacts", "/v1/m/models/agent-artifacts", body, opts);
  }

  /**
   * GET /v1/m/models/agent-artifacts/aibom — Returns the live agent-supply-chain BOM (read-only export, not sealed; read-tier, not audited — observer effect).
   *
   * Stability: beta.
   */
  getV1MModelsAgentArtifactsAibom(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/agent-artifacts/aibom", "/v1/m/models/agent-artifacts/aibom", undefined, opts);
  }

  /**
   * POST /v1/m/models/agent-artifacts/aibom — Generates the agent-supply-chain BOM, anchors the audit-chain head, persists an append-only models.agent_aibom seal and self-audits it — the same pattern as handleSealAIBOM on the artifact inventory, so it is tamper-evident evidence (and a SEPARATE evidence class from the model-lineage seals compliance counts).
   *
   * Stability: beta.
   */
  postV1MModelsAgentArtifactsAibom(opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/models/agent-artifacts/aibom", "/v1/m/models/agent-artifacts/aibom", undefined, opts);
  }

  /**
   * GET /v1/m/models/agent-artifacts/aiboms — Lists the agent-supply-chain BOM seals.
   *
   * Stability: beta.
   */
  getV1MModelsAgentArtifactsAiboms(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/agent-artifacts/aiboms", "/v1/m/models/agent-artifacts/aiboms", undefined, opts);
  }

  /**
   * DELETE /v1/m/models/agent-artifacts/{id} — models module route (requires models:registry:write)
   *
   * Stability: beta.
   */
  deleteV1MModelsAgentArtifactsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/models/agent-artifacts/{id}", `/v1/m/models/agent-artifacts/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/models/aiboms — models module route (requires models:registry:read)
   *
   * Stability: beta.
   */
  getV1MModelsAiboms(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/aiboms", "/v1/m/models/aiboms", undefined, opts);
  }

  /**
   * GET /v1/m/models/catalog — Returns the declared reference catalog.
   *
   * Stability: beta.
   */
  getV1MModelsCatalog(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/catalog", "/v1/m/models/catalog", undefined, opts);
  }

  /**
   * GET /v1/m/models/data-governance — Returns the Claude context-management / memory-tool + ZDR-eligibility matrix (CLA-15): for each feature, whether it can clear the model's working context server-side (a forensics implication), where its data persists, and whether it is ZDR-eligible (a data-residency concern).
   *
   * Stability: beta.
   */
  getV1MModelsDataGovernance(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/data-governance", "/v1/m/models/data-governance", undefined, opts);
  }

  /**
   * GET /v1/m/models/datasets — models module route (requires models:registry:read)
   *
   * Stability: beta.
   */
  getV1MModelsDatasets(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/datasets", "/v1/m/models/datasets", undefined, opts);
  }

  /**
   * POST /v1/m/models/datasets — models module route (requires models:registry:write)
   *
   * Stability: beta.
   */
  postV1MModelsDatasets(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/models/datasets", "/v1/m/models/datasets", body, opts);
  }

  /**
   * DELETE /v1/m/models/datasets/{id} — models module route (requires models:registry:write)
   *
   * Stability: beta.
   */
  deleteV1MModelsDatasetsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/models/datasets/{id}", `/v1/m/models/datasets/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/models/features — Returns the capability matrix: per API feature, which declared families support it.
   *
   * Stability: beta.
   */
  getV1MModelsFeatures(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/features", "/v1/m/models/features", undefined, opts);
  }

  /**
   * GET /v1/m/models/finetune-jobs — models module route (requires models:registry:read)
   *
   * Stability: beta.
   */
  getV1MModelsFinetuneJobs(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/finetune-jobs", "/v1/m/models/finetune-jobs", undefined, opts);
  }

  /**
   * POST /v1/m/models/finetune-jobs — models module route (requires models:registry:write)
   *
   * Stability: beta.
   */
  postV1MModelsFinetuneJobs(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/models/finetune-jobs", "/v1/m/models/finetune-jobs", body, opts);
  }

  /**
   * GET /v1/m/models/finetune-jobs/{id} — models module route (requires models:registry:read)
   *
   * Stability: beta.
   */
  getV1MModelsFinetuneJobsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/finetune-jobs/{id}", `/v1/m/models/finetune-jobs/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/models/finetune-jobs/{id} — Records a status/lineage transition on a fine-tune job (the control plane TRACKS the job; it does not run it).
   *
   * Stability: beta.
   */
  putV1MModelsFinetuneJobsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/models/finetune-jobs/{id}", `/v1/m/models/finetune-jobs/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * GET /v1/m/models/gpai-posture — Lists the attested GPAI posture of brokered providers, optionally filtered by provider_ref or verified.
   *
   * Stability: beta.
   */
  getV1MModelsGpaiPosture(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/gpai-posture", "/v1/m/models/gpai-posture", undefined, opts);
  }

  /**
   * PUT /v1/m/models/gpai-posture — Records (or replaces) a provider's GPAI posture.
   *
   * Stability: beta.
   */
  putV1MModelsGpaiPosture(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/models/gpai-posture", "/v1/m/models/gpai-posture", body, opts);
  }

  /**
   * GET /v1/m/models/inference-deployments — models module route (requires models:registry:read)
   *
   * Stability: beta.
   */
  getV1MModelsInferenceDeployments(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/inference-deployments", "/v1/m/models/inference-deployments", undefined, opts);
  }

  /**
   * POST /v1/m/models/inference-deployments — models module route (requires models:registry:write)
   *
   * Stability: beta.
   */
  postV1MModelsInferenceDeployments(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/models/inference-deployments", "/v1/m/models/inference-deployments", body, opts);
  }

  /**
   * PUT /v1/m/models/inference-deployments/{id} — models module route (requires models:registry:write)
   *
   * Stability: beta.
   */
  putV1MModelsInferenceDeploymentsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/models/inference-deployments/{id}", `/v1/m/models/inference-deployments/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/models/inference-deployments/{id} — models module route (requires models:registry:write)
   *
   * Stability: beta.
   */
  deleteV1MModelsInferenceDeploymentsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/models/inference-deployments/{id}", `/v1/m/models/inference-deployments/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/models/keys — Lists key/workspace governance references, optionally filtered by ref_kind, provider_ref or status.
   *
   * Stability: beta.
   */
  getV1MModelsKeys(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/keys", "/v1/m/models/keys", undefined, opts);
  }

  /**
   * POST /v1/m/models/keys — Registers a new key/workspace governance reference.
   *
   * Stability: beta.
   */
  postV1MModelsKeys(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/models/keys", "/v1/m/models/keys", body, opts);
  }

  /**
   * PUT /v1/m/models/keys/{id} — Updates a key/workspace governance reference in place.
   *
   * Stability: beta.
   */
  putV1MModelsKeysById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/models/keys/{id}", `/v1/m/models/keys/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/models/keys/{id} — Removes a key/workspace governance reference.
   *
   * Stability: beta.
   */
  deleteV1MModelsKeysById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/models/keys/{id}", `/v1/m/models/keys/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/models/model-access — models module route (requires models:model-access:read)
   *
   * Stability: beta.
   */
  getV1MModelsModelAccess(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/model-access", "/v1/m/models/model-access", undefined, opts);
  }

  /**
   * POST /v1/m/models/model-access — models module route (requires models:model-access:admin)
   *
   * Stability: beta.
   */
  postV1MModelsModelAccess(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/models/model-access", "/v1/m/models/model-access", body, opts);
  }

  /**
   * PUT /v1/m/models/model-access/{id} — models module route (requires models:model-access:admin)
   *
   * Stability: beta.
   */
  putV1MModelsModelAccessById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/models/model-access/{id}", `/v1/m/models/model-access/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/models/model-access/{id} — models module route (requires models:model-access:admin)
   *
   * Stability: beta.
   */
  deleteV1MModelsModelAccessById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/models/model-access/{id}", `/v1/m/models/model-access/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/models/model-admissions — models module route (requires models:admission:read)
   *
   * Stability: beta.
   */
  getV1MModelsModelAdmissions(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/model-admissions", "/v1/m/models/model-admissions", undefined, opts);
  }

  /**
   * GET /v1/m/models/model-groups — models module route (requires models:model-group:read)
   *
   * Stability: beta.
   */
  getV1MModelsModelGroups(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/model-groups", "/v1/m/models/model-groups", undefined, opts);
  }

  /**
   * POST /v1/m/models/model-groups — models module route (requires models:model-group:write)
   *
   * Stability: beta.
   */
  postV1MModelsModelGroups(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/models/model-groups", "/v1/m/models/model-groups", body, opts);
  }

  /**
   * GET /v1/m/models/model-groups/{id} — models module route (requires models:model-group:read)
   *
   * Stability: beta.
   */
  getV1MModelsModelGroupsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/model-groups/{id}", `/v1/m/models/model-groups/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/models/model-groups/{id} — models module route (requires models:model-group:write)
   *
   * Stability: beta.
   */
  putV1MModelsModelGroupsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/models/model-groups/{id}", `/v1/m/models/model-groups/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/models/model-groups/{id} — Deletes a model-group, but REFUSES (409) while any model-access grant still targets it — symmetric with create-time validateTargetGroup, which blocks a grant to a non-existent group.
   *
   * Stability: beta.
   */
  deleteV1MModelsModelGroupsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/models/model-groups/{id}", `/v1/m/models/model-groups/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/models/model-versions — models module route (requires models:registry:read)
   *
   * Stability: beta.
   */
  getV1MModelsModelVersions(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/model-versions", "/v1/m/models/model-versions", undefined, opts);
  }

  /**
   * POST /v1/m/models/model-versions — models module route (requires models:registry:write)
   *
   * Stability: beta.
   */
  postV1MModelsModelVersions(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/models/model-versions", "/v1/m/models/model-versions", body, opts);
  }

  /**
   * DELETE /v1/m/models/model-versions/{id} — models module route (requires models:registry:write)
   *
   * Stability: beta.
   */
  deleteV1MModelsModelVersionsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/models/model-versions/{id}", `/v1/m/models/model-versions/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/models/model-versions/{id}/admit — Verifies an OMS v1.0 signature for a model_version and records the verdict (upsert, claim-vs-verified).
   *
   * Stability: beta.
   */
  postV1MModelsModelVersionsByIdAdmit(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/models/model-versions/{id}/admit", `/v1/m/models/model-versions/${encodeURIComponent(id)}/admit`, body, opts);
  }

  /**
   * GET /v1/m/models/models — Lists the governed core models (the live estate, enriched).
   *
   * Stability: beta.
   */
  getV1MModelsModels(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/models", "/v1/m/models/models", undefined, opts);
  }

  /**
   * GET /v1/m/models/models/{id} — Returns one governed model with its provider reference.
   *
   * Stability: beta.
   */
  getV1MModelsModelsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/models/{id}", `/v1/m/models/models/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/models/owned-models — models module route (requires models:registry:read)
   *
   * Stability: beta.
   */
  getV1MModelsOwnedModels(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/owned-models", "/v1/m/models/owned-models", undefined, opts);
  }

  /**
   * POST /v1/m/models/owned-models — models module route (requires models:registry:write)
   *
   * Stability: beta.
   */
  postV1MModelsOwnedModels(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/models/owned-models", "/v1/m/models/owned-models", body, opts);
  }

  /**
   * GET /v1/m/models/owned-models/{id} — models module route (requires models:registry:read)
   *
   * Stability: beta.
   */
  getV1MModelsOwnedModelsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/owned-models/{id}", `/v1/m/models/owned-models/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/models/owned-models/{id} — models module route (requires models:registry:write)
   *
   * Stability: beta.
   */
  putV1MModelsOwnedModelsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/models/owned-models/{id}", `/v1/m/models/owned-models/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/models/owned-models/{id} — models module route (requires models:registry:write)
   *
   * Stability: beta.
   */
  deleteV1MModelsOwnedModelsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/models/owned-models/{id}", `/v1/m/models/owned-models/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/models/owned-models/{id}/aibom — Returns the live AI bill of materials for an owned model (read-only export, not sealed).
   *
   * Stability: beta.
   */
  getV1MModelsOwnedModelsByIdAibom(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/owned-models/{id}/aibom", `/v1/m/models/owned-models/${encodeURIComponent(id)}/aibom`, undefined, opts);
  }

  /**
   * POST /v1/m/models/owned-models/{id}/aibom — Generates the AIBOM, computes its canonical content hash, anchors the current audit-chain head, persists an append-only models.aibom seal record and self-audits the seal as the next ledger event — making the AIBOM tamper-evident compliance evidence (the same pattern as compliance's evidence package).
   *
   * Stability: beta.
   */
  postV1MModelsOwnedModelsByIdAibom(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/models/owned-models/{id}/aibom", `/v1/m/models/owned-models/${encodeURIComponent(id)}/aibom`, undefined, opts);
  }

  /**
   * GET /v1/m/models/owned-models/{id}/model-card — Returns the generated model card for an owned model (read-only export from the live inventory; read-tier, not audited — observer effect, exactly like the AIBOM generate route).
   *
   * Stability: beta.
   */
  getV1MModelsOwnedModelsByIdModelCard(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/owned-models/{id}/model-card", `/v1/m/models/owned-models/${encodeURIComponent(id)}/model-card`, undefined, opts);
  }

  /**
   * GET /v1/m/models/platforms — Returns the declared platforms reference.
   *
   * Stability: beta.
   */
  getV1MModelsPlatforms(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/platforms", "/v1/m/models/platforms", undefined, opts);
  }

  /**
   * GET /v1/m/models/rate-limits — Returns the read-only rate-limit inventory.
   *
   * Stability: beta.
   */
  getV1MModelsRateLimits(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/rate-limits", "/v1/m/models/rate-limits", undefined, opts);
  }

  /**
   * GET /v1/m/models/routing-policies — Lists routing policies.
   *
   * Stability: beta.
   */
  getV1MModelsRoutingPolicies(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/routing-policies", "/v1/m/models/routing-policies", undefined, opts);
  }

  /**
   * POST /v1/m/models/routing-policies — Creates a routing policy.
   *
   * Stability: beta.
   */
  postV1MModelsRoutingPolicies(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/models/routing-policies", "/v1/m/models/routing-policies", body, opts);
  }

  /**
   * GET /v1/m/models/routing-policies/{id} — Returns one routing policy.
   *
   * Stability: beta.
   */
  getV1MModelsRoutingPoliciesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/routing-policies/{id}", `/v1/m/models/routing-policies/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/models/routing-policies/{id} — Updates a routing policy in place.
   *
   * Stability: beta.
   */
  putV1MModelsRoutingPoliciesById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/models/routing-policies/{id}", `/v1/m/models/routing-policies/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/models/routing-policies/{id} — Deletes a routing policy.
   *
   * Stability: beta.
   */
  deleteV1MModelsRoutingPoliciesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/models/routing-policies/{id}", `/v1/m/models/routing-policies/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/models/routing-policies/{id}/execute — Resolves a stored routing policy AND executes the resolved target chain through the governed Executor, emitting the result's CostSample.
   *
   * Stability: beta.
   */
  postV1MModelsRoutingPoliciesByIdExecute(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/models/routing-policies/{id}/execute", `/v1/m/models/routing-policies/${encodeURIComponent(id)}/execute`, body, opts);
  }

  /**
   * POST /v1/m/models/routing-policies/{id}/resolve — Resolves a stored routing policy against the governed estate and returns the routing decision.
   *
   * Stability: beta.
   */
  postV1MModelsRoutingPoliciesByIdResolve(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/models/routing-policies/{id}/resolve", `/v1/m/models/routing-policies/${encodeURIComponent(id)}/resolve`, undefined, opts);
  }

  /**
   * GET /v1/m/models/tool-types — Returns the declared dated Anthropic tool-type catalog.
   *
   * Stability: beta.
   */
  getV1MModelsToolTypes(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/tool-types", "/v1/m/models/tool-types", undefined, opts);
  }

  /**
   * GET /v1/m/models/workspace-residency — Lists the persisted per-workspace residency references, optionally filtered by workspace_ref.
   *
   * Stability: beta.
   */
  getV1MModelsWorkspaceResidency(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/models/workspace-residency", "/v1/m/models/workspace-residency", undefined, opts);
  }

  /**
   * PUT /v1/m/models/workspace-residency — Records (or replaces) one workspace's residency reference.
   *
   * Stability: beta.
   */
  putV1MModelsWorkspaceResidency(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/models/workspace-residency", "/v1/m/models/workspace-residency", body, opts);
  }

  /**
   * GET /v1/m/notify/deliveries — Lists the append-only delivery ledger, optionally filtered by ?status, ?finding_kind, ?destination and ?route (a route id).
   *
   * Stability: beta.
   */
  getV1MNotifyDeliveries(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/notify/deliveries", "/v1/m/notify/deliveries", undefined, opts);
  }

  /**
   * GET /v1/m/notify/destinations — Returns the provisioned destination names (from the wired transport).
   *
   * Stability: beta.
   */
  getV1MNotifyDestinations(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/notify/destinations", "/v1/m/notify/destinations", undefined, opts);
  }

  /**
   * GET /v1/m/notify/match-types — Returns the routable event types a route's match_types may name — the console's multiselect source and the reference the validation errors point at.
   *
   * Stability: beta.
   */
  getV1MNotifyMatchTypes(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/notify/match-types", "/v1/m/notify/match-types", undefined, opts);
  }

  /**
   * GET /v1/m/notify/outbox — Lists the durable outbox, optionally filtered by ?status (e.g. status=dead for the dead-letter queue view) and ?destination.
   *
   * Stability: beta.
   */
  getV1MNotifyOutbox(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/notify/outbox", "/v1/m/notify/outbox", undefined, opts);
  }

  /**
   * POST /v1/m/notify/outbox/{id}/redeliver — Requeues a TERMINAL outbox row (dead-letter drain / ack-and- retry).
   *
   * Stability: beta.
   */
  postV1MNotifyOutboxByIdRedeliver(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/notify/outbox/{id}/redeliver", `/v1/m/notify/outbox/${encodeURIComponent(id)}/redeliver`, undefined, opts);
  }

  /**
   * GET /v1/m/notify/routes — Lists routing rules, optionally filtered by ?destination and ?enabled.
   *
   * Stability: beta.
   */
  getV1MNotifyRoutes(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/notify/routes", "/v1/m/notify/routes", undefined, opts);
  }

  /**
   * POST /v1/m/notify/routes — Declares a routing rule.
   *
   * Stability: beta.
   */
  postV1MNotifyRoutes(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/notify/routes", "/v1/m/notify/routes", body, opts);
  }

  /**
   * POST /v1/m/notify/routes/evaluate — Answers "which routes would this signal select?" without delivering anything.
   *
   * Stability: beta.
   */
  postV1MNotifyRoutesEvaluate(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/notify/routes/evaluate", "/v1/m/notify/routes/evaluate", body, opts);
  }

  /**
   * GET /v1/m/notify/routes/{id} — Returns one route.
   *
   * Stability: beta.
   */
  getV1MNotifyRoutesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/notify/routes/{id}", `/v1/m/notify/routes/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/notify/routes/{id} — Replaces a route's predicate, destination and windows.
   *
   * Stability: beta.
   */
  putV1MNotifyRoutesById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/notify/routes/{id}", `/v1/m/notify/routes/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/notify/routes/{id} — Removes a route (hard delete).
   *
   * Stability: beta.
   */
  deleteV1MNotifyRoutesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/notify/routes/{id}", `/v1/m/notify/routes/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/notify/routes/{id}/restore — Re-applies the configuration of an earlier revision to an EXISTING route (never the name — the natural key, exactly like update).
   *
   * Stability: beta.
   */
  postV1MNotifyRoutesByIdRestore(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/notify/routes/{id}/restore", `/v1/m/notify/routes/${encodeURIComponent(id)}/restore`, body, opts);
  }

  /**
   * GET /v1/m/notify/routes/{id}/revisions — Lists a route's revision ledger, keyset-paginated by the time-ordered row id (chronological by ingestion, the decision-ledger convention).
   *
   * Stability: beta.
   */
  getV1MNotifyRoutesByIdRevisions(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/notify/routes/{id}/revisions", `/v1/m/notify/routes/${encodeURIComponent(id)}/revisions`, undefined, opts);
  }

  /**
   * POST /v1/m/notify/routes/{id}/test — Sends a synthetic test notification through a route's destination and records the attempt in the ledger.
   *
   * Stability: beta.
   */
  postV1MNotifyRoutesByIdTest(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/notify/routes/{id}/test", `/v1/m/notify/routes/${encodeURIComponent(id)}/test`, undefined, opts);
  }

  /**
   * GET /v1/m/observability/attestation — Returns the measured truth about the RUNNING binary.
   *
   * Stability: beta.
   */
  getV1MObservabilityAttestation(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/observability/attestation", "/v1/m/observability/attestation", undefined, opts);
  }

  /**
   * GET /v1/m/observability/ingestion-health — Returns the per-standard table + the live per-source counters.
   *
   * Stability: beta.
   */
  getV1MObservabilityIngestionHealth(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/observability/ingestion-health", "/v1/m/observability/ingestion-health", undefined, opts);
  }

  /**
   * GET /v1/m/observability/traces — Lists the traces correlated in the ledger window, newest first.
   *
   * Stability: beta.
   */
  getV1MObservabilityTraces(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/observability/traces", "/v1/m/observability/traces", undefined, opts);
  }

  /**
   * GET /v1/m/observability/traces/{id} — Returns one trace's detail: ONE span row per distinct span_id, each grouping the ledger events that engine span produced.
   *
   * Stability: beta.
   */
  getV1MObservabilityTracesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/observability/traces/{id}", `/v1/m/observability/traces/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/observability/traces/{id}/export — Exports one trace as OTLP-compatible JSON so the operator can import it into Jaeger, Grafana Tempo, Datadog, or any OTLP-aware tool.
   *
   * Stability: beta.
   */
  getV1MObservabilityTracesByIdExport(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/observability/traces/{id}/export", `/v1/m/observability/traces/${encodeURIComponent(id)}/export`, undefined, opts);
  }

  /**
   * GET /v1/m/orchestration/decisions — Lists the whole append-only fire/miss ledger for the tenant.
   *
   * Stability: beta.
   */
  getV1MOrchestrationDecisions(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/decisions", "/v1/m/orchestration/decisions", undefined, opts);
  }

  /**
   * GET /v1/m/orchestration/flows — Returns the derived multi-agent flows (a supervisor and the workers it delegates to) with a read-time-derived lifecycle state — a privileged, self-audited read.
   *
   * Stability: beta.
   */
  getV1MOrchestrationFlows(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/flows", "/v1/m/orchestration/flows", undefined, opts);
  }

  /**
   * GET /v1/m/orchestration/graph — Returns the React-Flow communication/delegation graph, derived from the relation table, with honest coverage.
   *
   * Stability: beta.
   */
  getV1MOrchestrationGraph(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/graph", "/v1/m/orchestration/graph", undefined, opts);
  }

  /**
   * GET /v1/m/orchestration/graph/neighbors — Returns the subgraph around one node (its incoming and/or outgoing relations) — a privileged, self-audited read.
   *
   * Stability: beta.
   */
  getV1MOrchestrationGraphNeighbors(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/graph/neighbors", "/v1/m/orchestration/graph/neighbors", undefined, opts);
  }

  /**
   * GET /v1/m/orchestration/schedules — Lists governed schedules.
   *
   * Stability: beta.
   */
  getV1MOrchestrationSchedules(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/schedules", "/v1/m/orchestration/schedules", undefined, opts);
  }

  /**
   * POST /v1/m/orchestration/schedules — Declares a governed schedule (write-tier, self-audited).
   *
   * Stability: beta.
   */
  postV1MOrchestrationSchedules(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/orchestration/schedules", "/v1/m/orchestration/schedules", body, opts);
  }

  /**
   * GET /v1/m/orchestration/schedules/{id} — Returns one schedule with its derived health and last observed activity.
   *
   * Stability: beta.
   */
  getV1MOrchestrationSchedulesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/schedules/{id}", `/v1/m/orchestration/schedules/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PATCH /v1/m/orchestration/schedules/{id} — Enables/disables/retargets a schedule (write-tier, self-audited).
   *
   * Stability: beta.
   */
  patchV1MOrchestrationSchedulesById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PATCH", "/v1/m/orchestration/schedules/{id}", `/v1/m/orchestration/schedules/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * GET /v1/m/orchestration/schedules/{id}/decisions — Lists the append-only fire/miss ledger for one schedule.
   *
   * Stability: beta.
   */
  getV1MOrchestrationSchedulesByIdDecisions(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/schedules/{id}/decisions", `/v1/m/orchestration/schedules/${encodeURIComponent(id)}/decisions`, undefined, opts);
  }

  /**
   * POST /v1/m/orchestration/schedules/{id}/fire — orchestration module route (requires orchestration:schedule:admin)
   *
   * Stability: beta.
   */
  postV1MOrchestrationSchedulesByIdFire(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/orchestration/schedules/{id}/fire", `/v1/m/orchestration/schedules/${encodeURIComponent(id)}/fire`, body, opts);
  }

  /**
   * POST /v1/m/orchestration/schedules/{id}/restore — Re-applies the mutable shape of an earlier revision via the patch verb's exact application path (same validation, same cadence-miss clearing, same plan_hash consequence).
   *
   * Stability: beta.
   */
  postV1MOrchestrationSchedulesByIdRestore(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/orchestration/schedules/{id}/restore", `/v1/m/orchestration/schedules/${encodeURIComponent(id)}/restore`, body, opts);
  }

  /**
   * GET /v1/m/orchestration/schedules/{id}/revisions — Lists a schedule's revision ledger, keyset-paginated by the time-ordered row id (chronological by ingestion, the decision-ledger convention).
   *
   * Stability: beta.
   */
  getV1MOrchestrationSchedulesByIdRevisions(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/schedules/{id}/revisions", `/v1/m/orchestration/schedules/${encodeURIComponent(id)}/revisions`, undefined, opts);
  }

  /**
   * GET /v1/m/orchestration/stream — Serves the live comm graph as server-sent events, pinned to the request's single authorized tenant so a client only ever sees its own tenant's relations.
   *
   * Stability: beta.
   */
  getV1MOrchestrationStream(opts?: RequestOptions): Promise<string> {
    return this.doRaw("GET", "/v1/m/orchestration/stream", "/v1/m/orchestration/stream", opts);
  }

  /**
   * GET /v1/m/orchestration/timeline — Returns one subject's merged orchestration history (its delegation activity and its fire/miss decisions) in reverse-chronological order — a privileged, self-audited read.
   *
   * Stability: beta.
   */
  getV1MOrchestrationTimeline(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/timeline", "/v1/m/orchestration/timeline", undefined, opts);
  }

  /**
   * GET /v1/m/orchestration/workflows — Lists the tenant's workflows (list shape, no graphs).
   *
   * Stability: beta.
   */
  getV1MOrchestrationWorkflows(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/workflows", "/v1/m/orchestration/workflows", undefined, opts);
  }

  /**
   * POST /v1/m/orchestration/workflows — Declares a workflow (write-tier, self-audited, revisioned).
   *
   * Stability: beta.
   */
  postV1MOrchestrationWorkflows(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/orchestration/workflows", "/v1/m/orchestration/workflows", body, opts);
  }

  /**
   * GET /v1/m/orchestration/workflows/{id} — Returns one workflow with its full canonical graph.
   *
   * Stability: beta.
   */
  getV1MOrchestrationWorkflowsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/workflows/{id}", `/v1/m/orchestration/workflows/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PATCH /v1/m/orchestration/workflows/{id} — Updates description/enabled (write-tier, revisioned).
   *
   * Stability: beta.
   */
  patchV1MOrchestrationWorkflowsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PATCH", "/v1/m/orchestration/workflows/{id}", `/v1/m/orchestration/workflows/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * POST /v1/m/orchestration/workflows/{id}/dry-run — Computes the execution plan for a workflow as declared, with ZERO effects (read-tier).
   *
   * Stability: beta.
   */
  postV1MOrchestrationWorkflowsByIdDryRun(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/orchestration/workflows/{id}/dry-run", `/v1/m/orchestration/workflows/${encodeURIComponent(id)}/dry-run`, undefined, opts);
  }

  /**
   * POST /v1/m/orchestration/workflows/{id}/restore — Re-applies the MUTABLE shape of an earlier revision — description, enabled, steps — through the exact same validation as the live verbs (the restore rule).
   *
   * Stability: beta.
   */
  postV1MOrchestrationWorkflowsByIdRestore(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/orchestration/workflows/{id}/restore", `/v1/m/orchestration/workflows/${encodeURIComponent(id)}/restore`, body, opts);
  }

  /**
   * GET /v1/m/orchestration/workflows/{id}/revisions — Lists a workflow's append-only revision ledger.
   *
   * Stability: beta.
   */
  getV1MOrchestrationWorkflowsByIdRevisions(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/workflows/{id}/revisions", `/v1/m/orchestration/workflows/${encodeURIComponent(id)}/revisions`, undefined, opts);
  }

  /**
   * POST /v1/m/orchestration/workflows/{id}/run — orchestration module route (requires orchestration:workflow:admin)
   *
   * Stability: beta.
   */
  postV1MOrchestrationWorkflowsByIdRun(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/orchestration/workflows/{id}/run", `/v1/m/orchestration/workflows/${encodeURIComponent(id)}/run`, body, opts);
  }

  /**
   * GET /v1/m/orchestration/workflows/{id}/runs — Lists a workflow's runs, newest-first pagination by id.
   *
   * Stability: beta.
   */
  getV1MOrchestrationWorkflowsByIdRuns(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/workflows/{id}/runs", `/v1/m/orchestration/workflows/${encodeURIComponent(id)}/runs`, undefined, opts);
  }

  /**
   * GET /v1/m/orchestration/workflows/{id}/runs/{run} — Returns one run's timeline.
   *
   * Stability: beta.
   */
  getV1MOrchestrationWorkflowsByIdRunsByRun(id: string, run: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/orchestration/workflows/{id}/runs/{run}", `/v1/m/orchestration/workflows/${encodeURIComponent(id)}/runs/${encodeURIComponent(run)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/orchestration/workflows/{id}/steps — Replaces the whole step graph atomically (write-tier, revisioned).
   *
   * Stability: beta.
   */
  putV1MOrchestrationWorkflowsByIdSteps(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/orchestration/workflows/{id}/steps", `/v1/m/orchestration/workflows/${encodeURIComponent(id)}/steps`, body, opts);
  }

  /**
   * GET /v1/m/posture/export — Assembles the posture projection inside ONE audited tenant scope and returns it.
   *
   * Stability: beta.
   */
  getV1MPostureExport(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/posture/export", "/v1/m/posture/export", undefined, opts);
  }

  /**
   * POST /v1/m/recording/ack — Records the operator's explicit AC-8 acknowledgement: it opens (or finds) the caller's active recording session and stamps consent.
   *
   * Stability: beta.
   */
  postV1MRecordingAck(opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/recording/ack", "/v1/m/recording/ack", undefined, opts);
  }

  /**
   * GET /v1/m/recording/config — Returns the resolved recording policy (defaults applied).
   *
   * Stability: beta.
   */
  getV1MRecordingConfig(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/recording/config", "/v1/m/recording/config", undefined, opts);
  }

  /**
   * PUT /v1/m/recording/config — Replaces the tenant's recording policy (admin; self-audited).
   *
   * Stability: beta.
   */
  putV1MRecordingConfig(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/recording/config", "/v1/m/recording/config", body, opts);
  }

  /**
   * GET /v1/m/recording/notice — Returns the caller's recording posture for this tenant: what is recorded, the consent mode, and whether THIS credential has an acknowledged active session.
   *
   * Stability: beta.
   */
  getV1MRecordingNotice(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/recording/notice", "/v1/m/recording/notice", undefined, opts);
  }

  /**
   * GET /v1/m/recording/sessions — Lists recording sessions (admin).
   *
   * Stability: beta.
   */
  getV1MRecordingSessions(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/recording/sessions", "/v1/m/recording/sessions", undefined, opts);
  }

  /**
   * GET /v1/m/recording/sessions/{id} — Returns one session's metadata (admin; self-audited).
   *
   * Stability: beta.
   */
  getV1MRecordingSessionsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/recording/sessions/{id}", `/v1/m/recording/sessions/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/recording/sessions/{id}/export — Returns the session and its full frame list as either a JSON document (format=json, default) or a plain-text timeline summary (format=summary).
   *
   * Stability: beta.
   */
  getV1MRecordingSessionsByIdExport(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/recording/sessions/{id}/export", `/v1/m/recording/sessions/${encodeURIComponent(id)}/export`, undefined, opts);
  }

  /**
   * GET /v1/m/recording/sessions/{id}/replay — Returns the forensic replay of one session: its session header, a page of frames (chain order), and the correlated audit-ledger events of the session's window — reconstruction, not blobs.
   *
   * Stability: beta.
   */
  getV1MRecordingSessionsByIdReplay(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/recording/sessions/{id}/replay", `/v1/m/recording/sessions/${encodeURIComponent(id)}/replay`, undefined, opts);
  }

  /**
   * POST /v1/m/recording/sessions/{id}/seal — Closes one ACTIVE session explicitly (admin).
   *
   * Stability: beta.
   */
  postV1MRecordingSessionsByIdSeal(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/recording/sessions/{id}/seal", `/v1/m/recording/sessions/${encodeURIComponent(id)}/seal`, undefined, opts);
  }

  /**
   * POST /v1/m/recording/sessions/{id}/summarize — Produces the optional Claude-backed reviewer summary of a session (admin).
   *
   * Stability: beta.
   */
  postV1MRecordingSessionsByIdSummarize(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/recording/sessions/{id}/summarize", `/v1/m/recording/sessions/${encodeURIComponent(id)}/summarize`, undefined, opts);
  }

  /**
   * GET /v1/m/recording/sessions/{id}/unified — Returns the combined forensic + operational view of one recording session in a single call: session header, frames (paginated), ledger correlation, chain-verification verdict, and the correlated operational timeline from the sessions module (via the TimelineResolver seam).
   *
   * Stability: beta.
   */
  getV1MRecordingSessionsByIdUnified(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/recording/sessions/{id}/unified", `/v1/m/recording/sessions/${encodeURIComponent(id)}/unified`, undefined, opts);
  }

  /**
   * GET /v1/m/recording/sessions/{id}/verify — Recomputes one session's frame chain and re-checks every ledger anchor (open, periodic, seal) against the chain — proves, never trusts.
   *
   * Stability: beta.
   */
  getV1MRecordingSessionsByIdVerify(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/recording/sessions/{id}/verify", `/v1/m/recording/sessions/${encodeURIComponent(id)}/verify`, undefined, opts);
  }

  /**
   * POST /v1/m/recording/sweep — Seals every idle ACTIVE session (admin; the lazy-seal safety net for credentials that never came back).
   *
   * Stability: beta.
   */
  postV1MRecordingSweep(opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/recording/sweep", "/v1/m/recording/sweep", undefined, opts);
  }

  /**
   * GET /v1/m/redteam/catalog — Returns the battery taxonomy (metadata + OWASP/ATLAS coverage).
   *
   * Stability: beta.
   */
  getV1MRedteamCatalog(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/redteam/catalog", "/v1/m/redteam/catalog", undefined, opts);
  }

  /**
   * GET /v1/m/redteam/runs — Lists the tenant's red-team runs (newest selectable by target).
   *
   * Stability: beta.
   */
  getV1MRedteamRuns(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/redteam/runs", "/v1/m/redteam/runs", undefined, opts);
  }

  /**
   * POST /v1/m/redteam/runs — Runs a battery against an AUTHORIZED target and records the scorecard.
   *
   * Stability: beta.
   */
  postV1MRedteamRuns(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/redteam/runs", "/v1/m/redteam/runs", body, opts);
  }

  /**
   * GET /v1/m/redteam/runs/{id} — Returns one run with its per-family breakdown recomputed from the stored result rows (the run row holds the aggregates; the breakdown is derived).
   *
   * Stability: beta.
   */
  getV1MRedteamRunsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/redteam/runs/{id}", `/v1/m/redteam/runs/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/redteam/runs/{id}/results — Lists one run's per-probe results.
   *
   * Stability: beta.
   */
  getV1MRedteamRunsByIdResults(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/redteam/runs/{id}/results", `/v1/m/redteam/runs/${encodeURIComponent(id)}/results`, undefined, opts);
  }

  /**
   * GET /v1/m/redteam/targets — Lists the tenant's registered targets.
   *
   * Stability: beta.
   */
  getV1MRedteamTargets(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/redteam/targets", "/v1/m/redteam/targets", undefined, opts);
  }

  /**
   * POST /v1/m/redteam/targets — Registers a client-governed agent as a candidate target.
   *
   * Stability: beta.
   */
  postV1MRedteamTargets(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/redteam/targets", "/v1/m/redteam/targets", body, opts);
  }

  /**
   * GET /v1/m/redteam/targets/{id} — Returns one target.
   *
   * Stability: beta.
   */
  getV1MRedteamTargetsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/redteam/targets/{id}", `/v1/m/redteam/targets/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/redteam/targets/{id}/authorize — Grants or revokes CONSENT to red-team a target.
   *
   * Stability: beta.
   */
  postV1MRedteamTargetsByIdAuthorize(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/redteam/targets/{id}/authorize", `/v1/m/redteam/targets/${encodeURIComponent(id)}/authorize`, body, opts);
  }

  /**
   * GET /v1/m/reporting/branding — reporting module route (requires reporting:report:read)
   *
   * Stability: beta.
   */
  getV1MReportingBranding(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/reporting/branding", "/v1/m/reporting/branding", undefined, opts);
  }

  /**
   * PUT /v1/m/reporting/branding — reporting module route (requires reporting:report:write)
   *
   * Stability: beta.
   */
  putV1MReportingBranding(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/reporting/branding", "/v1/m/reporting/branding", body, opts);
  }

  /**
   * GET /v1/m/reporting/enterprise/bundle — reporting module route (requires reporting:report:read)
   *
   * Stability: beta.
   */
  getV1MReportingEnterpriseBundle(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/reporting/enterprise/bundle", "/v1/m/reporting/enterprise/bundle", undefined, opts);
  }

  /**
   * GET /v1/m/reporting/enterprise/posture — reporting module route (requires reporting:report:read)
   *
   * Stability: beta.
   */
  getV1MReportingEnterprisePosture(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/reporting/enterprise/posture", "/v1/m/reporting/enterprise/posture", undefined, opts);
  }

  /**
   * GET /v1/m/reporting/enterprise/risk — reporting module route (requires reporting:report:read)
   *
   * Stability: beta.
   */
  getV1MReportingEnterpriseRisk(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/reporting/enterprise/risk", "/v1/m/reporting/enterprise/risk", undefined, opts);
  }

  /**
   * GET /v1/m/reporting/reports — reporting module route (requires reporting:report:read)
   *
   * Stability: beta.
   */
  getV1MReportingReports(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/reporting/reports", "/v1/m/reporting/reports", undefined, opts);
  }

  /**
   * GET /v1/m/reporting/reports/{type} — reporting module route (requires reporting:report:read)
   *
   * Stability: beta.
   */
  getV1MReportingReportsByType(type_: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/reporting/reports/{type}", `/v1/m/reporting/reports/${encodeURIComponent(type_)}`, undefined, opts);
  }

  /**
   * GET /v1/m/reporting/schedules — reporting module route (requires reporting:report:read)
   *
   * Stability: beta.
   */
  getV1MReportingSchedules(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/reporting/schedules", "/v1/m/reporting/schedules", undefined, opts);
  }

  /**
   * POST /v1/m/reporting/schedules — reporting module route (requires reporting:report:write)
   *
   * Stability: beta.
   */
  postV1MReportingSchedules(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/reporting/schedules", "/v1/m/reporting/schedules", body, opts);
  }

  /**
   * DELETE /v1/m/reporting/schedules/{id} — reporting module route (requires reporting:report:write)
   *
   * Stability: beta.
   */
  deleteV1MReportingSchedulesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/reporting/schedules/{id}", `/v1/m/reporting/schedules/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/reporting/schedules/{id}/runs — reporting module route (requires reporting:report:read)
   *
   * Stability: beta.
   */
  getV1MReportingSchedulesByIdRuns(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/reporting/schedules/{id}/runs", `/v1/m/reporting/schedules/${encodeURIComponent(id)}/runs`, undefined, opts);
  }

  /**
   * GET /v1/m/reporting/schedules/{id}/runs/{rid} — reporting module route (requires reporting:report:read)
   *
   * Stability: beta.
   */
  getV1MReportingSchedulesByIdRunsByRid(id: string, rid: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/reporting/schedules/{id}/runs/{rid}", `/v1/m/reporting/schedules/${encodeURIComponent(id)}/runs/${encodeURIComponent(rid)}`, undefined, opts);
  }

  /**
   * GET /v1/m/reporting/templates/{type} — reporting module route (requires reporting:report:read)
   *
   * Stability: beta.
   */
  getV1MReportingTemplatesByType(type_: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/reporting/templates/{type}", `/v1/m/reporting/templates/${encodeURIComponent(type_)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/reporting/templates/{type} — reporting module route (requires reporting:report:write)
   *
   * Stability: beta.
   */
  putV1MReportingTemplatesByType(type_: string, body: Uint8Array, opts?: RequestOptions): Promise<Json> {
    return this.doReqRawWithType("PUT", "/v1/m/reporting/templates/{type}", `/v1/m/reporting/templates/${encodeURIComponent(type_)}`, body, "text/html", opts);
  }

  /**
   * DELETE /v1/m/reporting/templates/{type} — reporting module route (requires reporting:report:write)
   *
   * Stability: beta.
   */
  deleteV1MReportingTemplatesByType(type_: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/reporting/templates/{type}", `/v1/m/reporting/templates/${encodeURIComponent(type_)}`, undefined, opts);
  }

  /**
   * POST /v1/m/sandbox/compare — Runs the SAME scenario/steps against two variants, scores both (if a Scorer is wired) or compares their deterministic outputs_hash, derives a verdict + delta, and persists two runs plus a sandbox_comparison (append-only deploy-decision evidence).
   *
   * Stability: beta.
   */
  postV1MSandboxCompare(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sandbox/compare", "/v1/m/sandbox/compare", body, opts);
  }

  /**
   * GET /v1/m/sandbox/comparisons — Lists the tenant's pre/post-deploy comparisons.
   *
   * Stability: beta.
   */
  getV1MSandboxComparisons(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sandbox/comparisons", "/v1/m/sandbox/comparisons", undefined, opts);
  }

  /**
   * GET /v1/m/sandbox/comparisons/{id} — Returns one comparison.
   *
   * Stability: beta.
   */
  getV1MSandboxComparisonsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sandbox/comparisons/{id}", `/v1/m/sandbox/comparisons/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/sandbox/replay — Reconstructs a historical session's input timeline via HistorySource and re-executes it DETERMINISTICALLY against the supplied mocks.
   *
   * Stability: beta.
   */
  postV1MSandboxReplay(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sandbox/replay", "/v1/m/sandbox/replay", body, opts);
  }

  /**
   * GET /v1/m/sandbox/runs — Lists the tenant's runs (optionally filtered by kind / scenario).
   *
   * Stability: beta.
   */
  getV1MSandboxRuns(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sandbox/runs", "/v1/m/sandbox/runs", undefined, opts);
  }

  /**
   * GET /v1/m/sandbox/runs/{id} — Returns one run.
   *
   * Stability: beta.
   */
  getV1MSandboxRunsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sandbox/runs/{id}", `/v1/m/sandbox/runs/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/sandbox/runs/{id}/outputs — Lists one run's per-step outputs.
   *
   * Stability: beta.
   */
  getV1MSandboxRunsByIdOutputs(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sandbox/runs/{id}/outputs", `/v1/m/sandbox/runs/${encodeURIComponent(id)}/outputs`, undefined, opts);
  }

  /**
   * GET /v1/m/sandbox/runs/{id}/stream — sandbox module route (requires sandbox:run:read)
   *
   * Stability: beta.
   */
  getV1MSandboxRunsByIdStream(id: string, opts?: RequestOptions): Promise<string> {
    return this.doRaw("GET", "/v1/m/sandbox/runs/{id}/stream", `/v1/m/sandbox/runs/${encodeURIComponent(id)}/stream`, opts);
  }

  /**
   * GET /v1/m/sandbox/scenarios — Lists the tenant's scenarios (optionally filtered by status).
   *
   * Stability: beta.
   */
  getV1MSandboxScenarios(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sandbox/scenarios", "/v1/m/sandbox/scenarios", undefined, opts);
  }

  /**
   * POST /v1/m/sandbox/scenarios — Records a synthetic, operator-authored scenario fixture.
   *
   * Stability: beta.
   */
  postV1MSandboxScenarios(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sandbox/scenarios", "/v1/m/sandbox/scenarios", body, opts);
  }

  /**
   * GET /v1/m/sandbox/scenarios/{id} — Returns one scenario.
   *
   * Stability: beta.
   */
  getV1MSandboxScenariosById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sandbox/scenarios/{id}", `/v1/m/sandbox/scenarios/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/sandbox/scenarios/{id}/archive — Marks a scenario archived (admin-tier + self-audited).
   *
   * Stability: beta.
   */
  postV1MSandboxScenariosByIdArchive(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/sandbox/scenarios/{id}/archive", `/v1/m/sandbox/scenarios/${encodeURIComponent(id)}/archive`, undefined, opts);
  }

  /**
   * POST /v1/m/sandbox/scenarios/{id}/run — Loads a scenario, runs its steps against the isolated runner, persists the run + per-step outputs, and (if a suite_ref is given AND a Scorer is wired) scores the outputs.
   *
   * Stability: beta.
   */
  postV1MSandboxScenariosByIdRun(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sandbox/scenarios/{id}/run", `/v1/m/sandbox/scenarios/${encodeURIComponent(id)}/run`, body, opts);
  }

  /**
   * GET /v1/m/security/anomalies — Computes the tenant's prioritized anomalies on demand: the permitted-vs-observed drift (consumed via the store, not recomputed), egress/exfil-relevant drift labeled from the resource, and the correlated/high security findings (incl. the joined anti_evasion mark).
   *
   * Stability: beta.
   */
  getV1MSecurityAnomalies(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/security/anomalies", "/v1/m/security/anomalies", undefined, opts);
  }

  /**
   * GET /v1/m/security/cases — Lists the tenant's forensic cases.
   *
   * Stability: beta.
   */
  getV1MSecurityCases(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/security/cases", "/v1/m/security/cases", undefined, opts);
  }

  /**
   * POST /v1/m/security/cases — Opens an incident case, snapshotting the ledger integrity at the moment of opening (so a later tamper is detectable against the open-time state).
   *
   * Stability: beta.
   */
  postV1MSecurityCases(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/security/cases", "/v1/m/security/cases", body, opts);
  }

  /**
   * GET /v1/m/security/cases/{id} — Returns one case.
   *
   * Stability: beta.
   */
  getV1MSecurityCasesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/security/cases/{id}", `/v1/m/security/cases/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PATCH /v1/m/security/cases/{id} — Updates a case's lifecycle (status/severity/summary), self-audited.
   *
   * Stability: beta.
   */
  patchV1MSecurityCasesById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PATCH", "/v1/m/security/cases/{id}", `/v1/m/security/cases/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * GET /v1/m/security/cases/{id}/export — Exports a case's relevant ledger events in a SIEM format (every format audit.Formats() lists), re-verifiable offline.
   *
   * Stability: beta.
   */
  getV1MSecurityCasesByIdExport(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/security/cases/{id}/export", `/v1/m/security/cases/${encodeURIComponent(id)}/export`, undefined, opts);
  }

  /**
   * GET /v1/m/security/cases/{id}/links — Lists a case's chain of custody.
   *
   * Stability: beta.
   */
  getV1MSecurityCasesByIdLinks(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/security/cases/{id}/links", `/v1/m/security/cases/${encodeURIComponent(id)}/links`, undefined, opts);
  }

  /**
   * POST /v1/m/security/cases/{id}/links — Appends one immutable chain-of-custody link attaching a finding / ledger sequence / anomaly / note to a case.
   *
   * Stability: beta.
   */
  postV1MSecurityCasesByIdLinks(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/security/cases/{id}/links", `/v1/m/security/cases/${encodeURIComponent(id)}/links`, body, opts);
  }

  /**
   * GET /v1/m/security/cases/{id}/timeline — Reconstructs a case's timeline from the append-only ledger and VERIFIES the chain and (where a checkpoint key is wired) the signed checkpoints, then enriches it with attribution (identity), least-privilege drift and data lineage.
   *
   * Stability: beta.
   */
  getV1MSecurityCasesByIdTimeline(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/security/cases/{id}/timeline", `/v1/m/security/cases/${encodeURIComponent(id)}/timeline`, undefined, opts);
  }

  /**
   * GET /v1/m/security/enforcement — Returns the tenant's inline-enforcement posture (all classes).
   *
   * Stability: beta.
   */
  getV1MSecurityEnforcement(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/security/enforcement", "/v1/m/security/enforcement", undefined, opts);
  }

  /**
   * PUT /v1/m/security/enforcement — Sets the inline-enforcement posture for a guardrail class.
   *
   * Stability: beta.
   */
  putV1MSecurityEnforcement(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/security/enforcement", "/v1/m/security/enforcement", body, opts);
  }

  /**
   * GET /v1/m/security/findings — Lists the tenant's findings, filterable by kind/severity/ status/source.
   *
   * Stability: beta.
   */
  getV1MSecurityFindings(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/security/findings", "/v1/m/security/findings", undefined, opts);
  }

  /**
   * GET /v1/m/security/findings/export — Exports a complete filtered snapshot.
   *
   * Stability: beta.
   */
  getV1MSecurityFindingsExport(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/security/findings/export", "/v1/m/security/findings/export", undefined, opts);
  }

  /**
   * GET /v1/m/security/findings/{id} — Returns one finding.
   *
   * Stability: beta.
   */
  getV1MSecurityFindingsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/security/findings/{id}", `/v1/m/security/findings/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PATCH /v1/m/security/findings/{id} — Updates a finding's triage state (open/triaged/resolved/ dismissed).
   *
   * Stability: beta.
   */
  patchV1MSecurityFindingsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PATCH", "/v1/m/security/findings/{id}", `/v1/m/security/findings/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * POST /v1/m/security/guardrails/inspect — Runs the guardrails over a submitted piece of agent text.
   *
   * Stability: beta.
   */
  postV1MSecurityGuardrailsInspect(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/security/guardrails/inspect", "/v1/m/security/guardrails/inspect", body, opts);
  }

  /**
   * GET /v1/m/security/integrity/verify — security module route (requires security:integrity:read)
   *
   * Stability: beta.
   */
  getV1MSecurityIntegrityVerify(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/security/integrity/verify", "/v1/m/security/integrity/verify", undefined, opts);
  }

  /**
   * GET /v1/m/security/safety-posture — Aggregates the tenant's provider safety-posture findings (the OpenAI Moderation / AWS Bedrock Guardrails / Azure RAI read-first posture) into a per-provider-surface roll-up.
   *
   * Stability: beta.
   */
  getV1MSecuritySafetyPosture(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/security/safety-posture", "/v1/m/security/safety-posture", undefined, opts);
  }

  /**
   * GET /v1/m/sessions/decisions — sessions module route (requires sessions:decision:read)
   *
   * Stability: beta.
   */
  getV1MSessionsDecisions(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/decisions", "/v1/m/sessions/decisions", undefined, opts);
  }

  /**
   * POST /v1/m/sessions/decisions — sessions module route (requires sessions:decision:write)
   *
   * Stability: beta.
   */
  postV1MSessionsDecisions(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/decisions", "/v1/m/sessions/decisions", body, opts);
  }

  /**
   * GET /v1/m/sessions/decisions/{id} — sessions module route (requires sessions:decision:read)
   *
   * Stability: beta.
   */
  getV1MSessionsDecisionsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/decisions/{id}", `/v1/m/sessions/decisions/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/sessions/decisions/{id}/revoke — sessions module route (requires sessions:decision:admin)
   *
   * Stability: beta.
   */
  postV1MSessionsDecisionsByIdRevoke(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/decisions/{id}/revoke", `/v1/m/sessions/decisions/${encodeURIComponent(id)}/revoke`, body, opts);
  }

  /**
   * GET /v1/m/sessions/leases — sessions module route (requires sessions:lease:read)
   *
   * Stability: beta.
   */
  getV1MSessionsLeases(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/leases", "/v1/m/sessions/leases", undefined, opts);
  }

  /**
   * GET /v1/m/sessions/live — Lists live sessions, most-recently-active first.
   *
   * Stability: beta.
   */
  getV1MSessionsLive(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/live", "/v1/m/sessions/live", undefined, opts);
  }

  /**
   * GET /v1/m/sessions/live/{ref} — Returns the full live operation of one session by its reference.
   *
   * Stability: beta.
   */
  getV1MSessionsLiveByRef(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/live/{ref}", `/v1/m/sessions/live/${encodeURIComponent(ref)}`, undefined, opts);
  }

  /**
   * GET /v1/m/sessions/live/{ref}/timeline — Returns a session's reconstructable timeline in chronological (ingestion) order, keyset-paginated by the time-ordered row id.
   *
   * Stability: beta.
   */
  getV1MSessionsLiveByRefTimeline(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/live/{ref}/timeline", `/v1/m/sessions/live/${encodeURIComponent(ref)}/timeline`, undefined, opts);
  }

  /**
   * GET /v1/m/sessions/protocol-binding-specs — Lists protocol binding spec generations in one workspace.
   *
   * Stability: beta.
   */
  getV1MSessionsProtocolBindingSpecs(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/protocol-binding-specs", "/v1/m/sessions/protocol-binding-specs", undefined, opts);
  }

  /**
   * POST /v1/m/sessions/protocol-binding-specs — Records a new draft generation of a protocol binding spec.
   *
   * Stability: beta.
   */
  postV1MSessionsProtocolBindingSpecs(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/protocol-binding-specs", "/v1/m/sessions/protocol-binding-specs", body, opts);
  }

  /**
   * GET /v1/m/sessions/protocol-binding-specs/{id} — Returns one immutable protocol binding spec generation.
   *
   * Stability: beta.
   */
  getV1MSessionsProtocolBindingSpecsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/protocol-binding-specs/{id}", `/v1/m/sessions/protocol-binding-specs/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/sessions/protocol-binding-specs/{id}/activate — Makes one spec generation the active desired state for its binding.
   *
   * Stability: beta.
   */
  postV1MSessionsProtocolBindingSpecsByIdActivate(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/sessions/protocol-binding-specs/{id}/activate", `/v1/m/sessions/protocol-binding-specs/${encodeURIComponent(id)}/activate`, body, opts);
  }

  /**
   * POST /v1/m/sessions/protocol-binding-specs/{id}/disable — Stops one spec generation from being the active desired state.
   *
   * Stability: beta.
   */
  postV1MSessionsProtocolBindingSpecsByIdDisable(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/sessions/protocol-binding-specs/{id}/disable", `/v1/m/sessions/protocol-binding-specs/${encodeURIComponent(id)}/disable`, body, opts);
  }

  /**
   * GET /v1/m/sessions/protocol-bindings — Lists the protocol bindings visible to the caller, filtered and paginated by the query.
   *
   * Stability: beta.
   */
  getV1MSessionsProtocolBindings(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/protocol-bindings", "/v1/m/sessions/protocol-bindings", undefined, opts);
  }

  /**
   * GET /v1/m/sessions/protocol-bindings/{id} — Returns one protocol binding in the caller's tenant.
   *
   * Stability: beta.
   */
  getV1MSessionsProtocolBindingsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/protocol-bindings/{id}", `/v1/m/sessions/protocol-bindings/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/sessions/protocol-bindings/{id}/reconcile — Drives one binding towards its desired state.
   *
   * Stability: beta.
   */
  postV1MSessionsProtocolBindingsByIdReconcile(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/sessions/protocol-bindings/{id}/reconcile", `/v1/m/sessions/protocol-bindings/${encodeURIComponent(id)}/reconcile`, body, opts);
  }

  /**
   * GET /v1/m/sessions/runs — sessions module route (requires sessions:run:read)
   *
   * Stability: beta.
   */
  getV1MSessionsRuns(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/runs", "/v1/m/sessions/runs", undefined, opts);
  }

  /**
   * POST /v1/m/sessions/runs — sessions module route (requires sessions:run:write)
   *
   * Stability: beta.
   */
  postV1MSessionsRuns(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/runs", "/v1/m/sessions/runs", body, opts);
  }

  /**
   * GET /v1/m/sessions/runs/{ref} — sessions module route (requires sessions:run:read)
   *
   * Stability: beta.
   */
  getV1MSessionsRunsByRef(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/runs/{ref}", `/v1/m/sessions/runs/${encodeURIComponent(ref)}`, undefined, opts);
  }

  /**
   * DELETE /v1/m/sessions/runs/{ref} — sessions module route (requires sessions:run:admin)
   *
   * Stability: beta.
   */
  deleteV1MSessionsRunsByRef(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/sessions/runs/{ref}", `/v1/m/sessions/runs/${encodeURIComponent(ref)}`, undefined, opts);
  }

  /**
   * GET /v1/m/sessions/runs/{ref}/attach — Streams a live session's bridged I/O as server-sent events, replaying from an optional ?from=<seq> cursor.
   *
   * Stability: beta.
   */
  getV1MSessionsRunsByRefAttach(ref: string, opts?: RequestOptions): Promise<string> {
    return this.doRaw("GET", "/v1/m/sessions/runs/{ref}/attach", `/v1/m/sessions/runs/${encodeURIComponent(ref)}/attach`, opts);
  }

  /**
   * POST /v1/m/sessions/runs/{ref}/cleanup — sessions module route (requires sessions:run:admin)
   *
   * Stability: beta.
   */
  postV1MSessionsRunsByRefCleanup(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/sessions/runs/{ref}/cleanup", `/v1/m/sessions/runs/${encodeURIComponent(ref)}/cleanup`, undefined, opts);
  }

  /**
   * GET /v1/m/sessions/runs/{ref}/events — sessions module route (requires sessions:run:read)
   *
   * Stability: beta.
   */
  getV1MSessionsRunsByRefEvents(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/runs/{ref}/events", `/v1/m/sessions/runs/${encodeURIComponent(ref)}/events`, undefined, opts);
  }

  /**
   * POST /v1/m/sessions/runs/{ref}/input — sessions module route (requires sessions:run:write)
   *
   * Stability: beta.
   */
  postV1MSessionsRunsByRefInput(ref: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/runs/{ref}/input", `/v1/m/sessions/runs/${encodeURIComponent(ref)}/input`, body, opts);
  }

  /**
   * POST /v1/m/sessions/runs/{ref}/resume — sessions module route (requires sessions:run:write)
   *
   * Stability: beta.
   */
  postV1MSessionsRunsByRefResume(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/sessions/runs/{ref}/resume", `/v1/m/sessions/runs/${encodeURIComponent(ref)}/resume`, undefined, opts);
  }

  /**
   * POST /v1/m/sessions/runs/{ref}/stop — sessions module route (requires sessions:run:write)
   *
   * Stability: beta.
   */
  postV1MSessionsRunsByRefStop(ref: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/sessions/runs/{ref}/stop", `/v1/m/sessions/runs/${encodeURIComponent(ref)}/stop`, body, opts);
  }

  /**
   * GET /v1/m/sessions/stream — Serves the live operation as server-sent events.
   *
   * Stability: beta.
   */
  getV1MSessionsStream(opts?: RequestOptions): Promise<string> {
    return this.doRaw("GET", "/v1/m/sessions/stream", "/v1/m/sessions/stream", opts);
  }

  /**
   * GET /v1/m/sessions/templates — sessions module route (requires sessions:template:read)
   *
   * Stability: beta.
   */
  getV1MSessionsTemplates(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/templates", "/v1/m/sessions/templates", undefined, opts);
  }

  /**
   * POST /v1/m/sessions/templates — sessions module route (requires sessions:template:write)
   *
   * Stability: beta.
   */
  postV1MSessionsTemplates(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/templates", "/v1/m/sessions/templates", body, opts);
  }

  /**
   * GET /v1/m/sessions/templates/{id} — sessions module route (requires sessions:template:read)
   *
   * Stability: beta.
   */
  getV1MSessionsTemplatesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/templates/{id}", `/v1/m/sessions/templates/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/sessions/templates/{id} — sessions module route (requires sessions:template:write)
   *
   * Stability: beta.
   */
  putV1MSessionsTemplatesById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/sessions/templates/{id}", `/v1/m/sessions/templates/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/sessions/templates/{id} — sessions module route (requires sessions:template:admin)
   *
   * Stability: beta.
   */
  deleteV1MSessionsTemplatesById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/sessions/templates/{id}", `/v1/m/sessions/templates/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/sessions/templates/{id}/apply — sessions module route (requires sessions:template:read)
   *
   * Stability: beta.
   */
  postV1MSessionsTemplatesByIdApply(id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/sessions/templates/{id}/apply", `/v1/m/sessions/templates/${encodeURIComponent(id)}/apply`, body, opts);
  }

  /**
   * POST /v1/m/sessions/templates/{id}/duplicate — sessions module route (requires sessions:template:write)
   *
   * Stability: beta.
   */
  postV1MSessionsTemplatesByIdDuplicate(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/templates/{id}/duplicate", `/v1/m/sessions/templates/${encodeURIComponent(id)}/duplicate`, body, opts);
  }

  /**
   * POST /v1/m/sessions/work-events/{event_id}/replay — sessions module route (requires sessions:work:admin)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkEventsByEventIdReplay(event_id: string, body?: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/sessions/work-events/{event_id}/replay", `/v1/m/sessions/work-events/${encodeURIComponent(event_id)}/replay`, body, opts);
  }

  /**
   * GET /v1/m/sessions/work-items — sessions module route (requires sessions:work:read)
   *
   * Stability: beta.
   */
  getV1MSessionsWorkItems(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/work-items", "/v1/m/sessions/work-items", undefined, opts);
  }

  /**
   * POST /v1/m/sessions/work-items — sessions module route (requires sessions:work:write)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkItems(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/work-items", "/v1/m/sessions/work-items", body, opts);
  }

  /**
   * GET /v1/m/sessions/work-items/{id} — sessions module route (requires sessions:work:read)
   *
   * Stability: beta.
   */
  getV1MSessionsWorkItemsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/work-items/{id}", `/v1/m/sessions/work-items/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PATCH /v1/m/sessions/work-items/{id} — sessions module route (requires sessions:work:write)
   *
   * Stability: beta.
   */
  patchV1MSessionsWorkItemsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PATCH", "/v1/m/sessions/work-items/{id}", `/v1/m/sessions/work-items/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * GET /v1/m/sessions/work-items/{id}/acceptance — sessions module route (requires sessions:work:read)
   *
   * Stability: beta.
   */
  getV1MSessionsWorkItemsByIdAcceptance(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/work-items/{id}/acceptance", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/acceptance`, undefined, opts);
  }

  /**
   * POST /v1/m/sessions/work-items/{id}/acceptance — sessions module route (requires sessions:work:write)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkItemsByIdAcceptance(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/work-items/{id}/acceptance", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/acceptance`, body, opts);
  }

  /**
   * PATCH /v1/m/sessions/work-items/{id}/acceptance/{criterion_id} — sessions module route (requires sessions:work:write)
   *
   * Stability: beta.
   */
  patchV1MSessionsWorkItemsByIdAcceptanceByCriterionId(id: string, criterion_id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PATCH", "/v1/m/sessions/work-items/{id}/acceptance/{criterion_id}", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/acceptance/${encodeURIComponent(criterion_id)}`, body, opts);
  }

  /**
   * POST /v1/m/sessions/work-items/{id}/assignments — sessions module route (requires sessions:work:admin)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkItemsByIdAssignments(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/work-items/{id}/assignments", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/assignments`, body, opts);
  }

  /**
   * GET /v1/m/sessions/work-items/{id}/dependencies — sessions module route (requires sessions:work:read)
   *
   * Stability: beta.
   */
  getV1MSessionsWorkItemsByIdDependencies(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/work-items/{id}/dependencies", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/dependencies`, undefined, opts);
  }

  /**
   * POST /v1/m/sessions/work-items/{id}/dependencies — sessions module route (requires sessions:work:write)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkItemsByIdDependencies(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/work-items/{id}/dependencies", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/dependencies`, body, opts);
  }

  /**
   * DELETE /v1/m/sessions/work-items/{id}/dependencies/{dep_id} — sessions module route (requires sessions:work:write)
   *
   * Stability: beta.
   */
  deleteV1MSessionsWorkItemsByIdDependenciesByDepId(id: string, dep_id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("DELETE", "/v1/m/sessions/work-items/{id}/dependencies/{dep_id}", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/dependencies/${encodeURIComponent(dep_id)}`, body, opts);
  }

  /**
   * GET /v1/m/sessions/work-items/{id}/events — sessions module route (requires sessions:work:read)
   *
   * Stability: beta.
   */
  getV1MSessionsWorkItemsByIdEvents(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/work-items/{id}/events", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/events`, undefined, opts);
  }

  /**
   * GET /v1/m/sessions/work-items/{id}/lease — sessions module route (requires sessions:lease:read)
   *
   * Stability: beta.
   */
  getV1MSessionsWorkItemsByIdLease(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/work-items/{id}/lease", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/lease`, undefined, opts);
  }

  /**
   * POST /v1/m/sessions/work-items/{id}/lease/acquire — sessions module route (requires sessions:lease:write)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkItemsByIdLeaseAcquire(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/work-items/{id}/lease/acquire", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/lease/acquire`, body, opts);
  }

  /**
   * POST /v1/m/sessions/work-items/{id}/lease/clock-rebase — sessions module route (requires sessions:lease:admin)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkItemsByIdLeaseClockRebase(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/work-items/{id}/lease/clock-rebase", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/lease/clock-rebase`, body, opts);
  }

  /**
   * POST /v1/m/sessions/work-items/{id}/lease/release — sessions module route (requires sessions:lease:write)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkItemsByIdLeaseRelease(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/work-items/{id}/lease/release", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/lease/release`, body, opts);
  }

  /**
   * POST /v1/m/sessions/work-items/{id}/lease/renew — sessions module route (requires sessions:lease:write)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkItemsByIdLeaseRenew(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/work-items/{id}/lease/renew", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/lease/renew`, body, opts);
  }

  /**
   * POST /v1/m/sessions/work-items/{id}/lease/revoke — sessions module route (requires sessions:lease:admin)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkItemsByIdLeaseRevoke(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/work-items/{id}/lease/revoke", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/lease/revoke`, body, opts);
  }

  /**
   * POST /v1/m/sessions/work-items/{id}/lease/takeover — sessions module route (requires sessions:lease:admin)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkItemsByIdLeaseTakeover(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/work-items/{id}/lease/takeover", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/lease/takeover`, body, opts);
  }

  /**
   * POST /v1/m/sessions/work-items/{id}/transitions — sessions module route (requires sessions:work:write)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkItemsByIdTransitions(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/work-items/{id}/transitions", `/v1/m/sessions/work-items/${encodeURIComponent(id)}/transitions`, body, opts);
  }

  /**
   * GET /v1/m/sessions/work-stream — sessions module route (requires sessions:work:read)
   *
   * Stability: beta.
   */
  getV1MSessionsWorkStream(opts?: RequestOptions): Promise<string> {
    return this.doRaw("GET", "/v1/m/sessions/work-stream", "/v1/m/sessions/work-stream", opts);
  }

  /**
   * GET /v1/m/sessions/workspaces — sessions module route (requires sessions:workspace:read)
   *
   * Stability: beta.
   */
  getV1MSessionsWorkspaces(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/workspaces", "/v1/m/sessions/workspaces", undefined, opts);
  }

  /**
   * POST /v1/m/sessions/workspaces — sessions module route (requires sessions:workspace:admin)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkspaces(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/workspaces", "/v1/m/sessions/workspaces", body, opts);
  }

  /**
   * GET /v1/m/sessions/workspaces/{ref} — sessions module route (requires sessions:workspace:read)
   *
   * Stability: beta.
   */
  getV1MSessionsWorkspacesByRef(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/workspaces/{ref}", `/v1/m/sessions/workspaces/${encodeURIComponent(ref)}`, undefined, opts);
  }

  /**
   * DELETE /v1/m/sessions/workspaces/{ref} — sessions module route (requires sessions:workspace:admin)
   *
   * Stability: beta.
   */
  deleteV1MSessionsWorkspacesByRef(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/sessions/workspaces/{ref}", `/v1/m/sessions/workspaces/${encodeURIComponent(ref)}`, undefined, opts);
  }

  /**
   * GET /v1/m/sessions/workspaces/{ref}/files — sessions module route (requires sessions:workspace:read)
   *
   * Stability: beta.
   */
  getV1MSessionsWorkspacesByRefFiles(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/workspaces/{ref}/files", `/v1/m/sessions/workspaces/${encodeURIComponent(ref)}/files`, undefined, opts);
  }

  /**
   * DELETE /v1/m/sessions/workspaces/{ref}/files — sessions module route (requires sessions:workspace:write)
   *
   * Stability: beta.
   */
  deleteV1MSessionsWorkspacesByRefFiles(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/sessions/workspaces/{ref}/files", `/v1/m/sessions/workspaces/${encodeURIComponent(ref)}/files`, undefined, opts);
  }

  /**
   * POST /v1/m/sessions/workspaces/{ref}/files/dir — sessions module route (requires sessions:workspace:write)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkspacesByRefFilesDir(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/sessions/workspaces/{ref}/files/dir", `/v1/m/sessions/workspaces/${encodeURIComponent(ref)}/files/dir`, undefined, opts);
  }

  /**
   * POST /v1/m/sessions/workspaces/{ref}/files/move — sessions module route (requires sessions:workspace:write)
   *
   * Stability: beta.
   */
  postV1MSessionsWorkspacesByRefFilesMove(ref: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sessions/workspaces/{ref}/files/move", `/v1/m/sessions/workspaces/${encodeURIComponent(ref)}/files/move`, body, opts);
  }

  /**
   * GET /v1/m/sessions/workspaces/{ref}/files/raw — sessions module route (requires sessions:workspace:read)
   *
   * Stability: beta.
   */
  getV1MSessionsWorkspacesByRefFilesRaw(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/workspaces/{ref}/files/raw", `/v1/m/sessions/workspaces/${encodeURIComponent(ref)}/files/raw`, undefined, opts);
  }

  /**
   * PUT /v1/m/sessions/workspaces/{ref}/files/raw — sessions module route (requires sessions:workspace:write)
   *
   * Stability: beta.
   */
  putV1MSessionsWorkspacesByRefFilesRaw(ref: string, body: Uint8Array, opts?: RequestOptions): Promise<Json> {
    return this.doReqRawWithType("PUT", "/v1/m/sessions/workspaces/{ref}/files/raw", `/v1/m/sessions/workspaces/${encodeURIComponent(ref)}/files/raw`, body, "application/octet-stream", opts);
  }

  /**
   * GET /v1/m/sessions/workspaces/{ref}/files/stat — sessions module route (requires sessions:workspace:read)
   *
   * Stability: beta.
   */
  getV1MSessionsWorkspacesByRefFilesStat(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sessions/workspaces/{ref}/files/stat", `/v1/m/sessions/workspaces/${encodeURIComponent(ref)}/files/stat`, undefined, opts);
  }

  /**
   * GET /v1/m/sourcescope/assignments — Lists connector→workspace assignments, optionally filtered by ?connector_name / ?workspace_ref.
   *
   * Stability: beta.
   */
  getV1MSourcescopeAssignments(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sourcescope/assignments", "/v1/m/sourcescope/assignments", undefined, opts);
  }

  /**
   * POST /v1/m/sourcescope/assignments — Assigns a global connector to a workspace.
   *
   * Stability: beta.
   */
  postV1MSourcescopeAssignments(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sourcescope/assignments", "/v1/m/sourcescope/assignments", body, opts);
  }

  /**
   * GET /v1/m/sourcescope/assignments/{id} — Returns one assignment.
   *
   * Stability: beta.
   */
  getV1MSourcescopeAssignmentsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sourcescope/assignments/{id}", `/v1/m/sourcescope/assignments/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/sourcescope/assignments/{id} — Updates an assignment (enabled, note).
   *
   * Stability: beta.
   */
  putV1MSourcescopeAssignmentsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/sourcescope/assignments/{id}", `/v1/m/sourcescope/assignments/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/sourcescope/assignments/{id} — Removes a connector→workspace assignment.
   *
   * Stability: beta.
   */
  deleteV1MSourcescopeAssignmentsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/sourcescope/assignments/{id}", `/v1/m/sourcescope/assignments/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/sourcescope/bindings — Lists the tenant's source→scope bindings, optionally filtered by ?source_type / ?source_ref / ?scope_tree.
   *
   * Stability: beta.
   */
  getV1MSourcescopeBindings(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sourcescope/bindings", "/v1/m/sourcescope/bindings", undefined, opts);
  }

  /**
   * POST /v1/m/sourcescope/bindings — Binds a connected source to a workspace/agent-group, recording a self-audit attributed to the real principal.
   *
   * Stability: beta.
   */
  postV1MSourcescopeBindings(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sourcescope/bindings", "/v1/m/sourcescope/bindings", body, opts);
  }

  /**
   * GET /v1/m/sourcescope/bindings/{id} — Returns one binding.
   *
   * Stability: beta.
   */
  getV1MSourcescopeBindingsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sourcescope/bindings/{id}", `/v1/m/sourcescope/bindings/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/sourcescope/bindings/{id} — Updates a binding in place (scope, credential reference, enabled, note).
   *
   * Stability: beta.
   */
  putV1MSourcescopeBindingsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/sourcescope/bindings/{id}", `/v1/m/sourcescope/bindings/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/sourcescope/bindings/{id} — Removes a binding (the source reverts to global/unbound, or to any remaining bindings).
   *
   * Stability: beta.
   */
  deleteV1MSourcescopeBindingsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/sourcescope/bindings/{id}", `/v1/m/sourcescope/bindings/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/sourcescope/guard-postures — Lists active guard-posture overrides.
   *
   * Stability: beta.
   */
  getV1MSourcescopeGuardPostures(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sourcescope/guard-postures", "/v1/m/sourcescope/guard-postures", undefined, opts);
  }

  /**
   * PUT /v1/m/sourcescope/guard-postures — sourcescope module route (requires sourcescope:binding:write)
   *
   * Stability: beta.
   */
  putV1MSourcescopeGuardPostures(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/sourcescope/guard-postures", "/v1/m/sourcescope/guard-postures", body, opts);
  }

  /**
   * GET /v1/m/sourcescope/posture-requests — Lists posture-change requests, optionally filtered by ?status / ?source_type / ?source_ref (default surfaces the pending queue for reviewers).
   *
   * Stability: beta.
   */
  getV1MSourcescopePostureRequests(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sourcescope/posture-requests", "/v1/m/sourcescope/posture-requests", undefined, opts);
  }

  /**
   * GET /v1/m/sourcescope/posture-requests/{id} — Returns one posture-change request.
   *
   * Stability: beta.
   */
  getV1MSourcescopePostureRequestsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sourcescope/posture-requests/{id}", `/v1/m/sourcescope/posture-requests/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * POST /v1/m/sourcescope/posture-requests/{id}/approve — sourcescope module route (requires sourcescope:posture:admin)
   *
   * Stability: beta.
   */
  postV1MSourcescopePostureRequestsByIdApprove(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/sourcescope/posture-requests/{id}/approve", `/v1/m/sourcescope/posture-requests/${encodeURIComponent(id)}/approve`, undefined, opts);
  }

  /**
   * POST /v1/m/sourcescope/posture-requests/{id}/reject — Rejects a pending relaxation (no change applied).
   *
   * Stability: beta.
   */
  postV1MSourcescopePostureRequestsByIdReject(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("POST", "/v1/m/sourcescope/posture-requests/{id}/reject", `/v1/m/sourcescope/posture-requests/${encodeURIComponent(id)}/reject`, undefined, opts);
  }

  /**
   * GET /v1/m/sourcescope/resolve — Answers "what would the runtime resolver decide for this actor and source" — the console's binding-effect preview.
   *
   * Stability: beta.
   */
  getV1MSourcescopeResolve(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sourcescope/resolve", "/v1/m/sourcescope/resolve", undefined, opts);
  }

  /**
   * GET /v1/m/sourcescope/resources — sourcescope module route (requires sourcescope:binding:read)
   *
   * Stability: beta.
   */
  getV1MSourcescopeResources(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sourcescope/resources", "/v1/m/sourcescope/resources", undefined, opts);
  }

  /**
   * POST /v1/m/sourcescope/sources/disable-scoping — Proposes disabling ALL scoping for a source (it becomes global).
   *
   * Stability: beta.
   */
  postV1MSourcescopeSourcesDisableScoping(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sourcescope/sources/disable-scoping", "/v1/m/sourcescope/sources/disable-scoping", body, opts);
  }

  /**
   * GET /v1/m/sourcescope/workspace-connectors — Lists workspace-scoped connectors, optionally filtered by ?workspace_ref / ?kind.
   *
   * Stability: beta.
   */
  getV1MSourcescopeWorkspaceConnectors(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sourcescope/workspace-connectors", "/v1/m/sourcescope/workspace-connectors", undefined, opts);
  }

  /**
   * POST /v1/m/sourcescope/workspace-connectors — Creates a workspace-scoped connector.
   *
   * Stability: beta.
   */
  postV1MSourcescopeWorkspaceConnectors(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/sourcescope/workspace-connectors", "/v1/m/sourcescope/workspace-connectors", body, opts);
  }

  /**
   * GET /v1/m/sourcescope/workspace-connectors/{id} — Returns one workspace connector.
   *
   * Stability: beta.
   */
  getV1MSourcescopeWorkspaceConnectorsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/sourcescope/workspace-connectors/{id}", `/v1/m/sourcescope/workspace-connectors/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * PUT /v1/m/sourcescope/workspace-connectors/{id} — Updates a workspace connector.
   *
   * Stability: beta.
   */
  putV1MSourcescopeWorkspaceConnectorsById(id: string, body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/sourcescope/workspace-connectors/{id}", `/v1/m/sourcescope/workspace-connectors/${encodeURIComponent(id)}`, body, opts);
  }

  /**
   * DELETE /v1/m/sourcescope/workspace-connectors/{id} — Removes a workspace connector and cascade-deletes its workspace-owned sealed secrets.
   *
   * Stability: beta.
   */
  deleteV1MSourcescopeWorkspaceConnectorsById(id: string, opts?: RequestOptions): Promise<Json> {
    return this.do("DELETE", "/v1/m/sourcescope/workspace-connectors/{id}", `/v1/m/sourcescope/workspace-connectors/${encodeURIComponent(id)}`, undefined, opts);
  }

  /**
   * GET /v1/m/voice/decisions — Lists the whole append-only open/close ledger for the tenant, newest first: the page limit keeps the most recent decisions, not the oldest ones.
   *
   * Stability: beta.
   */
  getV1MVoiceDecisions(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/voice/decisions", "/v1/m/voice/decisions", undefined, opts);
  }

  /**
   * GET /v1/m/voice/policies — Lists the voice-open policies.
   *
   * Stability: beta.
   */
  getV1MVoicePolicies(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/voice/policies", "/v1/m/voice/policies", undefined, opts);
  }

  /**
   * PUT /v1/m/voice/policies — Upserts a voice-open policy (default-deny means a policy is what PERMITS an open).
   *
   * Stability: beta.
   */
  putV1MVoicePolicies(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("PUT", "/v1/m/voice/policies", "/v1/m/voice/policies", body, opts);
  }

  /**
   * GET /v1/m/voice/sessions — Lists voice sessions with read-time-derived state, metadata only.
   *
   * Stability: beta.
   */
  getV1MVoiceSessions(opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/voice/sessions", "/v1/m/voice/sessions", undefined, opts);
  }

  /**
   * POST /v1/m/voice/sessions/open — voice module route (requires voice:session:admin)
   *
   * Stability: beta.
   */
  postV1MVoiceSessionsOpen(body: JsonInput, opts?: RequestOptions): Promise<Json> {
    return this.doJsonRequired("POST", "/v1/m/voice/sessions/open", "/v1/m/voice/sessions/open", body, opts);
  }

  /**
   * GET /v1/m/voice/sessions/{ref} — Returns one session's metadata by its reference.
   *
   * Stability: beta.
   */
  getV1MVoiceSessionsByRef(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/voice/sessions/{ref}", `/v1/m/voice/sessions/${encodeURIComponent(ref)}`, undefined, opts);
  }

  /**
   * GET /v1/m/voice/sessions/{ref}/decisions — Lists the append-only open/close ledger for one session.
   *
   * Stability: beta.
   */
  getV1MVoiceSessionsByRefDecisions(ref: string, opts?: RequestOptions): Promise<Json> {
    return this.do("GET", "/v1/m/voice/sessions/{ref}/decisions", `/v1/m/voice/sessions/${encodeURIComponent(ref)}/decisions`, undefined, opts);
  }

  /**
   * GET /v1/m/voice/sessions/{ref}/stream — Serves one session's live metadata as server-sent events, pinned to the request's single authorized tenant.
   *
   * Stability: beta.
   */
  getV1MVoiceSessionsByRefStream(ref: string, opts?: RequestOptions): Promise<string> {
    return this.doRaw("GET", "/v1/m/voice/sessions/{ref}/stream", `/v1/m/voice/sessions/${encodeURIComponent(ref)}/stream`, opts);
  }

}
